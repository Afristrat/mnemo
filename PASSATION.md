# PASSATION — Strate (repo Infra)

> Passation de quart (protocole nucléaire). Le quart suivant reprend **sans relire d'autre fichier que celui-ci** ; pointeurs détaillés en bas.

```
== PASSATION STRATE 2026-05-28T19:40 ==
[ETAT]    prd.json = **60 stories, 50 faites, 10 restantes** (ÉPIC i18n S-057→S-060 + ÉPIC RÉSIDENCE/DR S-043→S-048). **ÉPIC LLM COMPLET** (S-034→040 + 052 + 053). **S-053 ACTIVÉ + ACCESSIBLE EN PROD** (console /admin live, cf. ENCOURS). origin/main = `d1276a1`, **arbre PROPRE**. Qualité (dernier build code) : typecheck 0 · lint 0/0 · **356 unit + 10 skip** · build OK. ✅ **PROD = MAIN** : dernier déploiement applicatif après le fix URL Supabase (deploy `bdi28ky0qt0fx0iiuzo3eff7`, finished) ; les commits suivants (`01a8411` decks business, `d1276a1` scope i18n) sont **docs-only** (n'altèrent pas le build prod). Assistant `/api/llm/chat` vérifié live (cite le chiffre fourni sans inventer + 3 sources web). Proxy LiteLLM `proxy.ai-mpower.com`, `deepseek-v4-flash`.
[ENCOURS] **RAS — checkpoint propre, prod à jour, rien à mi-chemin.** **✅ S-053 (console admin) PLEINEMENT ACTIF + ACCESSIBLE EN PROD** : migration `20260528120000_admin_prompts.sql` appliquée au Supabase self-hosté ; super-admin `medamine.mansouriidrissi@gmail.com` (id `f056b97e-0e14-497c-94b7-1c2d11beba7a`) créé via GoTrue admin + seedé dans `super_admins` (count=1) ; **Supabase exposé publiquement sur `https://db.ai-mpower.com`** (ingress ajouté au tunnel cloudflared → Kong:8200, route DNS déjà sur le tunnel) ; `NEXT_PUBLIC_SUPABASE_URL=https://db.ai-mpower.com` + `ANON_KEY` (buildtime) + `SUPABASE_SERVICE_ROLE_KEY` (runtime) dans Coolify ; redéployé ; **login + rôle super-admin VÉRIFIÉS de bout en bout** (grant password via db.ai-mpower.com → token + RLS self-select renvoie la ligne). Console : **https://infra.ai-mpower.com/admin** (e-mail + mdp du coffre `MNEMO_ADMIN_*`).
[FAIT]    Session 2026-05-28 (4 stories livrées + déployées + vérifiées) : **S-052** (texte libre par bloc intégré via intake LLM borné + narration ; lève dette freeNotes) · **S-056 option A** (heuristiques de dimensionnement → SizingParams injectables + sourcés ; ComponentCandidate porte gpuLoadFactor/storageFactor consommés si dans bornes [0.25,4], sinon repli baseline ; garde-fou reconcile ; snapshot via catalogue gelé) · **S-053 option C** (prompts système versionnés éditables : migration prompts+super_admins RLS, lib/prompts/{registry,store}, client service-role, builders refactorés en gabarit+composePrompt, console /admin login+éditeur ; option C sûre car validateurs serveur bornent la sortie) · **S-040** (assistant Q&A : serializeRecoFacts + buildChatMessages, route /api/llm/chat + searchWeb sourcé, AssistantPanel sur /resultats ; cite SEULEMENT les faits affichés + web sourcé). Fix typage : `TableShape` gagne `Relationships: []` (requis supabase-js v2). **Puis (après 14:20)** : ② **S-053 ACTIVÉ EN PROD** (migration psql via SSH+base64 ; super-admin via API admin GoTrue ; **`db.ai-mpower.com` exposé** via ingress tunnel cloudflared + reload SIGHUP ; env Supabase dans Coolify ; redéploy ; login vérifié). ③ **Livrables business committés** (`docs/`) : `strate-business-model.pptx` (25 slides) + `strate-business-model-canvas.pptx` (5 slides, BMC 9 blocs) + scripts `docs/build/` (agent bizmodel). ④ **`LITELLM_MASTER_KEY` ajoutée au coffre DPAPI** (via presse-papier d'Amine → DPAPI ; routes admin LiteLLM / skill `/update-model`). ⑤ **ÉPIC i18n scopé** (S-057→060, cf. NEXT). Commits `a42eaea`→`d1276a1`.
[ALERTE]  ⚠ **Déploiement** : **`COOLIFY_PUBLIC_URL`** (PAS `COOLIFY_URL` = IP LAN injoignable) ; PS 5.1 → forcer TLS 1.2. · **Supabase prod = self-hosté** (SSH `serveurai`, conteneur `supabase-db`, compose `/home/serveurai/stacks/supabase/docker`, Kong `localhost:8200`) **EXPOSÉ public sur `https://db.ai-mpower.com`** via **Cloudflare Tunnel `cloudflared-nahda`** (config `~/.cloudflared/config-nahda.yml`, ingress `db→localhost:8200`). · ⚠ **`serveurai` N'A PAS de sudo sans mot de passe** → recharger cloudflared par **SIGHUP** (process possédé par serveurai : `kill -HUP $(pgrep -x cloudflared)`), JAMAIS `sudo systemctl restart`. · SQL prod : base64 → `docker exec -i supabase-db psql -U postgres -d postgres` ; **body contenant un secret via `scp` fichier** (PAS stdin PowerShell → encodage casse le JSON). · Tables prod = init_rails + prompts/super_admins ; conversion(S-023)/catalog_obs(S-036) NON appliquées (aucun consommateur prod). · **LLM tokens NON chiffré** (dette). · **Prix = [PLACEHOLDER]**. · **e2e local fragile** (`next start` KO en `output:standalone`) → prod via `E2E_BASE_URL=https://infra.ai-mpower.com`. · Coolify env champ `is_buildtime`. · webhook GitHub→Coolify absent (redeploy manuel API).
[BLOQUE]  RAS. (S-053 activé en prod ; reste = épic résidence, 100 % buildable.)
[NEXT]    **DEUX ÉPICS RESTANTS (10 stories) — chacun idéalement en session fraîche dédiée :**
① **ÉPIC i18n / MULTILINGUE (S-057→S-060, ouvert le 2026-05-28 par Amine)** — ⚠ l'app était **FR-only par convention** (jamais au PRD avant) : AUCUN i18n, AUCUN sélecteur de langue, AUCUNE clé i18n n'existait. Approche décidée : **next-intl** (Next 15 App Router/RSC), **locale en cookie `NEXT_LOCALE` SANS préfixe d'URL** (+ détection Accept-Language) → disruption minimale. Langues **fr (défaut) + en**, puis **ar (RTL) = S-060**. Ordre : **S-057** fondation (next-intl + LocaleSwitcher + messages fr/en + migration accueil/layout en preuve) → **S-058** extraction EXHAUSTIVE des clés UI (wizard/résultats/fiduciaire/sondage/primitives) + garde-fou « zéro chaîne en dur » → **S-059** **prompts soignés + LOCALISÉS** (réécrire les 4 DEFAULT_PROMPTS niveau natif EN gardant placeholders + clauses DÉFCON 1 ; passer la locale aux routes LLM → réponses user-facing dans la langue de l'utilisateur ; livrable/PDF localisés — répond à la demande Amine « prompts ridicules, structure très soignée comme un natif ») → **S-060** arabe + RTL.
② **ÉPIC RÉSIDENCE/DR (S-043→S-048, spec n°2 validée)** : S-043 pricing egress + table conformité transferts → S-044 moteur résidence/DR (conflit exposé) → S-045 scoring **9→10** (`geosov`) → S-046 intégration recommend → S-047 Exit Escrow DR → S-048 UI + radar 10.
Puis : story LLM tokens (prix live) ; backlog test P2 (U5/U7/U8).
[MEMO]    Coffre GLOBAL ~/.claude/secrets/secrets.env.dpapi (load-secrets.ps1, **42 secrets**) : SSH, Coolify (`COOLIFY_PUBLIC_URL`, `COOLIFY_API_TOKEN`, `MNEMO_APP_UUID`=`by7kdehyeieujf6oxzzt1r0m`), Firecrawl, Supabase serveur (`SRV_SUPABASE_*` ; `SUPABASE_SERVICE_ROLE_KEY` désormais posée en prod), sondage, `LITELLM_BASE_URL` + `LITELLM_API_KEY` (**clé VIRTUELLE restreinte `llm_api_routes`** — refusée sur routes admin) + **`LITELLM_MASTER_KEY`** (admin, ajoutée 2026-05-28 ; app Coolify `litellm-proxy` uuid `blmesg55fcrs39723v74q0yt`). Anti-leak : preuve = code HTTP, jamais la valeur ; ajout secret = via presse-papier → DPAPI (jamais le chat). DÉCISIONS AMINE verrouillées : admin prompts = super-admin GLOBAL ; **S-053 édition = option C** (tout éditable, garanties dans les validateurs serveur) ; **S-056 = option A** (paramètres vivants) ; **ÉPIC i18n OUVERT 2026-05-28** (l'app était FR-only ; next-intl + cookie-locale SANS préfixe ; **fr défaut + en** puis **ar/RTL** ; prompts à soigner+localiser = S-059) ; presets ADR-020 ; texte libre INTÉGRÉ (S-052) ; doctrine déterminisme (LLM propose, moteur calcule). Mémoires CLÉS : `strate-audit-options-presets-admin` · `strate-reco-vivante-llm-plateforme` · `prix-jamais-hardcodes-feed-live` · `definition-de-termine` · `strate-avis-critique-non-oriente` · `secrets-handling-protocol`. « reprends en Ralph » relit `.ralph/prd.json`+`progress.md`+AGENTS.md AVANT d'agir.
== FIN PASSATION ==
```

---

## COMMENT REPRENDRE
1. Session dans `C:\Users\amans\OneDrive\Projets\Infra`.
2. Charger les accès : `. "C:\Users\amans\.claude\secrets\load-secrets.ps1"` → peuple `$env:*` (DPAPI, déchiffrable sous le compte/machine d'Amine). Dot-sourcer dans CHAQUE commande qui utilise un secret. Protocole anti-leak : jamais afficher une valeur, preuve = code HTTP / résultat.
3. Lire `.ralph/prd.json` + `.ralph/progress.md` + `AGENTS.md` AVANT d'agir (mode Ralph permanent).
4. Vérifs d'entrée : `npm run typecheck && npm test && npm run lint && npm run build` → vert. Prod : `curl -s -o /dev/null -w "%{http_code}" https://infra.ai-mpower.com/health` → 200.
5. Après tout commit de code mergé : **redéployer la prod** : `POST $COOLIFY_PUBLIC_URL/api/v1/deploy?uuid=$MNEMO_APP_UUID&force=true` (Bearer `$env:COOLIFY_API_TOKEN` ; ⚠ `COOLIFY_PUBLIC_URL`, PAS `COOLIFY_URL` qui est l'IP LAN injoignable hors réseau ; PS 5.1 → forcer TLS 1.2), poller `GET .../api/v1/deployments/<deployment_uuid>` jusqu'à `finished`, puis vérifier au **navigateur** (HTML SSR sans le récap/preset, rendus après hydratation) ou via la route concernée. Prod (code) = `d1276a1` (commits `01a8411`/`d1276a1` = docs-only, ne nécessitent pas de redéploiement applicatif). ⚠ Migration Supabase prod = manuelle (SSH + psql, cf. ALERTE), PAS auto au déploiement.

## DÉCISIONS PRODUIT VERROUILLÉES (ne pas re-litiger)
- Cœur = conseil → déploiement ; « recette ouverte, cuisine payante ». Production-ready, zéro dette, DÉFCON 1.
- ±30 % assumé ; **prix JAMAIS hardcodés** (feed live + seed de repli daté) — étendu aux **choix de composants** (catalogue vivant, spec n°3).
- **Reco vivante = « tout vivant + filet »** (spec n°3) : web+LLM reconsidèrent tout, MAIS calcul de coût déterministe sur prix sourcés, snapshot daté reproductible, garde-fou DÉFCON 1, **le LLM ne calcule jamais un coût**. LLM au niveau plateforme via proxy LiteLLM (super-admin GLOBAL hors tenant — décision S-053).
- **Presets = modèle scoré/explicable** (ADR-020, S-051) : déclencheurs durs → HARD ; sinon score de besoin (volume dominant) → LIGHT (≤3) / MEDIUM ; étiquettes des profils-types réconciliées (test de garde). Score + drivers exposés dans `presetReason`.
- **Aucune option collectée ne doit rester sans effet** (DÉFCON 1) : growth/latency intégrés au compute (S-049) ; test de garde anti-dette.
- Trio de moats : ① Exit Escrow ② Fiduciary (zéro commission cachée) ③ Intelligence Network.
- **Honnêteté brutale** : tensions exposées, jamais résolues en douce (ex. résidence stricte × DR hot, spec n°2).
- Scoring : 9 dimensions (resilience ajoutée S-027) ; spec n°2 prévoit la 10ᵉ `geosov`.
- **Multilingue (décision 2026-05-28)** : l'app était FR-only par convention ; Amine OUVRE l'i18n (épic S-057→060). next-intl, **locale en cookie sans préfixe d'URL**, **fr (défaut) + en** puis **ar (RTL)**. Garde-fou « zéro chaîne en dur ». Les prompts seront réécrits niveau natif + localisés (S-059), en conservant les garde-fous DÉFCON 1.

## POINTEURS
- **Specs** : `docs/superpowers/specs/2026-05-27-strate-backup-design.md` (n°1, livrée) · `…-llm-plateforme-reco-vivante-design.md` (n°3, en cours S-037→S-040, S-052/053) · `…-strate-residence-dr-design.md` (n°2, validée, → S-043→S-048).
- **Plans** : `docs/superpowers/plans/2026-05-27-strate-backup.md` · `…-llm-plateforme-reco-vivante.md`.
- **Test utilisateur** : `docs/test-feedback-2026-05-27.md` (26 findings priorisés P0-P3 ; bugs P0 résolus par redéploiement).
- **Pricing sourcé** : `docs/pricing/{media,backup,compute}-cost-sources.md`.
- **Livrables business (2026-05-28)** : `docs/strate-business-model.pptx` (25 slides) · `docs/strate-business-model-canvas.pptx` (BMC 9 blocs) · scripts `docs/build/`.
- **Ralph** : `.ralph/prd.json` (**60 stories, 50 faites, 10 restantes = épics Résidence/DR + i18n**) · `.ralph/progress.md` (log + patterns). `PRD.md` · `docs/DECISIONS.md` (jusqu'à **ADR-020**) · `AGENTS.md` · `CLAUDE.md`.
- **Mémoires** : `~/.claude/projects/C--Users-amans-OneDrive-Projets-Infra/memory/MEMORY.md` (dont **`strate-audit-options-presets-admin`** = audit + décisions de cette session).
- **Déploiement** : Coolify app `mnemo` uuid `by7kdehyeieujf6oxzzt1r0m`, domaine `https://infra.ai-mpower.com`.
```
== FIN PASSATION ==
```
