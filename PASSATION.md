# PASSATION — Strate (repo Infra)

> Passation de quart (protocole nucléaire). Le quart suivant reprend **sans relire d'autre fichier que celui-ci** ; pointeurs détaillés en bas.

```
== PASSATION STRATE 2026-06-08 (16) ==
[ETAT]    **FIN SESSION 16 — RELIQUATS DURCISSEMENT (a+b) + VAGUE 3 MOATS (MBOM signé Ed25519 + SLA paramétrique #5) — TOUT DÉPLOYÉ + VÉRIFIÉ PROD. HEAD code = `0f71128`.** Demande Amine : « les deux composantes [du reliquat ①] et le 4 [vague 3] ». 2 forks tranchés par Amine (AskUserQuestion) : SLA #5 = **composition de SLA vendor sourcés** (pas de calibration sur réseau vide = DÉFCON) ; consentement real_cost = **durcissement complet + contribution anonyme tracée**. Qualité dernier commit : typecheck 0 · lint 0-0 (garde i18n) · **783 tests** + 20 skip · build OK · **e2e 45/45** · parité i18n **1394×3** (fr/en/ar). Prod `https://infra.ai-mpower.com` à jour (health 200), déploiements `619d3ca`+`f2acb34`+`0f71128` = `finished` (vérifié coolify-db on-LAN). **TRAVAUX (4 chantiers)** : ① **(a) consentement real_cost SERVEUR** : `/api/network/real-cost` persiste via le **client de SESSION** (RLS appliquée, fin du bypass service-role) ; authentifié → cercle perso + **trace RGPD** (`network_consents`, `lib/network/consent-persist.ts`) ; anonyme conservé ; non bloquant. Pleinement actif dès S-089. ② **(b) RPC `select *` → projection** : `get_shared_reco`→`table(encoded)` ; `get_simulation_by_token`→contenu du rapport SANS share_token/circle_id/created_by. Migration `20260608120000_harden_rpc_projection.sql` **APPLIQUÉE PROD via SSH** (vérifiée `pg_get_function_result`) ; round-trip partage réel prouvé en prod. ③ **MBOM signé Ed25519 (vague 3 T2)** : authenticité (≠ intégrité). `lib/mbom/signature.ts` isomorphe Web Crypto **zéro dépendance** ; route `/api/mbom/sign` (clé privée env serveur, 503 si absente) ; panneau sceller+vérifier. **Clé de prod générée + stockée** (coffre DPAPI `MBOM_SIGNING_KEY_JWK` + env Coolify ; publique `fr65qsGZAOzu+UIVHyKVaMRKVBEplcPSHEnXZMmYAWo=` en CODE) ; **signature PROD vérifiée VALIDE**. ④ **SLA paramétrique #5 (vague 3)** : `lib/sla/` — rapporte les SLA **publiés** des IaaS de la classe d'hébergement (OVH 99,99 % · Scaleway 99–99,5 % · OUTSCALE ~99,95 % · AWS/GCP/Azure 99,99 % multi-AZ ; sourcés+datés 2026-06-08), traduits en indispo/mois. **Modèle honnête « plancher = IaaS »** (couches co-localisées → pas de multiplication série). Auto-hébergé = aucun SLA tiers + IaaS en référence. Panneau monté dans ResultsView. ⑤ **SLA #5 — disponibilité OBSERVÉE (ajout sur remarque Amine)** : la source de vérité de la dispo observée = les **pages de statut publiques** des fournisseurs (incidents réels datés), pas le SLA contractuel ni du synthétique. `lib/sla/status-{source,feed,seed}.ts` (parseurs PURS Statuspage `/api/v2/incidents.json` + GCP `incidents.json` ; fetch injectable, ne lève jamais → repli seed) + route `/api/sla/status?class=` (JSON public → simple fetch borné, PAS de scraper) + section « Disponibilité observée » dans SlaPanel. **Vérifié prod** : Scaleway=incident live, Anthropic=ok live. DÉFCON = faits sourcés, jamais de « % mesuré » fabriqué. **Couverture COMPLÈTE** (`a436906`) : OVH Public Cloud (Statuspage) + AWS/Azure (adaptateur RSS) ajoutés → toutes les classes couvertes ; prod vérifiée toutes classes en LIVE (AWS=2 incidents via RSS). **(— HISTORIQUE sessions ≤15 ci-dessous —)**
[ENCOURS] **SESSION 16 TERMINÉE — 4 chantiers livrés, déployés, vérifiés prod.** Rien en cours.
[FAIT]    Voir [ETAT]. Commits : `619d3ca` (durcissement a+b + migration RPC appliquée prod), `f2acb34` (MBOM Ed25519), `0f71128` (SLA #5 publié), `94e6829` (SLA #5 disponibilité observée). Tous poussés `main`, auto-deploy `finished`, prod vérifiée (signature MBOM valide ; `/api/sla/status` renvoie des incidents live). Migration RPC appliquée prod (SSH on-LAN, vérifiée). Coffre DPAPI : **+1 secret `MBOM_SIGNING_KEY_JWK`** (81 secrets ; backup `.bak-mbomkey-*`). Env Coolify `MBOM_SIGNING_KEY_JWK` créé. **Recherche : adoption crawl4ai.ai-mpower.com `/md`** (préférence Amine).
[ALERTE]  ⚠ **Clé signature MBOM** : privée = coffre DPAPI + env Coolify (jamais affichée) ; publique en CODE (`lib/mbom/signature.ts STRATE_MBOM_PUBLIC_KEY_B64`). Rotation = régénérer la paire, mettre à jour les DEUX (coffre/Coolify + constante code). · ⚠ **SLA** : valeur OUTSCALE (~99,95 %) = communiqué, confiance « à confirmer » (flag `medium`) ; les autres = pages SLA officielles (high). Donnée périssable → re-vérifier les SLA si on s'en sert pour un engagement. · ⚠ **TS 5.7 Web Crypto** : `Uint8Array` nu = `ArrayBufferLike` → `Uint8Array<ArrayBuffer>` + `utf8()`. · ⚠ **e2e import dynamique** : attendre l'effet (bouton actif) avant d'interagir (chunk froid > 5 s standalone). · (Rappels ≤15 valides : auto-deploy code only — `messages/`+`.ralph/` hors watch_paths, partent avec un commit de code ; SSH `192.168.100.8` LAN-only ; migrations prod MANUELLES.)
[BLOQUE]  **S-089 (dette auth) — sur Amine, PREP TURNKEY FAITE (s.16)** : ✅ route `/auth/callback` (échange PKCE, anti open-redirect, origine canonique — déployé/vérifié prod) + redirections magic-link/OAuth câblées + DETTE-VAULT-1 résolue. **Reste à fournir par Amine** (« dès qu'il rentre ») : SMTP réel + apps OAuth Google/GitHub (redirect provider = `https://db.ai-mpower.com/auth/v1/callback`). **Puis Claude wire via SSH** (supabase docker .env serveur) : `SMTP_*`, `ENABLE_EMAIL_AUTOCONFIRM=false`, `SITE_URL=https://infra.ai-mpower.com`, `ADDITIONAL_REDIRECT_URLS=https://infra.ai-mpower.com/auth/callback`, `GOTRUE_EXTERNAL_GOOGLE/GITHUB_*` → restart `supabase-auth` → smoke-test coffre `/compte` + e2e authentifié. **S-091** (arabe exhaustif) = optionnel (relecteur natif). ✅ Aucune migration en attente.
[NEXT]    Pistes, Amine tranche : ① **Relecture native AR** du site entier (les +54 clés ajoutées en s.16 sont MSA machine, flaggées). ② **S-091** arabe exhaustif par lots. ③ **Débloquer S-089** (SMTP+OAuth). ④ **Reliquat (a) pleinement effectif** une fois S-089 actif (le mécanisme serveur est déjà posé — rien à recoder, juste vérifier le parcours authentifié end-to-end). ⑤ Autres moats si Amine veut élargir (MBOM = T2 fait ; SLA #5 fait).
[CTX]     Run quasi-autonome (Amine tranche les forks via 2 questions). Workflow : Explore (cartographie) → décisions → TDD → vérif (typecheck/lint/test/build/e2e + round-trip prod) → commit/push/deploy/vérif prod. Outils : WebSearch/WebFetch (SLA sourcés), SSH (migration + clé), PowerShell (coffre DPAPI + Coolify). `.ralph/progress.md` enrichi (4 patterns : Ed25519 authenticité, SLA plancher IaaS, consentement serveur, e2e import dynamique).
[MEMO]    Coffre GLOBAL `~/.claude/secrets/secrets.env.dpapi` : **NOUVEAU `MBOM_SIGNING_KEY_JWK`** (Ed25519 privée b64, signature MBOM) + existants (SSH, Coolify `MNEMO_APP_UUID=by7kdehyeieujf6oxzzt1r0m`, LiteLLM kimi, Supabase serveur). Anti-leak : preuve = code HTTP / signature valide, jamais la valeur. **Chaîne de preuve = argument commercial central** ; vague 3 ajoute AUTHENTICITÉ (MBOM signé) + DISPONIBILITÉ PUBLIÉE (SLA #5) au socle vague 2 (7 artefacts). Pattern réutilisé : builder pur + (hash SHA-256 | signature Ed25519) + render pur + panneau ; DÉFCON = sourcer/vérifier, jamais fabriquer (SLA réseau vide → composer le publié ; clé absente → 503).
== FIN PASSATION ==
== PASSATION STRATE 2026-06-07 (15) ==
[ETAT]    **FIN SESSION 15 — VAGUE 2 MOAT « CHAÎNE DE PREUVE » COMPLÈTE (7 artefacts) + COPY REFONDUE FR/EN + e2e DÉ-FLAKÉE + LLM REBRANCHÉ (clé dédiée kimi) + DURCISSEMENT PRÉ-LANCEMENT. HEAD code = `79013d7`.** Backlog `.ralph/prd.json` = **95/97 passes=true** ; restent **S-089** (dette auth, BLOQUÉE sur Amine) + **S-091** (arabe exhaustif, optionnel). Qualité dernier commit : typecheck 0 · lint 0-0 (garde i18n) · **753 tests** + 20 skip · build OK · **e2e 41/41** · parité i18n **1320×3** (fr/en/ar). Prod `https://infra.ai-mpower.com` à jour (health 200, /resultats 200) — tous les déploiements de la session = `finished` vérifiés (coolify-db on-LAN). Auto-deploy ACTIF (push code→deploy ; ⚠ `messages/` + `.ralph/` hors watch_paths → redéploiement MANUEL si le commit ne touche QUE de l'i18n/doc ; si le commit touche aussi du code, l'i18n part avec). **TRAVAUX SESSION 15** (5 chantiers) : ① **VAGUE 2 MOAT TERMINÉE — 7 artefacts de preuve opposables** (« Strate ne recommande pas, il PROUVE ») : S-092 Decision Record (s.14) · **S-094 Restore Drill** (cert SHA-256, 2 modes A local/B CI jetable) · **S-095 Résidence continue** (audit par couche + evidence pack + migration `residency_continuity_observations` RLS APPLIQUÉE prod) · **S-096 Lock-in/Exit-Cost Meter** (#4 : coût de sortie = egress sourcé × storageGb ; frais API/engagement « à confirmer » jamais fabriqués) · **S-097 MBOM** (manifeste signé type SBOM, checksum SHA-256/fichier, async hors buildExitBundle) · **S-098 Drift Monitor** (#7 : reco signée vs état vivant rapporté, iso-contraintes). **Pattern réutilisé 7×** : builder PUR + hash SHA-256 isomorphe (`lib/decision/integrity`) + render PUR + panneau collage/vérif. **DÉFCON 1 fondateur** : Strate n'a PAS accès à l'infra vivante → fournit l'instrument (kit) + VÉRIFIE l'artefact rapporté par le client (RTO/juridiction/checksum jamais inventés). Tous montés dans `ResultsView` (zone preuve, mode expert), i18n fr/en/ar. Modules : `lib/restore-drill/*`, `lib/residency/continuity*+evidence`, `lib/exit-cost/meter`, `lib/mbom/*`, `lib/drift/*`. ② **REFONTE COPYWRITING FR+EN** (angle Amine : plus concret/segments + plus percutant) — réécriture des VALEURS (clés/ICU intouchés → parité préservée, zéro changement composant) sur Home (hero/why/difference/how/pricing/diagnostic/cta) + Nav/Intake/VerdictView/QuickProfile/Configurateur/Fiduciaire ; **footer + Metadata + meta-descriptions** réalignés sur « mémoire d'organisation souveraine » (l'ancien « Infrastructure de mémoire IA souveraine » retiré du hero/footer). EN en-GB natif. Récit preuve assumé sans surenchère (★ Outillé coûts / ★ Prouvé risques). **AR : 42 clés réalignées en MSA** — le proxy LiteLLM (pipeline d'origine) était HS (402 budget) → produit à la main, flag « à relire par un natif » (provenance = seed). `<mark>` + parité 1320×3 préservés. **Vérifié au NAVIGATEUR FR + EN + AR** (rendu, rtl, segments, preuve). ③ **e2e FLAKINESS — FIX RACINE (débogage systématique)** : hypothèse précédente « next start incompatible output:standalone » **RÉFUTÉE** (suite en `--workers=1` = 41/41 verte avec le MÊME serveur). VRAIE cause (lue dans error-context Playwright) = **courses d'hydratation dans 2 tests**, amplifiées par ~9 workers // sur machine 22 cœurs (1 serveur Next) : `strate:100 intake` (bouton gaté par état React `text` ; `fill` pré-hydratation perdu → bouton disabled à vie) + `i18n:43 arabe` (`selectOption` + `router.refresh` RSC > 5 s sous charge). Fix **condition-based `expect.toPass`** (re-tente jusqu'à l'effet React appliqué) — appli intacte, pas de worker-cap, pas de timeout masquant. Vérifié : série ✓ · `--repeat-each=3` @9 workers ✓ 123/123 · run normal ✓. + **2 régressions de sélecteurs e2e introduites par les panneaux vague 2 corrigées** (`/RGPD chap. V/` matchait 2 éléments → scopé à la carte Résidence&transferts ; bouton `/Markdown/` matchait export+MBOM → scopé à la carte « Emporter ce plan »). ④ **LLM — CLÉ LITELLM DÉDIÉE STRATE** : l'app tombait en 402 → cause = **plafond budget de la clé PARTAGÉE** (pas un SaaS à recharger ; LiteLLM auto-hébergé). Créé via skill add-key une **clé virtuelle `strate-infra`** scopée FAMILLE kimi/moonshot uniquement (9 modèles), budget **10 $/30 j** (ajustable `update-key`), user_id strate-infra. Inscrite au coffre DPAPI `MNEMO_LITELLM_API_KEY` (+ `MNEMO_LITELLM_MODEL`, backup `.bak-litellm-strate-20260606`, jamais affichée). Posée en env Coolify `LITELLM_API_KEY` (PATCH). **Modèle par défaut fixé EN CODE** `lib/llm/client.ts` `DEFAULT_MODEL="kimi-k2.6"` (car Coolify MASQUE les valeurs d'env au GET + l'app a ses envs DUPLIQUÉS 2 records/clé pré-existant → code = déterministe ; `LITELLM_MODEL` env supprimé). **Vérifié end-to-end** : la dépense de la clé strate-infra MONTE quand l'app appelle (intake prod). ⑤ **REVUE MULTI-AGENTS PRÉ-LANCEMENT + DURCISSEMENT** : 3 sous-agents read-only (sécurité, correctness vague 2, reliquats). Posture jugée SOLIDE (RLS 100 %, secrets confinés, server-only systématique, 0 P0 hors celui ci-dessous). Corrigé+déployé : **P0** (introduit par moi en S-094) = `kit.ts` générait un JSON de certificat INVALIDE → tout le Restore Drill cassé ; corrigé (booléen bash nu) + test + **gap sceller/vérifier comblé** (`sealCertificate` : le panneau SCELLE un cert de drill non scellé → artefact opposable, VÉRIFIE si déjà scellé) ; **rate-limiting** ajouté à 5 routes anonymes coûteuses (transfers→Firecrawl, real-cost, monitoring, share, lead) ; **bornage entrées** regimes/providers ; **sondage** token query→header `Authorization`+`timingSafeEqual` ; **nettoyage `as`** (regimes, residency-continuity, sondage) ; **exit-cost** garde `storageGb>0`. **(— HISTORIQUE session 14 et antérieures ci-dessous, conservé pour référence —)**
[ENCOURS] **SESSION 15 TERMINÉE — vague 2 moat complète, copy refondue, e2e stable, LLM opérationnel, durcissement appliqué ; tout déployé + vérifié prod.** Rien en cours. **2 reliquats de durcissement DÉFERRÉS** (cf. [NEXT]) : (a) real_cost consentement serveur (couplé S-089) ; (b) RPC select*→projection (migration manuelle, RPC non utilisées).
[FAIT]    Voir [ETAT] (5 chantiers). Commits clés : S-094..S-098 (chaîne de preuve), copy (`a94352e`+`714ea35`), e2e fix (`4feb939`+correctifs sélecteurs), LLM (`14c40d5`), P0+durcissement (`79013d7`). Tous poussés sur `main`, auto-deploy `finished`, prod vérifiée (health 200, /resultats 200, + vérifs navigateur des panneaux + dépense clé LLM). Migration `residency_continuity_observations` APPLIQUÉE prod (RLS=t, 2 policies, vérifié SSH on-LAN) + 1 ligne de test insérée puis SUPPRIMÉE.
[ALERTE]  ⚠ **LLM** : la clé `strate-infra` est **kimi-only** (famille kimi/moonshot), budget **10 $/30 j** (relever via skill `update-key` si besoin) ; les autres modèles (claude/deepseek/gemini) sont HORS budget sur le proxy. `DEFAULT_MODEL="kimi-k2.6"` est en CODE (`lib/llm/client.ts`), pas en env. · ⚠ **Coolify env** : le GET `/api/v1/applications/{uuid}/envs` **MASQUE toutes les valeurs** (ressortent vides — ce n'est PAS un bug) ; et l'app a ses env **DUPLIQUÉS** (2 records/clé, uuids distincts, pré-existant) → pour un NON-secret, préférer le code (déterministe) ; pour un secret, PATCH par clé (idempotent, vérifier par effet end-to-end). · ⚠ **LiteLLM 402** = plafond budget d'une clé (jamais « recharger un SaaS ») → relever le cap dans LiteLLM, pas payer dehors. · ✅ **e2e FLAKINESS RÉSOLUE À LA RACINE** (note de dette antérieure « next start/standalone » était FAUSSE, corrigée dans progress.md) — suite stable même sous parallélisme max ; CI a déjà `retries:2`. · ⚠ **Coolify masque les valeurs d'env** → un appel `key/info` LiteLLM ou un effet observable (dépense) sont les seules preuves. · (Rappels session ≤14 ci-dessous, toujours valides : scraping Crawl4AI/SearXNG ; SSH `192.168.100.8` LAN-only, hors-LAN = Coolify public + terminal web ; migrations prod MANUELLES ; veille vendor auto-planifiée `instrumentation.ts`.)
[BLOQUE]  **S-089 (dette auth) — SEUL vrai blocage, sur Amine** : fournir SMTP réel + apps OAuth Google/GitHub (redirection `https://db.ai-mpower.com/auth/v1/callback`) → AUTOCONFIRM=false, activer magic-link+OAuth, smoke-test coffre `/compte`, compléter l'e2e des parcours authentifiés. **S-091** (arabe exhaustif) = optionnel, non-certifiable sans relecteur natif arabe (les 42 clés récemment touchées sont alignées MSA, mais tout le site AR reste machine). ✅ PLUS AUCUNE MIGRATION EN ATTENTE (residency_continuity appliquée).
[NEXT]    Pistes, Amine tranche : ① **2 reliquats durcissement déferrés** — (a) **real_cost consentement SERVEUR** (aujourd'hui gate déclarative client `consent:true` + persist via admin qui BYPASSE RLS ; vrai fix = getUser+circle+lookup `network_consents` → COUPLÉ à S-089, audit anonyme aujourd'hui) ; (b) **RPC `get_simulation_by_token` + jumeau `get_shared_reco` en `select *`** → projection colonnes (migration SQL prod MANUELLE ; RPC actuellement NON appelées par l'app → faible urgence). ② **Relecture native AR** de tout le site (pas que les 42 clés). ③ **S-091** arabe exhaustif (par lots). ④ **Vague 3 moats** : SLA paramétrique (#5, exige le réseau de calibration mûr) ; MBOM signé HMAC/Ed25519 (T2 optionnel). ⑤ Débloquer **S-089** (si Amine fournit SMTP+OAuth).
[CTX]     Session longue, run quasi-autonome (Amine tranche les forks via questions). Workflow discipliné : brainstorming→spec (`docs/superpowers/specs/`)→writing-plans→TDD→vérif (typecheck/lint/test/build/e2e + NAVIGATEUR)→commit/push→deploy→vérif prod. Skills utilisées : superpowers (brainstorming/writing-plans/executing-plans/systematic-debugging), add-key. 3 sous-agents de revue (code-reviewer ×2 + general). Mémoires à jour. `.ralph/progress.md` = log détaillé + Codebase Patterns (chaîne de preuve, e2e hydratation toPass, LiteLLM clé dédiée).
[MEMO]    Coffre GLOBAL `~/.claude/secrets/secrets.env.dpapi` (load-secrets.ps1) : **NOUVEAU `MNEMO_LITELLM_API_KEY`** (clé virtuelle strate-infra kimi-only) + `MNEMO_LITELLM_MODEL=kimi-k2.6` ; + SSH (LAN-only), Coolify (`COOLIFY_PUBLIC_URL` hors-LAN, `COOLIFY_API_TOKEN`, `MNEMO_APP_UUID=by7kdehyeieujf6oxzzt1r0m`), Supabase serveur (`SRV_SUPABASE_*` Kong interne ON-LAN / REST public `https://db.ai-mpower.com`), `LITELLM_BASE_URL`/`LITELLM_MASTER_KEY` (admin proxy). Anti-leak : preuve = code HTTP/dépense, jamais la valeur ; clé générée → coffre DPAPI (jamais le chat). **La « chaîne de preuve » (7 artefacts) est l'argument commercial central** : Strate PROUVE (sortie testée, résidence surveillée, coût de sortie chiffré, ingrédients signés, conformité du déployé vérifiée), au-delà du conseil. Pattern artefact opposable = builder pur + SHA-256 isomorphe + render + panneau. Décisions Amine verrouillées (inchangées) : DÉFCON 1, prix/choix jamais hardcodés, presets scorés ADR-020, souveraineté portée par le preset, i18n next-intl fr+en+ar, honnêteté brutale, multi-segment.
== FIN PASSATION ==
== PASSATION STRATE 2026-06-04 (14) ==
[ETAT]    **FIN SESSION 14 — PRODUIT COMPLET, DURCI, DÉPLOYÉ. HEAD code = `03602e9`.** PRD F1→F15 livré + déployé + vérifié prod (`https://infra.ai-mpower.com`, health 200). Backlog `.ralph/prd.json` = **91/93 stories passes=true** ; restent **S-089** (dette auth, BLOQUÉE sur Amine) + **S-091** (polish arabe exhaustif, optionnel non-certifiable sans relecteur natif). Qualité au dernier commit : typecheck 0 · lint 0-0 (garde i18n) · **705 tests** + 20 skip · build OK · parité i18n **1244×3** (fr/en/ar). Auto-deploy ACTIF (push code→deploy ; ⚠ `messages/` et `.ralph/` hors watch_paths → redéploiement MANUEL après un changement i18n/doc rendu). ✅ **TOUTES LES MIGRATIONS PROD SONT APPLIQUÉES** (confirmé Amine, session 14 ; plus aucune en attente) : init_rails, admin_prompts, shared_reco, leads, conversion_data, catalog_observations, transfer_status_observations, vendor_vault, regime_observations, regime_code_lgpd_appi, health_metrics, real_cost_entries, vendor_alerts, vendor_alerts_insert_harden — toutes RLS=true. **TRAVAUX SESSION 14** (détail dans le bloc « DÉTAIL SESSION 14 » + [FAIT]) : ① **copywriting L99 fr+en** (cohérence en-GB stricte + anglicisme FR ; `b8c2b84`) ; ② **broadcast alertes vendor S-084 — PIPELINE COMPLET** (détection live vs baseline datée → route → persistance RLS dédupliquée → UI `VendorAlertsPanel` ; `3fe8aef`) ; ③ **veille vendor AUTO-PLANIFIÉE dans l'app** (scheduler `instrumentation.ts` + `setInterval` 6 h, **prouvé via logs Coolify** ; la scheduled task Coolify a été abandonnée car `curl` est ABSENT de l'image standalone ; route cron `/api/cron/vendor-watch` protégée `CRON_SECRET` conservée pour déclenchement manuel ; `121526c`/`7d04d40`) ; ④ **durcissement** : INSERT global `vendor_alerts` restreint au service-role (anti-désinformation) + `server-only` ×4 (`f7a97e7`) ; ⑤ **A · Decision Record opposable (S-092)** — dossier figé/haché SHA-256/horodaté/rejouable (décision + alternatives écartées + tensions + sources ; `lib/decision/*` + `DecisionRecordPanel` ; `e2287e3`) ; ⑥ **B · durcissement (S-093)** — validation profil anti-injection (`profileFromUnknown`) + rate-limiting des 6 routes anonymes (30/min/IP) + e2e gardes d'accès (3 verts vs prod : /compte→307, /admin→login only, POST /api/admin/prompts→403 ; `5eb1715`/`004d881`) ; ⑦ **2 audits sous-agents** : MOAT-HUNT vague 2 (consigné `docs/MOAT-HUNT.md`, top 3 = Decision Record ✅ + Restore Drill #2 + Résidence continue #3) + audit sécurité (posture solide, 0 P0). **PROCHAINES PISTES (Amine tranche, cf. [NEXT])** : suite moat vague 2 (#2 Restore Drill, #3 Résidence continue) · reliquats durcissement P2/P3 · débloquer S-089. **(— DÉTAIL SESSION 14 (copywriting, pipeline vendor, scheduler, audits) puis HISTORIQUE sessions 13→1 ci-dessous, conservés pour référence —)** **DÉTAIL SESSION 14 :** Sur demande Amine (3 chantiers). **① Migrations prod** : **4 migrations À APPLIQUER** par Amine (terminal web Coolify, base64 anti-wrap ; hors-LAN, SSH KO) — `regime_code_lgpd_appi` + `health_metrics` + `real_cost_entries` (sessions 12-13) **+ `vendor_alerts` (S-084, session 14)**. Régénérables : `for f in supabase/migrations/2026060113*.sql supabase/migrations/2026060114*.sql supabase/migrations/2026060115*.sql supabase/migrations/2026060116*.sql; do base64 -w0 "$f"; echo; done`. One-liners fournis dans la réponse de session. **② Reliquats** : constat DÉFCON — le reliquat « refresh UI live `ResidencyPanel` » est **DÉJÀ implémenté** (fetch `/api/legal/transfers` + fusion live/flagged + badges, `components/results/ResidencyPanel.tsx` l.58-103) → la passation 13 était périmée dessus. **broadcast vendor alerts (S-084) : PIPELINE COMPLET LIVRÉ + déployé session 14** (demande Amine « pipeline complet ») — `LivePriceItem` enrichi (vendor+baselineEur), `lib/pricing/price-watch.ts` (détection live vs baseline datée, plancher 1 %, status live only, source obligatoire), `vendor-alerts-observation.ts`+`vendor-alerts-persist.ts` (server-only), migration `vendor_alerts` (RLS pivot circle, circle_id null=broadcast public), route `GET /api/network/vendor-alerts`, `VendorAlertsPanel.tsx` monté dans ResultsView, i18n `Results.vendorAlertsPanel` ×3 (parité **1210×3**), +10 tests. Commit `3fe8aef`, **vérifié prod (persisted=3)**. **SESSION 14 suite — veille ACTIVE + durcissement (3 chantiers Amine : cron + durcissement + moat-hunt)** : (a) **veille planifiée** — séparation lecture/veille (`GET /api/network/vendor-alerts` ne persiste plus ; `POST /api/cron/vendor-watch` protégé `CRON_SECRET` persiste), `lib/utils/cron-auth.ts` (pur, testé), dédup d'audit `filterUnpersistedAlerts` (pur) + query existant dans persist (anti-doublon cron/consultation) ; `CRON_SECRET` généré + posé dans l'env Coolify (jamais affiché) ; route cron vérifiée prod (401 sans/mauvais secret). **VEILLE AUTO-PLANIFIÉE DANS L'APP (scheduler intégré)** — la scheduled task Coolify a été ABANDONNÉE (elle échouait : `curl` ABSENT de l'image Next standalone ; `wget`/`node` aussi peu fiables, et l'API Coolify renvoie 500 sur une commande `node`). Solution souveraine et garantie : `instrumentation.ts` (hook Next 15, register() au démarrage serveur) → `lib/network/vendor-watch.ts` `startVendorWatchScheduler` (`setInterval`, 1er cycle à boot+60 s puis toutes les 6 h) appelant `runVendorWatch` (cycle veille→persist, I/O injectables, ne lève jamais ; partagé avec la route `POST /api/cron/vendor-watch` restée dispo pour déclenchement manuel protégé `CRON_SECRET`). Gardes : runtime Node + production + `VENDOR_WATCH_DISABLED=1`. **PROUVÉ EN PROD via logs Coolify** (`GET /api/v1/applications/{uuid}/logs`) : « [vendor-watch] scheduler armé » + « [vendor-watch] cycle — détectées=0, persistées=0 » (0 = scrape à froid en repli/sans variation, gracieux). +3 tests `runVendorWatch`. ⚠ `CRON_SECRET` reste dans l'env Coolify (masqué par l'API) ; la scheduled task Coolify n'existe plus (supprimée). ⚠ Lecture directe `vendor_alerts` (vérif) = REST `https://db.ai-mpower.com/rest/v1/vendor_alerts` + service-role (`SRV_SUPABASE_SERVICE_ROLE_KEY`) — les URLs `SRV_SUPABASE_*` du coffre pointent vers Kong interne `localhost:8200` (ON-LAN only), utiliser le domaine public. À surveiller : le scraper (S-025, Crawl4AI) promeut-il des valeurs live ? (détectées=0 = peut-être scraper en repli — santé du scraper à vérifier séparément si les alertes restent vides). (b) **durcissement (audit avant ouverture)** : **migration `vendor_alerts_insert_harden`** — INSERT global (circle_id null) restreint au service-role (un anon ne peut plus injecter de fausse alerte dans le broadcast — P1 DÉFCON 1) ; `server-only` ajouté à catalog/persist, legal/persist, legal/regime-persist, prompts/store (défense en profondeur). Commit `f7a97e7`, deploy finished, health 200. typecheck 0/lint 0-0/**691 tests**/build OK. **2 AUDITS livrés** (sous-agents) : (1) **MOAT-HUNT vague 2** — top 3 = Decision Record opposable (effort S) + Restore Drill certifié (effort M) + Preuve de résidence continue (effort M) ; chaîne de preuve « Strate ne recommande pas, il PROUVE ». (2) **AUDIT DURCISSEMENT** — posture solide (RLS 100 %, secrets confinés, validation API robuste), AUCUN P0 ; P1 restants À DÉCIDER par Amine : couverture e2e auth/compte + coffre credentials + console admin (parcours sensibles non testés), [le P1 vendor_alerts insert a été CORRIGÉ ci-dessus] ; P2 : rate-limiting routes anonymes LLM/scraper, validation profil `catalog/live`+`residency/providers` (spread brut casté Profile), recoupement consentement real_cost vs network_consents. Reliquat : provisioning live MCP/OAuth (lié S-089 bloqué). **③ Copywriting L99 fr+en LIVRÉ + déployé** (commit `b8c2b84`) : passe de relecture EXHAUSTIVE ; constat honnête = contenu déjà natif L92-95 → corrections de COHÉRENCE seulement, zéro réécriture (anti-régression). EN passé en **en-GB strict** (labor→labour, license(nom)→licence, organizational→organisational, organization's→organisation's, optimization→optimisation, memorize→memorise ×6, Analyze→Analyse, Analyzing→Analysing, 2 guillemets FR égarés → guillemets anglais) ; FR : « Plus efficient »→« Plus efficace ». Clés + arguments ICU intouchés → **parité 1201×3 préservée** ; typecheck 0 · lint 0-0 + garde i18n · **674 tests** · build OK. **S-091 reste passes=false** (raison : relecture native ar EXHAUSTIVE non faite, hors validation DÉFCON sans relecteur natif arabe ; fr+en désormais traités). HEAD code = **`b8c2b84`** ; deploy `cotjc1xcldcaql63v1xqd7wk` déclenché (manuel, car `messages/` hors watch_paths) — **vérifier `status=finished` + prod** (cf. COMMENT REPRENDRE §5). **(— session 13 ci-dessous —)** **RUN AUTONOME MASSIF (session 13) : 11 stories livrées (S-078→S-088) → Lot 2 B+C+D + Lot 3 COMPLETS en code ; + S-090 prix figés + S-091 polish arabe.** Couverture PRD : **Lot 1 (F1-F9) + Lot 2 (F10 provisioning, F11 coffre, F12 monitoring, F13 network) + Lot 3 (F14 migration, F15 re-optim) = TOUS livrés en code**. Qualité à chaque commit : typecheck 0 · lint 0-0 (garde i18n) · **674 tests** + 20 skip · build OK · parité i18n **1201×3**. Backlog `.ralph/prd.json` : **89/91 stories passes=true** ; restent **S-089 (dette auth — BLOQUÉE sur Amine : SMTP réel + apps OAuth)** + **S-091 (polish arabe : passe ciblée FAITE ; exhaustive OPTIONNELLE par lots — base déjà de qualité native)**. HEAD code = **`b145ed2`**. Auto-deploy ACTIF. ✅ **S-090 PRIX FIGÉS** (sondage Van Westendorp terminé, **3 paliers confirmés explicitement par Amine** via question — DÉFCON 1 : AUCUN montant inventé) : Diagnostic gratuit · Essentiel 290 €/mois · Pro 690 €/mois · Souverain+ « sur devis, dès 1 900 €/mois » ; mentions provisoires RETIRÉES fr/en/ar (badge « Tarif indicatif » `Home.pricing.surveyTag` supprimé des messages + du rendu `app/page.tsx` ; `pricing.note` dé-« indicatifs » ; `VerdictView.firmPriceNote` « sondage en cours »→« prix de vente du service »). ✅ **S-091 polish arabe** : l'arabe en ligne est DÉJÀ de bonne qualité native MSA (deepseek + validation technique), PAS un brouillon ; poli `Home.hero` (tiret cadratin, accords, fluidité) + cohérence terminologique prix ; **passe EXHAUSTIVE des 22 namespaces NON faite** (gain marginal vs risque ICU si réécriture aveugle — à faire par LOTS validés SI Amine veut l'exhaustivité). ⚠ **3 MIGRATIONS PROD À APPLIQUER MANUELLEMENT** (SSH hors-LAN KO → terminal web Coolify, one-liners base64 = `for f in supabase/migrations/2026060113*.sql supabase/migrations/2026060114*.sql supabase/migrations/2026060115*.sql; do base64 -w0 "$f"; done` puis `… | base64 -d | docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`) : `regime_code_lgpd_appi` (fix S-077), `health_metrics` (S-081), `real_cost_entries` (S-082). L'auto-deploy déploie le CODE, JAMAIS les migrations DB. Détail du run ↓. **Stories du run** : **S-078** server-only (5 lecteurs secret) + découplage media-feed (dette couplage révélée+corrigée) · **S-079** moteur Infra Health Score (ADR-021) · **S-080** Dispatch Deviation Guide/MEL (ADR-022) · **S-081** UI monitoring + migration `health_metrics` RLS + persist · **S-082** collecte coût réel + migration `real_cost_entries` RLS + UI · **S-083** recalibration continue (proposition, jamais auto) · **S-084** alertes vendor sourcées · **S-085** Guaranteed Migration/In-Switch (bundle double-run+rollback) · **S-086** Continuous Re-optimization (iso-contraintes) · **S-087/088** provisioning guidé (procédures + cadre optim + garde-fou dur jamais créer compte/carte). **2 BUGS DÉFCON corrigés en route** : contrainte CHECK `regime_observations.regime_code` admet lgpd/appi (bug S-077) ; sélecteurs ICU `goIf/noGo` sans tiret (bug S-080, révélé par rendu UI). **(— session 12 ci-dessous —)** **Lot 1 backlog VIDE** + **Lot 2 · A (auth/coffre) LIVRÉ & DÉPLOYÉ** + **S-077 régimes dynamiques COMPLET (T1→T5, session 12 = T5)**. Qualité : typecheck 0 · lint 0-0 (garde i18n) · **611 tests** + 20 skip · build OK · e2e régimes 1/1. ✅ **S-077 T5 (session 12, dernière tranche)** : régimes phares hors-UE **MODÉLISÉS** dans le moteur — `REGULATIONS += lgpd` (Brésil, Lei 13.709/2018) `+ appi` (Japon) ; seed `code` mappé (bresil→lgpd, japon→appi) → pré-cochables ; `mapRegimeCode` LGPD/APPI ; `REGULATION_OPTIONS += lgpd/appi` (+hints) ; **`computeCompliance`** gagne 2 branches d'**actions de conformité SOURCÉES** : LGPD (Art. 41/18/37/38/33/48 — vérifiés ANPD/Planalto, WebSearch 2026-06-01) + APPI (finalité, sécurité, tiers, droits, fuite oblig. 2022, transfrontalier — source PPC, **sans n° d'article** car renumérotés) ; `scores.ts` +1 conf par régime data-protection ; i18n `Engine.diagnostics.compliance.{lgpd*,appi*}` (12) + `Options.regulation.{lgpd,appi,*Hint}` (4) en fr/en/ar (parité). Tout régime non modélisé reste **affichage-seul** (DÉFCON 1). Tests : `diagnostics.test.ts` (nouveau) + cas ajoutés discovery/seed/detector + e2e `regimes.spec.ts`. **S-077 formalisée dans `.ralph/prd.json`** (manquait — la session 11 avait livré T1-T4 sans l'inscrire). **(— session 11 ci-dessous : T1→T4 —)** + **3 BUGS confirmés CORRIGÉS (session 10)** + **S-077 régimes dynamiques T1→T4 LIVRÉS, déployés, vérifiés prod (session 11)**. origin/main = HEAD code = **`293e6a2`** (T4 ; doc `31268e9`). **Auto-deploy ACTIF** (push code → deploy ; doc ignoré). Qualité : typecheck 0 · lint 0-0 (garde i18n) · **605 tests** + 20 skip · build OK. PROD `https://infra.ai-mpower.com` à jour (health 200, `/resultats` 200, `/configurateur` 200). ✅ **S-077 T1→T4 (session 11)** : **T1** seed sourcé 16 juridictions (`lib/legal/regime-seed.ts`, sources officielles vérifiées 2026-06-01) · **T2** veille pure anti-hallucination (`regime-discovery.ts`, calque `residency/discovery`) · **T3** cache+fusion seed∪live + route `POST /api/legal/regimes` + **audit RLS `regime_observations` (migration appliquée prod, RLS=on vérifié)** — testé LIVE prod (`maroc`+résidence clients `bresil` → `source=mixed`, 7 régimes dont 5 live sourcés, 5 obs persistées) · **T4** UI configurateur : `RegimeDetector` (bouton « Détecter pour {pays} » + multi-input `Profile.clientResidence?` + pré-cochage des régimes mappables, cases manuelles maîtres) EN LIGNE (texte présent dans HTML SSR prod). Commits `864d4be`(T1-2)→`5cfcf3a`(T3)→`293e6a2`(T4). **Reste T5** (modélisation moteur LGPD/APPI + e2e). ✅ **3 BUGS CORRIGÉS (session 10)** : ① verdict enrichi (preset+risque saillant+contraintes régimes/souveraineté/géo, commit `aa27b39`) ② prix LLM per-1M dans le panneau fraîcheur (Anthropic Sonnet 4.6 ajouté au seed S-074, sourcé 2026-06-01 ; commit `80b5f68`) ③ fetchs live de `/resultats` bornés par `fetchWithTimeout` (AbortController 10 s ; commit `86a4822`). Déploiements `aa27b39`+`80b5f68`+`86a4822` = **finished** (vérifié base Coolify). ⚠ **Dette auth tracée** (magic-link SMTP, OAuth apps, AUTOCONFIRM→false, `server-only` — §10 spec Lot 2·A ; visibles en prod mais non fonctionnels, décision Amine). ✅ **MIGRATIONS PROD TOUTES APPLIQUÉES** (RLS=true), dont **`regime_observations` (S-077)** ; **CONSOLE ADMIN S-053 ACTIVE**. ⚠ **ARABE = brouillon MSA EN LIGNE, à RAFFINER par relecture native** (corrigeable à chaud via `messages/ar.json` + redeploy ; les clés redundancyPanel ar sont aussi du brouillon MSA). ⚠ NOTE déploiement (rappel) : « Collecting build traces » KO = OOM TRANSITOIRE → relancer.
[ENCOURS] **SESSION 14 TERMINÉE — backlog faisable VIDÉ, produit complet + durci + déployé ; toutes migrations appliquées.** Rien en cours. Pistes ouvertes (Amine tranche, cf. [NEXT]) : ① **suite moat vague 2** (chaîne de preuve, continuité de S-092) — Restore Drill certifié (#2 : sortie Exit Escrow REDÉPLOYÉE en sandbox jetable + checklist + certificat horodaté) · Preuve de résidence continue (#3 : moniteur juridiction × 7 couches) ; ② **S-089 dette auth** (BLOQUÉE — SMTP réel + apps OAuth fournis par Amine → activer + compléter l'e2e des parcours authentifiés coffre/admin) ; ③ **reliquats durcissement P2/P3** (mineurs : consentement real_cost vs network_consents · sondage token query→header · RPC `get_simulation_by_token` select*→projection) ; ④ **S-091 arabe exhaustif** (optionnel, par lots). **(— session 12 : S-077 ci-dessous —)** **S-077 régimes dynamiques COMPLET (T1→T5).** Spec VALIDÉE, 3 décisions verrouillées (entrées = pays cible + résidence clients · moteur = « les deux » modéliser phares + afficher le reste sourcé · persister audit RLS) → `docs/superpowers/specs/2026-05-31-s077-regimes-dynamiques-design.md`. **T5 livré (session 12)** : LGPD/APPI modélisés dans `computeCompliance` avec actions sourcées, i18n ×3, e2e — voir [ETAT]/[FAIT]. ⚠ **NON ENCORE DÉPLOYÉ au moment de l'écriture** : commit `[S-077]` poussé → auto-deploy déclenché (push code), vérifier `status=finished` + prod (cf. COMMENT REPRENDRE §5). **Prochaine story = Lot 2** : **B** (agent provisionnement F10) → **C** (monitoring F12) → **D** (réseau actif F13), chacun cycle spec→writing-plans→subagent-driven (plan de A : `docs/superpowers/plans/2026-05-31-lot2-fondations-securisees.md`). **Dette auth (à lever avant ouverture publique large)** : magic-link (SMTP réel), OAuth Google/GitHub (apps+creds), `AUTOCONFIRM`→false, `server-only` (DETTE-VAULT-1, §10 spec `2026-05-31-lot2-fondations-securisees-design.md`) ; magic-link+OAuth **visibles en prod mais non fonctionnels** (décision Amine) ; smoke-test navigateur du stockage credential `/compte` reste à faire. **Reliquats NON-bloquants** : relecture native `messages/ar.json` (dont clés S-077) ; prix service réel post-sondage (`[PLACEHOLDER]`) ; refresh UI live `ResidencyPanel` (transferts encore du seed `lookupTransferBasis`).
[FAIT]    **Session 2026-06-01 (13, suite) — S-090 prix figés + S-091 polish arabe** (commit `b145ed2`). **S-090** : sondage Van Westendorp terminé ; au lieu d'inventer (interdit DÉFCON 1), j'ai DEMANDÉ via question → Amine a confirmé les 3 paliers (Essentiel 290 / Pro 690 / Souverain+ dès 1 900 €/mois ; Diagnostic gratuit). Figeage = retrait des mentions provisoires fr/en/ar : clé `Home.pricing.surveyTag` (« Tarif indicatif ») supprimée des 3 messages + retirée du rendu `app/page.tsx` (le tableau `TIERS` n'a plus `surveyTag` + bloc badge retiré) ; `Home.pricing.note` dé-« indicatifs » ; `VerdictView.firmPriceNote` → « prix de vente du service ». ⚠ les tests `results.test`/`narrate.test` gardent `firmPriceTier: "[PLACEHOLDER]…"` comme VALEUR MOCK (DisplayVerdict) — c'est normal, pas la vraie valeur i18n (le verdict rend des NIVEAUX, pas un montant). **S-091** : CONSTAT = l'arabe (deepseek + validation technique) est DÉJÀ de qualité native MSA, pas un brouillon ; polish ciblé `Home.hero` (tiret cadratin —, accords, fluidité « تُحوِّل… مقرّك… لا لدى عمالقة أجانب ») + cohérence prix (`المسح قيد التقدم` retiré) ; passe exhaustive 22 namespaces NON faite (gain marginal vs risque ICU). parité 1201×3, typecheck 0 / lint 0-0 / 674 tests / build OK. **(— run principal session 13 ci-dessous —)** **Session 2026-06-01 (13) — RUN AUTONOME : Lot 2 B/C/D + Lot 3 livrés (S-078→S-088, 11 stories + 2 fix DÉFCON).** Méthode : mode Ralph séquentiel, une story à la fois, typecheck/lint/test/build verts + commit/push (auto-deploy) à chaque. **server-only S-078** (`e0ec6e6`) : `import "server-only"` sur vault/server+credentials, supabase/admin, llm/client, pricing/scraper ; stub vitest (`test/server-only-stub.ts` + alias) ; le garde-fou a RÉVÉLÉ que `media-feed.ts` (importé par Wizard/ResultsView clients) tirait scraper via `refreshMediaPriceFreshness` → EXTRAIT dans `lib/pricing/media-freshness.ts` (server-only), media-feed redevient pur/client-safe. **Monitoring F12** : `lib/monitoring/health.ts` (IHS composite pur, 6 dim pondérées, ADR-021, dimension non mesurée EXCLUE du composite) `63c141d` ; `mel.ts` (Dispatch Deviation Guide, ADR-022, no-go sur availability/resilience/compliance critiques) `9fe8a41` ; `signals.ts` (dérive resilience/compliance/budget de la reco) + `MonitoringPanel.tsx` + migration `health_metrics` RLS + `observation.ts`/`persist.ts` + route `/api/monitoring` `4da5c97`. **Network F13** : `real-cost.ts` (comparaison estimé/réel, tolérance 30 %) + migration `real_cost_entries` RLS + `RealCostPanel.tsx` + route gatée consentement `30924ac` ; `recalibrate.ts` (proposition baseline, médiane robuste, jamais auto, n-min+IQR) `33730b2` ; `alerts.ts` (alertes vendor sourcées, dedup, sévérité) `47c12c0`. **Lot 3** : `lib/migration/bundle.ts` (Guaranteed Migration/In-Switch, double-run+rollback, jumeau Exit Escrow) + `MigrationBundle.tsx` `2493230` ; `lib/optimize/reoptimize.ts` (re-optim iso-contraintes, gain>seuil, validation humaine) `c5cc9fb`. **Provisioning F10** : `procedures.ts` (procédures guidées, lien officiel sourcé) + `plan.ts` (cadre optim + **garde-fou dur** `runAgentAction` refuse create-account/enter-card/financial-commitment) + `ProvisioningPanel.tsx` `685c3eb`. **2 FIX DÉFCON** : `regime_code` CHECK +lgpd/appi (`bcbe0cd`, bug S-077) ; ICU select sans tiret `goIf/noGo` (inclus S-081, bug S-080 révélé par le rendu UI — le test ne rendait pas le Message). i18n fr/en/ar parité **1202×3**, **660 tests**, ADR-021/022. Tous les panneaux F12/F13/F10/F14 rendus dans `ResultsView` (mode expert). **(— session 12 ci-dessous —)** **Session 2026-06-01 (12) — S-077 TRANCHE 5 (dernière) → S-077 COMPLET.** Régimes phares hors-UE **modélisés** (décision Amine #2 « les deux » : on modélise ce qu'on peut sourcer rigoureusement, le reste = affichage-seul). **Sourçage DÉFCON 1** (WebSearch + Planalto/ANPD/PPC, 2026-06-01) : **LGPD** Brésil — articles confirmés sur source primaire (41 encarregado · 18 droits · 37 registre · 38 RIPD · 33 transferts internationaux · 48 communication d'incident) ; **APPI** Japon — obligations confirmées via la PPC (finalité d'utilisation · mesures de sécurité · fourniture à des tiers · droits des personnes · notification de fuite oblig. depuis l'amendement 2022 · transferts transfrontaliers), **numéros d'article volontairement omis** (renumérotés par les amendements 2020/2022 → non confirmés). **Code** : `lib/engine/types.ts` `REGULATIONS += ["lgpd","appi"]` (énum = source de vérité → share `encode.ts`/recap/export propagent sans modif) ; `lib/legal/regime-seed.ts` `code` mappé bresil→lgpd / japon→appi (pré-cochables) ; `regime-discovery.ts mapRegimeCode` LGPD/Lei 13.709/APPI ; `lib/wizard/options.ts REGULATION_OPTIONS += lgpd/appi` (+hints) ; `lib/engine/diagnostics.ts computeCompliance` 2 branches d'actions sourcées (6+6) ; `lib/engine/scores.ts` +1 conformité par régime data-protection structurant (cohérent +1 RGPD). **i18n** fr/en/ar : `Engine.diagnostics.compliance.{lgpd*,appi*}` (12) + `Options.regulation.{lgpd,appi,lgpdHint,appiHint}` (4), parité. **Tests** : `lib/engine/__tests__/diagnostics.test.ts` (NOUVEAU, 5 : actions lgpd/appi, cumul, « Aucun »→[], descripteurs purs) + cas ajoutés `regime-discovery` (mapRegimeCode LGPD/APPI ; out[0].code=lgpd) + `regime-seed` (BR→lgpd/JP→appi mappés, POPIA reste libre) + `regime-detector` (pré-coche lgpd+appi, POPIA affichage-seul) + e2e `e2e/regimes.spec.ts` (LGPD/APPI sélectionnables + détecteur présent dans /configurateur). **`.ralph/prd.json` : S-077 formalisée** (la session 11 avait livré T1-T4 sans l'y inscrire — dette de pilotage corrigée). typecheck 0 / lint 0-0 + garde i18n / **611 tests** (+6) / build OK / e2e régimes 1/1. **(— session 11 ci-dessous —)** **Session 2026-06-01 (11) — S-077 régimes dynamiques T1→T4 LIVRÉS, déployés, vérifiés prod.** Spec VALIDÉE (3 décisions Amine). **T1** `lib/legal/regime-seed.ts` : type `RegimeSuggestion` (`code` mappable énum moteur | null, scope, pays, source, confiance, provenance) + seed 16 juridictions, **sources OFFICIELLES vérifiées web 2026-06-01** (RGPD+AI Act UE, nLPD CH, UK GDPR, HIPAA+CCPA US, PIPEDA CA, 09-08 MA, NDPA NG, DPA-2019 KE, POPIA ZA, LGPD BR, LFPDPPP MX, APPI JP, PIPA KR, DPDP IN, PDPL AE/SA) ; `regimesSeedForCountries` (union dédup). **T2** `regime-discovery.ts` (pur, I/O injectée, calque `residency/discovery`) : web→LLM→régimes sourcés, **anti-hallucination d'URL**, `mapRegimeCode`, dédup, confiance live=`low` (conservateur droit S-062), `[]` si I/O KO. **T3** (`5cfcf3a`) `regime-feed.ts` (assemblage SearXNG/Crawl4AI+LiteLLM + **fusion PURE `mergeRegimes`** seed=référence + cache TTL 6 h) + route `POST /api/legal/regimes` (`{country, residences[]}`) + **persistance audit** : migration RLS `regime_observations` (pivot circle, jumeau `transfer_status_observations`) **APPLIQUÉE PROD** (RLS=on vérifié `relrowsecurity=t`, 2 policies) + builder pur + `persistRegimeObservations` (live seulement, jamais bloquant) + prompt `regime-veille` (registry, source unique). **VÉRIF LIVE PROD** : `POST {maroc, residences:[bresil]}` → `source=mixed`, 7 régimes (2 seed CNDP/LGPD + 5 live sourcés : 09-08+cyber 05-20 MA, LGPD+Marco Civil+Code conso BR), mapping `cndp` OK ; **audit `regime_observations`=5 lignes toutes `live`** (seed non persisté=gating OK). **T4** (`293e6a2`) `Profile.clientResidence?` (additif, zéro cascade moteur, round-trip share) + `components/wizard/RegimeDetector.tsx` (sous le champ régimes : multi-input « résidence clients » + bouton « Détecter pour {pays} » borné `fetchWithTimeout` → **pré-coche** mappables via `addRegulations` + **liste** sourcée ; cases manuelles MAÎTRES) + `useWizardProfile.addRegulations`/`toggleClientResidence` + `useOptions.allCountries` + i18n `Wizard.regimeDetector` ×3. **VÉRIF PROD** : `/configurateur` 200 + détecteur présent 4× dans HTML SSR. Tests : seed(6)+discovery(10)+feed(5)+observation(2)+persist(4)+route(3)+RLS gaté+hook(3)+detector(3)+share. typecheck 0 / lint 0-0 / **605 tests** / build OK. ⚠ note : un nom de régime DÉCOUVERT EN LIVE peut arriver mal encodé (mojibake scraper/LLM) — n'affecte pas le seed. **RESTE T5** (modélisation moteur LGPD/APPI + e2e). **(— session (10) ci-dessous —)** **Session 2026-06-01 (10) — Les 3 BUGS confirmés (session 9) CORRIGÉS, déployés au fil, vérifiés prod.** **① Verdict enrichi** (`aa27b39`) : `buildVerdict` rendait `risk`/`gain`/`firmPriceTier`/`nextStep` identiques entre profils opposés. Désormais `salientRisk` (incohérence détectée → sinon secret/secret-pro→`verdict.risk.secret`, régimes→`verdict.risk.regulated` plural, preferSovereign→sovereignty) ; `gain`+`firmPriceTier` = `select` ICU sur preset (firmPriceTier abandonne le « 290 € » inventé → niveau d'offre seul) ; `nextStep` = 4 cas (setup × régime) ; **nouveau `Verdict.constraints`** (preset+sensibilité+régimes+souveraineté+géo, descripteurs purs) en chips dans `VerdictView` (+ `constraintsLabel`). Cascade types/narrate(DisplayVerdict préservé par mergeVerdictNarration)/ResultsView/messages `Engine.verdict.*` ×3. **② Prix LLM per-1M** (`80b5f68`) : `PriceFreshness` affichait l'empreinte brute (« $0.01 · $0.02 … ») pour les couches LLM. `llm-token-seed.ts` ajout **Anthropic Claude Sonnet 4.6** (3/15 $/1M, relevé page officielle 2026-06-01 — sourcé+daté DÉFCON 1) + `LLM_FRESHNESS_TOKEN_USD` ; `feed.ts` `PriceObservation.tokenPricing` + `withLlmTokenPricing` (couches 1=Anthropic / 4=Mistral → per-1M, empreinte effacée ; infra inchangés) ; i18n `Results.freshness.tokenInOut` ×3. **③ `/resultats` ne s'idle jamais** (`86a4822`) : les 3 fetchs live (narration/catalogue/prix) sans timeout. **`lib/utils/fetchWithTimeout.ts`** (AbortController 10 s, compose signal externe) câblé `ResultsView` ×3 + `LivePriceStatus` + `PriceFreshness`. Chaque bug : typecheck 0 / lint 0-0 / build OK / ICU ×3 ; commit+push (auto-deploy) ; **`aa27b39`+`80b5f68`+`86a4822` = finished (base Coolify), health 200 + `/resultats` 200**. 569 tests (+11). **(— session (9) ci-dessous —)** **Session 2026-05-31 (9) — Revue Amine : 3 correctifs déployés + diagnostic mesuré de 3 bugs.** (1) Copy homepage pricing nettoyé (`ada5971`) : retiré « Van Westendorp/analyse conjointe », « au doigt mouillé », tarif Maroc MAD ; pastille « Tarif indicatif ». (2) Bascules backup step 2 (`df7c090`) : `offsite/airgap/immutable/byok` n'avaient qu'un `ariaLabel` invisible → helper `ToggleRow` (label visible). (3) **Découverte fournisseurs AUTO** (`bf34978`) : `RedundancyPanel` lance la recherche **au chargement (pays du profil) + sur changement, débouncé 500 ms, bouton supprimé** ; pré-remplissage pays toujours non vide (UE→« Union européenne », repli seed) ; `emptyHint` maj ×3 ; test réécrit (5 verts). **DIAGNOSTIC MESURÉ de 3 bugs (curl + repro moteur, PAS corrigés — voir [NEXT]).** Note : repro navigateur prod impossible (page `/resultats` ne s'idle jamais → screenshot/read_page timeout 45 s ; lecture JS bloquée par garde-fou confidentialité). Le `/api/catalog/live` répond en 0,77 s avec contenu **pertinent** (Nindohost/Docker MCP pour profil Maroc) → le catalogue de la vue expert N'EST PAS le contenu hors-sujet. **(— session (8) ci-dessous —)** **Session 2026-05-31 (8) — LOT 2 · A Fondations sécurisées : LIVRÉ, FUSIONNÉ `main`, DÉPLOYÉ, VÉRIFIÉ prod.** Méthode : brainstorming→spec→writing-plans→subagent-driven (8 tâches, un sous-agent/tâche + revues ; branche `lot2-a-fondations-securisees` fusionnée `--no-ff` → `f8ca722`). **Coffre** : chiffrement enveloppe AES-256-GCM (`lib/vault/crypto.ts` pur, durci+revu) + accesseur serveur KEK-env (`server.ts`) + helper store chiffré+audit (`credentials.ts`) ; **migration `vendor_vault.sql`** (vendor_credentials + credential_access + vue méta + RLS **deny-total** sur la table de base) **appliquée prod** (vérifié : RLS=on, 0 policy authenticated, vue OK). **Auth** : `getAuthUser()` (getUser) + session middleware (anonyme préservé, try/catch). **Tenancy** : circle auto (trigger existant) + migration anon→auth (server actions `app/actions/account.ts`). **UI** : `/connexion` (e-mail+mdp principal + magic-link/OAuth) + `/compte` + « Mes accès vendor » + nav, i18n fr/en/ar (parité 26 clés). Typage `Database` étendu sans `as` (+ `buildAdminInserter`). Revue finale : 0 critique ; 2 corrections appliquées (retrait d'un `as`, vérif appartenance avant révocation). **Prérequis posés par Claude (accès délégués)** : KEK générée → coffre DPAPI + env Coolify (jamais affichée) ; GoTrue prod configuré (SITE_URL/API_EXTERNAL_URL publics + AUTOCONFIRM) → **auth e-mail/mdp vérifiée end-to-end en prod** (signup→token + circle owner auto). Déploiement `qfhxagaz…` finished ; `/compte` non-auth → 307 `/connexion` (garde OK). typecheck 0 / lint 0-0 / 558 tests / build OK. **(— session (7) ci-dessous —)** **Session 2026-05-31 (7) — S-076 Géographie CONTINENT → PAYS (déployé via auto-deploy, vérifié prod).** Retour Amine : « pas de continents hors Europe ; États-Unis c'est un pays comme Maroc ». Refonte de l'axe géo SANS toucher moteur/légal : `lib/engine/geography.ts` (6 continents, 22 pays, `deriveZone(pays)`→bucket, `geographyFromZone` legacy) ; `Profile` +continent/+country (requis), **`zone` DÉRIVÉE** ; `Continent`/`CONTINENTS` déplacés dans `types.ts` (supprime cycle providers↔types) ; Wizard = sélecteur Continent(6)+Pays(dépendant) via `setGeography` ; recap « continent · pays » ; RedundancyPanel ouvre sur le continent choisi ; i18n fr/en/ar (Options.continent 6 + Options.country 22 + Wizard.fields/info + recap.label.geography, parité). DÉFCON 1 : aucun statut légal/pays fabriqué (précis UE/Maroc/US, sinon `other` conservateur). Cascade ~40 fabriques test + presets + quick-mapper + share encode/decode (round-trip continent/country). typecheck 0 / lint 0-0 / **546 tests** / build OK. **Auto-deploy `g139ca08…` déclenché par le push → finished ; health 200 ; les 6 continents présents dans le HTML prod de /configurateur (vérifié).** **(— session (6 bis) ci-dessous —)** **Session 2026-05-31 (6 bis) — S-075 CI/CD auto-deploy : FAIT 100% PAR API/DB/CLI (zéro UI).** Réponse à « tu as l'API Coolify, fais-le seul ». Diagnostic : `is_auto_deploy_enabled` DÉJÀ true mais source = « Public GitHub » (`source_id=0`) ⇒ aucun webhook ne livrait les push (la GitHub App org `coolify-afristrat` id 1 est sur dépôts SÉLECTIONNÉS sans mnemo ; l'y ajouter = scope `admin:org` indispo non-interactif). Solution **webhook MANUEL repo→Coolify** (pilotable par API repo-admin) : webhook `Afristrat/mnemo` (id `634221541`) → `coolify.ai-mpower.com/webhooks/source/github/events/manual`, push, json. **PIÈGE résolu** : `manual_webhook_secret_github` CHIFFRÉ en base (cast Laravel `encrypted`, ciphertext 256c) → secret EN CLAIR (40c) via `php artisan tinker` (modèle déchiffre) ; ciphertext ⇒ « Invalid signature ». `watch_paths` (inclusion code) posé par API `PATCH /applications/{uuid}`. **VÉRIFIÉ bout-en-bout** : push code (lib/) → 'Deployment queued' + deploy `gwh1rxup…` finished + health 200 ; push doc-only (.ralph/) → 'Changed files do not match watch paths. Ignoring deployment'. Secret jamais affiché (capté en variable runtime). Détails → mémoire `mnemo-deploiement-coolify` (MAJ). **(— même session, S-073 ci-dessous —)** **Session 2026-05-31 (6) — S-073 COMPLET (multi-continent + redondance), DÉPLOYÉ & VÉRIFIÉ prod.** 5 tranches, chacune typecheck/lint/test/build verts + commit. **T1** (`f5ac29c`) : annuaire fournisseurs souverains PAR CONTINENT — contrat pur `lib/engine/providers.ts` (`Continent` europe/north-america/latam/africa/middle-east/apac, `SovereignProvider`, `providersByContinent`/`coveredContinents`) + seed sourcé `lib/pricing/sovereign-providers-seed.ts` (16 fournisseurs, 6 continents : UE×6, Afrique UniCloud/iXAfrica, Moyen-Orient Core42/STC/e&, APAC Sakura/NTT/NAVER, LATAM Serpro, Canada OVH) + doc `docs/pricing/sovereign-providers-sources.md` + tests (6). **DÉFCON 1** : existence/pays/souveraineté + source datée, **AUCUN prix egress fabriqué**, statut légal inchangé. **T2** (`bcdaa49`) : `EgressVector` étendu (`continent?`/`country?` additifs, coût inchangé) + 7 vecteurs egress annotés (UE×5, NA×2) + `vectorsForContinent` + tests (4). **T3** (`c87c020`) : veille de DÉCOUVERTE par pays — `lib/residency/discovery.ts` (pur) + `discovery-cache.ts` (câblage SearXNG/Crawl4AI+LiteLLM + repli annuaire seed + cache) + route `POST /api/residency/providers`. **Contrainte S-072 ABSOLUE** appliquée 3× (requête + règle dure prompt + re-filtre `providerViolatesConstraints` : souveraineté exigée ⇒ rejet hyperscaler) ; anti-hallucination d'URL ; tests (12). **T4** (`2144e93`) : UI `RedundancyPanel` (sélecteur continent + pays → veille → liste sourcée badge souveraineté/provenance/source → sélection N fournisseurs → topologie redondante) ; i18n `Results.redundancyPanel` fr/en/ar ; rendu dans `ResultsView`(profil actif) ; tests (3). **T5** (`26ade53`) : caveats sur topologie composée — légal conservateur (≥2 juridictions → renvoi « Résidence & transferts », réutilise `lib/legal` S-044, jamais un avis) + coûts=devis (egress chiffré UE/NA, ailleurs à confirmer) ; i18n ×3. **Déploiement `x64q87wjnaabw5oco772z7z2`→finished ; vérifié prod : veille `/api/residency/providers` live → 6 fournisseurs souverains kényans, 0 hyperscaler (S-072 OK).** Learning clé : annuaire d'EXISTENCE (sans prix) = bon découpage DÉFCON 1 quand l'egress par fournisseur n'est pas sourçable sans devis (on ne shoehorne pas dans `EgressVector`). **(— session (5) ci-dessous —)** **Session 2026-05-31 (5) — DÉPLOIEMENT GLOBAL prod (remote-control).** (0) **Veilles S-036/S-062 : persistance d'audit câblée** (`persist.ts` catalog+legal, gatée live/flagged, service-role, non bloquante ; routes catalog/live+legal/transfers ; +8 tests → 502) — déployée (`62786c7`/`zekd1xasnn90mch6dwe2ju2y`), **vérifiée prod : 2 observations écrites par une veille live**. + correction trace « tokens LLM » (non chiffré = non coûté, ≠ sécurité). (1) **3 migrations appliquées** en prod via SSH+psql stdin `ON_ERROR_STOP` (conversion_data S-023 : simulation_log+lead_capture ; catalog_observations S-036 ; transfer_status_observations S-062) — RLS=true vérifiée sur les 4 tables. (2) **Console admin S-053 confirmée active** (tables+seed super_admin Amine+service-role key déjà en place — la passation la croyait à finaliser, elle l'était déjà). (3) **Déploiement HEAD `06fdd70`** (`ajgi7ff9m8jsi6fkq97nwj9z`→finished) → **arabe + RTL en ligne** (vérifié fr/en/ar + dir). Arabe = brouillon MSA, à raffiner par relecture native (réversible). **(— session (4) : S-060 livré —)** **Session 2026-05-31 (4) — S-060 (arabe + RTL) → ÉPIC i18n COMPLET.** **Tranche 1** (`7d12cd4`, infra RTL neutre fr/en) : `localeDir` + `<html dir>` ; migration physique→logique (`text-start/end`, `ps-`, `start-`, `marginInlineStart`) ; `:lang(ar)` police arabe (fallback système) ; `[dir=rtl] .font-mono` LTR (données mono). **Tranche 2** (`fac98b0`, arabe activé) : `messages/ar.json` **généré via le proxy LiteLLM du projet** (`deepseek-v4-flash`, namespace par namespace + découpe récursive anti-502) ; **validé techniquement** (parité 975/975×3 ; ICU parse `ar` 0 échec ; arguments ICU identiques via AST formatjs ; clés `select` préservées ; pluriels arabes corrects) ; `ar` activé (config + request loader) ; **e2e `ar` 6/6**. ⚠ **NON DÉPLOYÉ** (brouillon MSA, relecture native requise — DÉFCON 1). **(— session 2026-05-31 (3) : S-059 clôturée, 4 résiduels —)** **Session 2026-05-31 (3) — S-059 CLÔTURÉE (4 résiduels i18n), déployé.** **Résiduel 1** (`5cb21fa`) : prose moteur `ensemble.ts` → `Message` (label/intent/assumptions/uncertaintyLabel ; `agreement` reste une union + `select` ICU) — corrige le manque S-058. **Résiduels 2-4** (`7b7eeac`) : (2) `recap.ts buildChoiceRecap` localisé via traducteur `Wizard.recap` injecté (headings/labels/valeurs : managé, Oui/Non, backup select+plural, modalités, mode, moduleItem) ; (3) `precision-notes` préfixes → EN (cohérence prompt) + **constat** : les messages JSON `app/api/**` ne sont JAMAIS affichés (clients localisent leur propre erreur) → pas une lacune ; (4) `FIDUCIARY_CHARTER` → namespace `Fiduciaire` (source unique page+livrable ; `charter.ts` = clés ; `buildDeliverable` gagne `fiduciaryText`). parité 975/975. **(— session 2026-05-31 (2) : S-059 parties 1+2 — prompts EN + locale routes + livrable —)** **Session 2026-05-31 (2) — S-059 PARTIES 1+2 (prompts + livrable), déployé.** **PARTIE 1** (`4f72feb`) : 5 `DEFAULT_PROMPTS` réécrits en **ANGLAIS structuré natif** (ROLE/TASK/RULES/OUTPUT ; placeholders + clauses DÉFCON 1 préservés en EN) ; **langue de sortie pilotée par `{{language}}`** (narration/assistant ; JSON intake/veilles = langue-agnostiques) ; `promptLanguageNames` (i18n/config) ; builders `buildNarrateMessages`/`buildChatMessages` + routes narrate/chat threadent la locale (lue du body, validée `isLocale`, repli `defaultLocale`) ; clients ResultsView/AssistantPanel envoient `useLocale()` ; tests DÉFCON màj EN + flux locale→langue. DÉFCON 1 intact (validateurs serveur bornent la sortie). **PARTIE 2** (`7635d88`) : chrome du **livrable MD/PDF** localisé — `buildDeliverable` gagne `deliverableText` (namespace `Deliverable`) : sections, profil (15), méta, valeurs, provenance, disclaimer via clés ; `renderMarkdown`/`renderPdf` inchangés ; `ExportButtons` câblé ; parité **910/910** ; tests asserten les clés. **(— session 2026-05-31 (1) ci-dessous : RELIQUAT S-058 CLÔTURÉ, 3 tranches vertes —)**
[FAIT-PREC] **Session 2026-05-31 (1) — RELIQUAT S-058 CLÔTURÉ (3 tranches vertes, déployé).** **TRANCHE 6 (a) MODULES** (`4f49ccc`) : `EngineModule`/`ActiveModule` (name/why/baseTask/levels) → `Message` ; `ActiveModule.task` pré-composé (jamais lu) SUPPRIMÉ → helper pur `formatModuleTask` + `Engine.modules.taskTemplate` ; `scores.moduleBonus` en **`select` ICU sur `modId`** (le nom n'est plus une valeur ICU) + **test de parité anti-drift** ; consommateurs câblés (ModuleSlider/CostMap/export/bundle + `buildChoiceRecap` gagne `EngineResolver`/Wizard) ; `messages Engine.modules.*` EN natif. **TRANCHE 7 (b) VERDICT** (`95ef5db`) : `Verdict.{pain,gain,firmPriceTier,nextStep}` → `Message` (risk déjà fait) ; `pain` = `select` ICU sur l'activité **NORMALISÉE** (`-`→`_` : tiret INTERDIT en clé select ICU) avec branche `other`=défaut ; `ResultsView` résout les 5 champs vers `DisplayVerdict` ; constantes prose supprimées. **TRANCHE 8 (c) GARDE+E2E** (`cf34062`) : **garde CI** `scripts/check-no-hardcoded-ui-strings.mjs` (scan des chaînes ACCENTUÉES dans components+app **hors app/api** ; câblée `npm run lint` + script `check:i18n`) ; corrections UI surfacées (**StatusDot** i18n ; **page /resultats** était BRUTE — sans accents donc invisible au scan accent — → `generateMetadata`+header i18n ; **LayerStack** alternatives/inclus/perMonth) ; helper LLM-context **`buildOtherPrecisionNotes`** (lib/llm) extrait de ResultsView (+4 tests) ; **e2e `e2e/i18n.spec.ts`** (4 verts : bascule accueil fr→en→fr + **verdict rendu EN** = preuve i18n MOTEUR navigateur). Parité messages **863/863**. **(— sessions précédentes ci-dessous —)** **Session 2026-05-30 (suite).** **(1) FIX lien court partage** : cause racine = un `INSERT … RETURNING` est soumis à la politique SELECT (absente pour `anon` sur `shared_reco`) → l'insert anonyme échouait TOUJOURS (« new row violates RLS ») → 503 « Création du lien court impossible ». CORRIGÉ : id généré SERVEUR (`randomUUID`), insert SANS `.select()` (le moat anti-énumération via RPC SECURITY DEFINER `get_shared_reco` reste intact ; `SharedRecoInsert` gagne `id?`). + **QR code** du lien court (`qrcode.react`, rendu LOCAL, sans appel externe ; l'URL encodée est trop longue pour un QR → QR réservé au lien court). Déployé prod (`26ee408`) + **vérifié end-to-end** (POST `/api/share`→200, GET→encoded, roundtrip OK ; ligne de test purgée). **(2) i18n MOTEUR — 5 TRANCHES VERTES** (prose `lib/engine` → descripteurs `Message`, chacune commit+push, FR identique, EN natif) : **T1** `compliance` · **T2a** `risks`+`kmChecks`+`verdict.risk` (via type frontière **`DisplayVerdict`** = forme résolue tout-chaînes ; `ResultsView` résout le verdict moteur AVANT fusion/narration → `VerdictView`/`mergeVerdictNarration` inchangés) · **T2b** `presetReason` (type **`PresetReason`**={template:Message, drivers:Message[], suffix:Message|null} + helper PUR **`formatPresetReason(reason, resolve)`** exporté de l'engine ; pas de `select` ICU sur volume/sensibilité → clés distinctes car clés à chiffre initial invalides en ICU) · **T3** `WorkloadLine.estimate` · **T4** `backup.VectorDecision.reason` · **T5** résidence `conflict.reason`/`levers` + `interSiteSecurity.note` (+ `resolve` threadé `buildExitBundle→runbook→backupSection`/`residencySection`). **3 EXCLUSIONS ARGUMENTÉES (= data sourcée, JAMAIS i18n, DÉFCON 1)** : `CostSource.label` (vendor + feed live runtime), `Layer.choice/note` (catalogue), `TransferFlag.legalBasis/note` (veille légale seed + LIVE LLM runtime). Pattern UI : `useEngineText()` → `resolveEngine(msg)` ; pur : résolveur injecté. Tests de prose → asserter `.id`/`.values`. Commits `26ee408`→`2de5e94`. **(— session précédente ci-dessous —)** Session 2026-05-29→30 (énorme). **(A) ÉPIC i18n DÉMARRÉ** : **S-057** fondation next-intl (Next 15 App Router, mode SANS routing → locale cookie `NEXT_LOCALE` + détection Accept-Language ; `i18n/{config,request}.ts` ; `middleware.ts` ; `messages/{fr,en}.json` ; `LocaleSwitcher` ; layout `<html lang>` + `NextIntlClientProvider` ; accueil migré). **S-058 (tranche CHROME, marquée passes=true)** : architecture moteur i18n = **descripteur `Message {id,values}` PUR** (`lib/engine/message.ts`) résolu en ICU fr/en par `lib/i18n/engine.ts` (`useEngineText`) + résolveur injecté `EngineResolver` dans le code pur (`buildDeliverable`, `buildExitBundle`, `serializeRecoFacts`, `buildChoiceRecap`) ; **scores + couches déjà convertis** ; `options.ts` clé-based + hook `useOptions` ; **tout le CHROME bilingue** (accueil, verdict, page résultats + sous-composants, configurateur+blocs, pages /configurateur/fiduciaire/sondage/admin, prix). Provider de test = **`test/render.tsx`** (ré-exports EXPLICITES ; `export *` masque le render custom → piège). **(B) FIRECRAWL REMPLACÉ** (crédits cloud épuisés HTTP 402) par backend AUTO-HÉBERGÉ : `lib/pricing/firecrawl.ts → scraper.ts` = SearXNG (recherche) + Crawl4AI (`/md`) + extraction par Ollama local (LiteLLM). **(C) Features** : S-064 (saisie « Autre » intégrée reco) · S-065 (clarté prix repli + diagnostic) · S-066 (toggle souveraineté → bump preset) · S-067 (partage : lien encodé URL + lien court Supabase) · S-068 (lead-gate : recette experte derrière nom+email) · S-069 (régime « Aucun » exclusif) · S-071 (scroll wizard haut de page). **(D) Migrations prod appliquées** : `shared_reco` + `leads` (RLS, vérifié end-to-end `/api/lead`→200). Commits ~`f2bad69`→`e188bee`.
[ALERTE]  ⚠ **SCRAPING (NOUVEAU, remplace Firecrawl)** : backend auto-hébergé sur serveurai. **Crawl4AI = app COOLIFY managée** (uuid `a12v92dlfhvnna5f14j1qoza`) exposée **`https://crawl4ai.ai-mpower.com` + token** ; **SearXNG = conteneur BRUT interne** `searxng-strate:8080` (settings json activé, JAMAIS public). App env : `CRAWL4AI_BASE_URL=https://crawl4ai.ai-mpower.com` + `CRAWL4AI_API_TOKEN` + `SEARXNG_BASE_URL=http://searxng-strate:8080`. Extraction JSON = Crawl4AI markdown + `callLLM` (LiteLLM/Ollama). **Firecrawl cloud = crédits épuisés, NE PAS s'y fier** (la clé existe encore mais 402). Détails : mémoire `scraping-selfhosted-crawl4ai-searxng`. · ⚠ **Coolify DNS interne INSTABLE** : une app Coolify n'a qu'un alias horodaté `<uuid>-<ts>` (change à chaque deploy) → pour un appel inter-service interne stable, conteneur BRUT (nom fixe) OU domaine public. · ⚠ **cloudflared (`config-nahda.yml`, tunnel `nahda-tunnel`)** : le **SIGHUP NE RECHARGE PAS l'ingress** en mode `tunnel run` → il faut **RESTART** ; service système `User=serveurai`, `Restart=always`, pas de sudo nopasswd → **tuer le process** (serveurai le possède) → systemd respawn (blip ~1 s). ⚠ NE PAS `pkill -f "<motif présent dans ta propre commande>"` (auto-match → tue ta session SSH) ; matcher un motif absent ou tuer par PID. · ⚠ **ACCÈS INFRA (ON-LAN vs HORS-LAN — IMPORTANT)** : l'IP `192.168.100.8` est **LAN-only**. **HORS LAN** (cas par défaut) : Coolify API via **`$env:COOLIFY_PUBLIC_URL`** (= `https://coolify.ai-mpower.com`, FONCTIONNE — utilisé en session 11) ; **SSH `ssh serveurai@192.168.100.8` ÉCHOUE** (timeout port 22) → les opérations **SSH/psql (migrations DB, requêtes coolify-db)** ne sont PAS disponibles hors LAN → passer par le **terminal web Coolify** (`coolify.ai-mpower.com`, ⚠ one-liner base64 anti-wrap obligatoire) OU une session on-LAN. **SUR LAN** : `$env:COOLIFY_URL` (IP:8000) + SSH directs OK. PS 5.1 → TLS 1.2. **Auto-deploy ACTIF** (webhook manuel repo→Coolify, S-075 ; push code → deploy, push doc → ignoré ; vérif statut via base coolify-db en SSH = on-LAN only, sinon via HTML servi). ⚠ Vérif « finished » d'un commit via base : `select status,commit from application_deployment_queues where commit like '<sha>%'` (SSH on-LAN). · ⚠ **Supabase prod self-hosté** (`supabase-db`, Kong `localhost:8200`, public `https://db.ai-mpower.com`). Migration prod = MANUELLE : `cat migration.sql | ssh serveurai 'docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1'` (stdin, trust loopback ; ÉVITE le quoting heredoc/`$$` → bash les mange). ✅ **TOUTES les migrations prod sont APPLIQUÉES** (confirmé Amine, fin session 14, RLS=true) : init_rails, admin_prompts, shared_reco, leads, conversion_data, catalog_observations, transfer_status_observations, vendor_vault, regime_observations, regime_code_lgpd_appi, health_metrics, real_cost_entries, vendor_alerts, vendor_alerts_insert_harden. ⚠ Migration prod FUTURE = MANUELLE (PAS auto au déploiement) : hors LAN → terminal web Coolify, one-liner `echo '<BLOB_BASE64>' | tr -d '[:space:]' | base64 -d | docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1` (BLOB = `base64 -w0 mig.sql`) ; on LAN → `cat mig.sql | ssh serveurai 'docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1'`. ✅ **Veille vendor AUTO-PLANIFIÉE** (S-084) : scheduler intégré `instrumentation.ts`+`setInterval` 6 h, PROUVÉ via logs Coolify (`GET /api/v1/applications/{uuid}/logs` → « [vendor-watch] cycle ») ; PAS de scheduled task Coolify (`curl` absent de l'image standalone) ; `CRON_SECRET` en env Coolify (masqué par l'API). Lecture directe `vendor_alerts` (vérif) = REST `https://db.ai-mpower.com/rest/v1/vendor_alerts` + `SRV_SUPABASE_SERVICE_ROLE_KEY` ; ⚠ les URLs `SRV_SUPABASE_*` du coffre pointent vers Kong interne `localhost:8200` (ON-LAN only) → utiliser le DOMAINE PUBLIC. ✅ **Veilles S-036/S-062 : persistance d'audit CÂBLÉE + vérifiée prod.** ✅ **`ResidencyPanel` refresh UI live FAIT** (fetch `/api/legal/transfers` + fusion live/flagged + badges ; l'ancienne passation le croyait « à faire » → périmé). · **Console /admin S-053 ACTIVE** (super_admin Amine seedé, service-role key en env). ✅ **Prix service FIGÉS** (S-090 : Diagnostic gratuit · Essentiel 290 · Pro 690 · Souverain+ dès 1 900 €/mois). · **Coût d'inférence LLM à l'usage NON TARIFÉ** (« non chiffré » = pas coûté en €, ≠ sécurité ; flaggé « à chiffrer en devis ») — dette de complétude prix, pas un risque. · **Arabe en ligne = qualité native MSA** (deepseek + validation technique ; polish ciblé) ; passe exhaustive ar par lots = optionnelle (S-091).
[BLOQUE]  **S-089 (dette auth) — SEUL VRAI BLOCAGE, sur Amine** : fournir SMTP réel + apps OAuth Google/GitHub (redirection `https://db.ai-mpower.com/auth/v1/callback`) → repasser AUTOCONFIRM=false, activer magic-link+OAuth, smoke-test navigateur du coffre credential `/compte` + compléter l'e2e des parcours authentifiés. NE PAS marquer faite sans les vrais éléments (DÉFCON 1). `server-only` déjà fait. **S-091** (arabe exhaustif) = optionnel, non-certifiable sans relecteur natif arabe. ✅ **PLUS AUCUNE MIGRATION EN ATTENTE** (toutes appliquées, confirmé Amine session 14). ✅ S-090 prix figés, S-092 Decision Record, S-093 durcissement = FAITS.
[NEXT]    **Produit complet (91/93 passes=true), durci, déployé ; toutes migrations appliquées. Plus rien de bloquant à faire côté Claude.** Pistes, Amine tranche (cf. réponse session 14) :
  **① Suite moat vague 2 (chaîne de preuve, continuité de S-092 Decision Record)** : **Restore Drill certifié (#2)** — sortie Exit Escrow REDÉPLOYÉE dans un sandbox jetable + checklist (services up, dump rechargé, embeddings rejouables, 1 requête répond) + RTO réel mesuré + certificat horodaté ; **Preuve de résidence continue (#3)** — moniteur juridiction × 7 couches + drapeau si backup/monitoring sort de zone. Détail + sources : `docs/MOAT-HUNT.md` (vague 2).
  **② S-089 dette auth (BLOQUÉE Amine)** : fournir SMTP + apps OAuth Google/GitHub (redirection `https://db.ai-mpower.com/auth/v1/callback`) → AUTOCONFIRM=false, activer magic-link+OAuth, smoke-test `/compte`, puis compléter l'e2e des parcours AUTHENTIFIÉS (coffre credentials, édition admin — aujourd'hui seules les GARDES d'accès sont testées, `e2e/auth-guards.spec.ts`).
  **③ Reliquats durcissement P2/P3** (mineurs) : recouper consentement real_cost avec `network_consents` ; sondage token query→header Authorization (+ comparaison constant-time) ; RPC `get_simulation_by_token` select*→projection de colonnes.
  **④ S-091 arabe exhaustif** (optionnel) : par LOTS de namespaces, parité+ICU à chaque lot, NE JAMAIS toucher aux arguments `{x}`/`select`/`plural`.
  Patterns consignés dans `.ralph/progress.md` (Codebase Patterns).
[MEMO]    Coffre GLOBAL `~/.claude/secrets/secrets.env.dpapi` (load-secrets.ps1, **~75 secrets**) : SSH (`serveurai@192.168.100.8` **LAN-only** — hors LAN inaccessible, cf. [ALERTE], clés `serveurai_mnemo`/`serveurai_key`), Coolify (`COOLIFY_URL`=IP:8000 on-LAN / **`COOLIFY_PUBLIC_URL`=`https://coolify.ai-mpower.com` hors-LAN**, `COOLIFY_API_TOKEN`, `MNEMO_APP_UUID`=`by7kdehyeieujf6oxzzt1r0m`), Firecrawl (épuisé), Supabase serveur (`SRV_SUPABASE_*`, `SRV_SUPABASE_SERVICE_ROLE_KEY` ; ⚠ URLs `SRV_SUPABASE_*` = Kong interne `localhost:8200` ON-LAN, REST public = `https://db.ai-mpower.com`), LiteLLM (`LITELLM_BASE_URL`+`LITELLM_API_KEY` clé restreinte + `LITELLM_MASTER_KEY`), `MNEMO_ADMIN_*` (console /admin). ⚠ `CRON_SECRET` posé dans l'env Coolify (masqué par l'API ; jamais dans le coffre) — sert la route cron `/api/cron/vendor-watch` (le scheduler intégré n'en a pas besoin). Anti-leak : preuve = code HTTP, jamais la valeur ; ajout secret = presse-papier → DPAPI (jamais le chat). DÉCISIONS AMINE verrouillées : déterminisme (LLM propose, moteur calcule, jamais de coût inventé — DÉFCON 1) ; prix/choix JAMAIS hardcodés (feed live + seed daté + garde-fou) ; **i18n = next-intl, cookie sans préfixe, fr défaut + en puis ar/RTL, périmètre moteur MAXIMAL** ; **souveraineté = preset porte la stack** (toggle preferSovereign bump le preset) ; presets ADR-020 ; honnêteté brutale (tensions exposées) ; multi-segment (pas de référent unique). Mémoires CLÉS : `scraping-selfhosted-crawl4ai-searxng` · `mnemo-deploiement-coolify` · `mnemo-infra-credentials` · `prix-jamais-hardcodes-feed-live` · `jamais-graver-decision-perissable` · `strate-avis-critique-non-oriente` · `secrets-handling-protocol` · `autonomous-run-protocol`. « reprends en Ralph » relit `.ralph/prd.json`+`progress.md`+AGENTS.md AVANT d'agir.
== FIN PASSATION ==
```

---

## COMMENT REPRENDRE
1. Session dans `C:\Users\amans\OneDrive\Projets\Infra`.
2. Charger les accès : `. "C:\Users\amans\.claude\secrets\load-secrets.ps1"` → peuple `$env:*` (DPAPI, déchiffrable sous le compte/machine d'Amine). Dot-sourcer dans CHAQUE commande qui utilise un secret. Anti-leak : jamais afficher une valeur ; preuve = code HTTP / résultat.
3. Lire `.ralph/prd.json` + `.ralph/progress.md` + `AGENTS.md` AVANT d'agir (mode Ralph permanent).
4. Vérifs d'entrée : `npm run typecheck && npm run lint && npm test && npm run build` → vert. Prod : `curl -s -o /dev/null -w "%{http_code}" https://infra.ai-mpower.com/health` → 200.
5. Après tout commit de code mergé : **redéployer la prod** : `GET $COOLIFY_URL/api/v1/deploy?uuid=$MNEMO_APP_UUID&force=false` (Bearer `$env:COOLIFY_API_TOKEN` ; sur le LAN d'Amine `COOLIFY_URL`=IP marche, sinon `COOLIFY_PUBLIC_URL` ; PS 5.1 → TLS 1.2). Poll : `GET /api/v1/deployments/<deployment_uuid>` → `status`. Vérifier au **navigateur** ou via la route. ⚠ **Un commit DOC ne nécessite PAS de redéploiement.** ⚠ Build échoue à « Collecting build traces » = **OOM TRANSITOIRE** du build container (`next build` gourmand, PAS un bug code) → **relancer** ; logs build = `docker exec coolify-db psql -U coolify -d coolify -t -A -c "select logs from application_deployment_queues where deployment_uuid='<uuid>'" | jq -r ".[].output"`. ⚠ Migration Supabase prod = MANUELLE (SSH + psql stdin, cf. ALERTE), PAS auto au déploiement.
6. **i18n** : nouvelle chaîne UI → `useTranslations` + `messages/{fr,en}.json` (EN natif, accents FR sur majuscules). Test rendant un composant à `useTranslations` → importer depuis `@/test/render`. Le moteur reste PUR → renvoie un `Message {id,values}`, jamais de prose.

## DÉCISIONS PRODUIT VERROUILLÉES (ne pas re-litiger)
- Cœur = conseil → déploiement ; « recette ouverte, cuisine payante ». Production-ready, zéro dette, DÉFCON 1.
- ±30 % assumé ; **prix ET choix de composants JAMAIS hardcodés** (feed/veille live + seed de repli daté + garde-fou ; le LLM ne calcule jamais un coût).
- **Reco vivante = « tout vivant + filet »** ; LLM au niveau plateforme via proxy LiteLLM (super-admin GLOBAL, S-053).
- **Presets = modèle scoré/explicable** (ADR-020) ; **souveraineté portée par le preset** (toggle `preferSovereign` → bump LIGHT→MEDIUM→HARD, S-066).
- **Aucune option collectée ne reste sans effet** (DÉFCON 1, tests de garde).
- Trio de moats : ① Exit Escrow ② Fiduciary ③ Intelligence Network. **Honnêteté brutale** (tensions exposées). Scoring **10 dimensions** (`resilience` S-027, `geosov` S-045).
- **i18n (2026-05-28→30)** : next-intl, locale cookie sans préfixe d'URL, **fr (défaut) + en** puis **ar (RTL)**. Refactor moteur → descripteurs `Message` (périmètre MAXIMAL). Prompts réécrits niveau natif + localisés (S-059). Garde « zéro littéral ».
- **Scraping souverain (2026-05-30)** : Firecrawl cloud abandonné (crédits) → Crawl4AI (Coolify, public+token) + SearXNG (interne) + Ollama local.

## POINTEURS
- **Specs** : `docs/superpowers/specs/` (n°1 backup, n°2 résidence/DR, n°3 LLM/reco vivante — toutes livrées).
- **Ralph** : `.ralph/prd.json` (**97 stories, 95 FAITES (restent S-089 auth bloquée + S-091 arabe optionnel) ** ; PRD F1→F15 livré en code : Lot 1 + Lot 2 A/B/C/D + Lot 3 + i18n fr/en/ar + régimes dynamiques + prix figés + S-092 Decision Record + S-093 durcissement) · `.ralph/progress.md` (log + Codebase Patterns détaillés : i18n moteur `Message`/`DisplayVerdict`/`select` ICU/garde `check:i18n` ; `server-only`+découplage ; IHS/MEL ADR-021/022 ; recalibration ; migration bundle ; garde-fou agent provisioning ; veille auto-planifiée `instrumentation.ts` ; Decision Record `lib/decision/*` hash SHA-256 ; rate-limit `lib/utils/rate-limit.ts`).
- **Pricing sourcé** : `docs/pricing/{media,backup,compute,residency}-cost-sources.md`. **ADR** : `docs/DECISIONS.md` (jusqu'à ADR-022 : IHS composition, MEL dispatch).
- **Mémoires** : `~/.claude/projects/C--Users-amans-OneDrive-Projets-Infra/memory/MEMORY.md` (index). CLÉ pour l'infra scraping : `scraping-selfhosted-crawl4ai-searxng`.
- **Déploiement** : Coolify app `mnemo` uuid `by7kdehyeieujf6oxzzt1r0m`, `https://infra.ai-mpower.com`. Services scraping : `crawl4ai.ai-mpower.com` (Coolify `a12v92dlfhvnna5f14j1qoza`) + `searxng-strate` (brut). Supabase `https://db.ai-mpower.com`.
```
== FIN PASSATION ==
```
