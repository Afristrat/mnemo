# PASSATION — Strate (repo Infra)

> Passation de quart (protocole nucléaire). Le quart suivant reprend **sans relire d'autre fichier que celui-ci** ; pointeurs détaillés en bas.

```
== PASSATION STRATE 2026-05-31 (5) ==
[ETAT]    prd.json = **70 stories, 70 FAITES (`passes:true`)** → **ÉPIC i18n S-057→S-060 COMPLET, BACKLOG VIDE**. origin/main = HEAD = `62786c7`. **Arbre PROPRE, tout poussé**. Qualité : typecheck 0 · lint 0/0 + garde i18n · **502 unit + 16 skip** · build OK · **e2e i18n 6/6** (fr/en/ar). parité messages **975/975 × 3 locales**. ✅ **PROD = HEAD `62786c7`, DÉPLOYÉE & VÉRIFIÉE 2026-05-31** (déploiement `zekd1xasnn90mch6dwe2ju2y` → finished ; health 200 ; **fr/en/ar + RTL EN LIGNE** ; `/api/legal/transfers`=200 ; veille juridique live → **2 observations d'audit persistées**, vérifiées en base). ✅ **MIGRATIONS PROD : TOUTES APPLIQUÉES** (conversion_data/catalog_observations/transfer_status_observations posées 2026-05-31, RLS=true vérifiée ; + init_rails/admin_prompts/shared_reco/leads déjà là). ✅ **CONSOLE ADMIN S-053 PLEINEMENT ACTIVE** : `super_admins`+`prompts` présentes, Amine seedé super_admin (`f056b97e…`), `SUPABASE_SERVICE_ROLE_KEY` dans l'env Coolify (vérifié). ⚠ **ARABE = brouillon MSA généré, EN LIGNE mais à RAFFINER par relecture native** (copie UI corrigeable à chaud via `messages/ar.json` + redeploy — pas de chiffre/coût concerné, risque faible et réversible). ⚠ NOTE déploiement (rappel) : « Collecting build traces » KO = OOM TRANSITOIRE → relancer.
[ENCOURS] **Batch directive Amine (remote) — quasi terminé.** prd.json = **74 stories, 72 faites** (S-072 ✅, S-074 ✅) ; ouvertes : **S-073** (multi-continent + fournisseurs par pays + redondance — gros épic, DERNIER), **S-075** (CI/CD auto-deploy — toggle UI 1-clic). **PROD = HEAD `fe947ff` (déploiement `x144g9v5qhgzvflfogb6hxuk` finished, health 200).** FAIT + DÉPLOYÉ ce batch : refresh UI live veille juridique · **S-072 contraintes absolues** (preferSovereign) · persistance audit veilles · **PRIX débloqué** (escalier Gratuit/Essentiel 290 €/Pro 690 €/Souverain+ dès 1 900 €, indicatif, Maroc PPP) · **S-074 tarification LLM** (au 1M in/out, prix sourcés en direct, gating self-host, ligne CostMap, parité 987×3). ⚠ **S-073 (À FAIRE — DÉFCON 1 SENSIBLE)** : touche le modèle `TransferRegion` (union eu/maroc/us/other → continents/pays) ET les **bases légales de transfert par juridiction** (`lib/legal/transfers.ts`). NE PAS fabriquer de statuts juridiques par pays sans recherche (le défaut conservateur `restricted`+disclaimer de `lookupTransferBasis` couvre les paires inconnues). Décompo recommandée : (1) étendre les régions (+ MAJ exhaustive des `Record<Region,…>` : REGION_KEY CostMap/ResidencyPanel, REGION_OPTIONS, labels i18n ×3) ; (2) seed fournisseurs souverains par continent (EgressVector multi-segment, S-043) ; (3) veille découverte fournisseurs par pays **respectant S-072** ; (4) UI choix N fournisseurs redondants + topologie ; (5) coûts/transferts via S-044. **CI/CD (S-075)** : toggle UI Coolify + `watch_paths` (API peu fiable). Relecture `ar.json` : Amine.
[FAIT]    **Session 2026-05-31 (5) — DÉPLOIEMENT GLOBAL prod (remote-control).** (0) **Veilles S-036/S-062 : persistance d'audit câblée** (`persist.ts` catalog+legal, gatée live/flagged, service-role, non bloquante ; routes catalog/live+legal/transfers ; +8 tests → 502) — déployée (`62786c7`/`zekd1xasnn90mch6dwe2ju2y`), **vérifiée prod : 2 observations écrites par une veille live**. + correction trace « tokens LLM » (non chiffré = non coûté, ≠ sécurité). (1) **3 migrations appliquées** en prod via SSH+psql stdin `ON_ERROR_STOP` (conversion_data S-023 : simulation_log+lead_capture ; catalog_observations S-036 ; transfer_status_observations S-062) — RLS=true vérifiée sur les 4 tables. (2) **Console admin S-053 confirmée active** (tables+seed super_admin Amine+service-role key déjà en place — la passation la croyait à finaliser, elle l'était déjà). (3) **Déploiement HEAD `06fdd70`** (`ajgi7ff9m8jsi6fkq97nwj9z`→finished) → **arabe + RTL en ligne** (vérifié fr/en/ar + dir). Arabe = brouillon MSA, à raffiner par relecture native (réversible). **(— session (4) : S-060 livré —)** **Session 2026-05-31 (4) — S-060 (arabe + RTL) → ÉPIC i18n COMPLET.** **Tranche 1** (`7d12cd4`, infra RTL neutre fr/en) : `localeDir` + `<html dir>` ; migration physique→logique (`text-start/end`, `ps-`, `start-`, `marginInlineStart`) ; `:lang(ar)` police arabe (fallback système) ; `[dir=rtl] .font-mono` LTR (données mono). **Tranche 2** (`fac98b0`, arabe activé) : `messages/ar.json` **généré via le proxy LiteLLM du projet** (`deepseek-v4-flash`, namespace par namespace + découpe récursive anti-502) ; **validé techniquement** (parité 975/975×3 ; ICU parse `ar` 0 échec ; arguments ICU identiques via AST formatjs ; clés `select` préservées ; pluriels arabes corrects) ; `ar` activé (config + request loader) ; **e2e `ar` 6/6**. ⚠ **NON DÉPLOYÉ** (brouillon MSA, relecture native requise — DÉFCON 1). **(— session 2026-05-31 (3) : S-059 clôturée, 4 résiduels —)** **Session 2026-05-31 (3) — S-059 CLÔTURÉE (4 résiduels i18n), déployé.** **Résiduel 1** (`5cb21fa`) : prose moteur `ensemble.ts` → `Message` (label/intent/assumptions/uncertaintyLabel ; `agreement` reste une union + `select` ICU) — corrige le manque S-058. **Résiduels 2-4** (`7b7eeac`) : (2) `recap.ts buildChoiceRecap` localisé via traducteur `Wizard.recap` injecté (headings/labels/valeurs : managé, Oui/Non, backup select+plural, modalités, mode, moduleItem) ; (3) `precision-notes` préfixes → EN (cohérence prompt) + **constat** : les messages JSON `app/api/**` ne sont JAMAIS affichés (clients localisent leur propre erreur) → pas une lacune ; (4) `FIDUCIARY_CHARTER` → namespace `Fiduciaire` (source unique page+livrable ; `charter.ts` = clés ; `buildDeliverable` gagne `fiduciaryText`). parité 975/975. **(— session 2026-05-31 (2) : S-059 parties 1+2 — prompts EN + locale routes + livrable —)** **Session 2026-05-31 (2) — S-059 PARTIES 1+2 (prompts + livrable), déployé.** **PARTIE 1** (`4f72feb`) : 5 `DEFAULT_PROMPTS` réécrits en **ANGLAIS structuré natif** (ROLE/TASK/RULES/OUTPUT ; placeholders + clauses DÉFCON 1 préservés en EN) ; **langue de sortie pilotée par `{{language}}`** (narration/assistant ; JSON intake/veilles = langue-agnostiques) ; `promptLanguageNames` (i18n/config) ; builders `buildNarrateMessages`/`buildChatMessages` + routes narrate/chat threadent la locale (lue du body, validée `isLocale`, repli `defaultLocale`) ; clients ResultsView/AssistantPanel envoient `useLocale()` ; tests DÉFCON màj EN + flux locale→langue. DÉFCON 1 intact (validateurs serveur bornent la sortie). **PARTIE 2** (`7635d88`) : chrome du **livrable MD/PDF** localisé — `buildDeliverable` gagne `deliverableText` (namespace `Deliverable`) : sections, profil (15), méta, valeurs, provenance, disclaimer via clés ; `renderMarkdown`/`renderPdf` inchangés ; `ExportButtons` câblé ; parité **910/910** ; tests asserten les clés. **(— session 2026-05-31 (1) ci-dessous : RELIQUAT S-058 CLÔTURÉ, 3 tranches vertes —)**
[FAIT-PREC] **Session 2026-05-31 (1) — RELIQUAT S-058 CLÔTURÉ (3 tranches vertes, déployé).** **TRANCHE 6 (a) MODULES** (`4f49ccc`) : `EngineModule`/`ActiveModule` (name/why/baseTask/levels) → `Message` ; `ActiveModule.task` pré-composé (jamais lu) SUPPRIMÉ → helper pur `formatModuleTask` + `Engine.modules.taskTemplate` ; `scores.moduleBonus` en **`select` ICU sur `modId`** (le nom n'est plus une valeur ICU) + **test de parité anti-drift** ; consommateurs câblés (ModuleSlider/CostMap/export/bundle + `buildChoiceRecap` gagne `EngineResolver`/Wizard) ; `messages Engine.modules.*` EN natif. **TRANCHE 7 (b) VERDICT** (`95ef5db`) : `Verdict.{pain,gain,firmPriceTier,nextStep}` → `Message` (risk déjà fait) ; `pain` = `select` ICU sur l'activité **NORMALISÉE** (`-`→`_` : tiret INTERDIT en clé select ICU) avec branche `other`=défaut ; `ResultsView` résout les 5 champs vers `DisplayVerdict` ; constantes prose supprimées. **TRANCHE 8 (c) GARDE+E2E** (`cf34062`) : **garde CI** `scripts/check-no-hardcoded-ui-strings.mjs` (scan des chaînes ACCENTUÉES dans components+app **hors app/api** ; câblée `npm run lint` + script `check:i18n`) ; corrections UI surfacées (**StatusDot** i18n ; **page /resultats** était BRUTE — sans accents donc invisible au scan accent — → `generateMetadata`+header i18n ; **LayerStack** alternatives/inclus/perMonth) ; helper LLM-context **`buildOtherPrecisionNotes`** (lib/llm) extrait de ResultsView (+4 tests) ; **e2e `e2e/i18n.spec.ts`** (4 verts : bascule accueil fr→en→fr + **verdict rendu EN** = preuve i18n MOTEUR navigateur). Parité messages **863/863**. **(— sessions précédentes ci-dessous —)** **Session 2026-05-30 (suite).** **(1) FIX lien court partage** : cause racine = un `INSERT … RETURNING` est soumis à la politique SELECT (absente pour `anon` sur `shared_reco`) → l'insert anonyme échouait TOUJOURS (« new row violates RLS ») → 503 « Création du lien court impossible ». CORRIGÉ : id généré SERVEUR (`randomUUID`), insert SANS `.select()` (le moat anti-énumération via RPC SECURITY DEFINER `get_shared_reco` reste intact ; `SharedRecoInsert` gagne `id?`). + **QR code** du lien court (`qrcode.react`, rendu LOCAL, sans appel externe ; l'URL encodée est trop longue pour un QR → QR réservé au lien court). Déployé prod (`26ee408`) + **vérifié end-to-end** (POST `/api/share`→200, GET→encoded, roundtrip OK ; ligne de test purgée). **(2) i18n MOTEUR — 5 TRANCHES VERTES** (prose `lib/engine` → descripteurs `Message`, chacune commit+push, FR identique, EN natif) : **T1** `compliance` · **T2a** `risks`+`kmChecks`+`verdict.risk` (via type frontière **`DisplayVerdict`** = forme résolue tout-chaînes ; `ResultsView` résout le verdict moteur AVANT fusion/narration → `VerdictView`/`mergeVerdictNarration` inchangés) · **T2b** `presetReason` (type **`PresetReason`**={template:Message, drivers:Message[], suffix:Message|null} + helper PUR **`formatPresetReason(reason, resolve)`** exporté de l'engine ; pas de `select` ICU sur volume/sensibilité → clés distinctes car clés à chiffre initial invalides en ICU) · **T3** `WorkloadLine.estimate` · **T4** `backup.VectorDecision.reason` · **T5** résidence `conflict.reason`/`levers` + `interSiteSecurity.note` (+ `resolve` threadé `buildExitBundle→runbook→backupSection`/`residencySection`). **3 EXCLUSIONS ARGUMENTÉES (= data sourcée, JAMAIS i18n, DÉFCON 1)** : `CostSource.label` (vendor + feed live runtime), `Layer.choice/note` (catalogue), `TransferFlag.legalBasis/note` (veille légale seed + LIVE LLM runtime). Pattern UI : `useEngineText()` → `resolveEngine(msg)` ; pur : résolveur injecté. Tests de prose → asserter `.id`/`.values`. Commits `26ee408`→`2de5e94`. **(— session précédente ci-dessous —)** Session 2026-05-29→30 (énorme). **(A) ÉPIC i18n DÉMARRÉ** : **S-057** fondation next-intl (Next 15 App Router, mode SANS routing → locale cookie `NEXT_LOCALE` + détection Accept-Language ; `i18n/{config,request}.ts` ; `middleware.ts` ; `messages/{fr,en}.json` ; `LocaleSwitcher` ; layout `<html lang>` + `NextIntlClientProvider` ; accueil migré). **S-058 (tranche CHROME, marquée passes=true)** : architecture moteur i18n = **descripteur `Message {id,values}` PUR** (`lib/engine/message.ts`) résolu en ICU fr/en par `lib/i18n/engine.ts` (`useEngineText`) + résolveur injecté `EngineResolver` dans le code pur (`buildDeliverable`, `buildExitBundle`, `serializeRecoFacts`, `buildChoiceRecap`) ; **scores + couches déjà convertis** ; `options.ts` clé-based + hook `useOptions` ; **tout le CHROME bilingue** (accueil, verdict, page résultats + sous-composants, configurateur+blocs, pages /configurateur/fiduciaire/sondage/admin, prix). Provider de test = **`test/render.tsx`** (ré-exports EXPLICITES ; `export *` masque le render custom → piège). **(B) FIRECRAWL REMPLACÉ** (crédits cloud épuisés HTTP 402) par backend AUTO-HÉBERGÉ : `lib/pricing/firecrawl.ts → scraper.ts` = SearXNG (recherche) + Crawl4AI (`/md`) + extraction par Ollama local (LiteLLM). **(C) Features** : S-064 (saisie « Autre » intégrée reco) · S-065 (clarté prix repli + diagnostic) · S-066 (toggle souveraineté → bump preset) · S-067 (partage : lien encodé URL + lien court Supabase) · S-068 (lead-gate : recette experte derrière nom+email) · S-069 (régime « Aucun » exclusif) · S-071 (scroll wizard haut de page). **(D) Migrations prod appliquées** : `shared_reco` + `leads` (RLS, vérifié end-to-end `/api/lead`→200). Commits ~`f2bad69`→`e188bee`.
[ALERTE]  ⚠ **SCRAPING (NOUVEAU, remplace Firecrawl)** : backend auto-hébergé sur serveurai. **Crawl4AI = app COOLIFY managée** (uuid `a12v92dlfhvnna5f14j1qoza`) exposée **`https://crawl4ai.ai-mpower.com` + token** ; **SearXNG = conteneur BRUT interne** `searxng-strate:8080` (settings json activé, JAMAIS public). App env : `CRAWL4AI_BASE_URL=https://crawl4ai.ai-mpower.com` + `CRAWL4AI_API_TOKEN` + `SEARXNG_BASE_URL=http://searxng-strate:8080`. Extraction JSON = Crawl4AI markdown + `callLLM` (LiteLLM/Ollama). **Firecrawl cloud = crédits épuisés, NE PAS s'y fier** (la clé existe encore mais 402). Détails : mémoire `scraping-selfhosted-crawl4ai-searxng`. · ⚠ **Coolify DNS interne INSTABLE** : une app Coolify n'a qu'un alias horodaté `<uuid>-<ts>` (change à chaque deploy) → pour un appel inter-service interne stable, conteneur BRUT (nom fixe) OU domaine public. · ⚠ **cloudflared (`config-nahda.yml`, tunnel `nahda-tunnel`)** : le **SIGHUP NE RECHARGE PAS l'ingress** en mode `tunnel run` → il faut **RESTART** ; service système `User=serveurai`, `Restart=always`, pas de sudo nopasswd → **tuer le process** (serveurai le possède) → systemd respawn (blip ~1 s). ⚠ NE PAS `pkill -f "<motif présent dans ta propre commande>"` (auto-match → tue ta session SSH) ; matcher un motif absent ou tuer par PID. · ⚠ **Coolify deploy** : sur la machine LAN d'Amine, `$env:COOLIFY_URL` (= `http://192.168.100.8:8000`) FONCTIONNE ; hors LAN utiliser `COOLIFY_PUBLIC_URL`. PS 5.1 → TLS 1.2. webhook GitHub→Coolify absent (redeploy manuel API). · ⚠ **Supabase prod self-hosté** (`supabase-db`, Kong `localhost:8200`, public `https://db.ai-mpower.com`). Migration prod = MANUELLE : `cat migration.sql | ssh serveurai 'docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1'` (stdin, trust loopback ; ÉVITE le quoting heredoc/`$$` → bash les mange). **Tables prod = TOUTES les migrations du repo appliquées (2026-05-31)** : init_rails, admin_prompts (prompts/super_admins), shared_reco, leads, conversion_data (simulation_log/lead_capture), catalog_observations, transfer_status_observations — toutes RLS=true. ✅ **Veilles S-036/S-062 : persistance d'audit CÂBLÉE + vérifiée prod** (`persistCatalogObservations`/`persistTransferObservations`, gatées live/flagged, service-role, non bloquantes ; routes catalog/live + legal/transfers ; `transfer_status_observations`=2 lignes écrites par une veille live réelle). **RESTE** : le **refresh UI live de `ResidencyPanel`** (les transferts affichés viennent encore du seed `lookupTransferBasis` ; `/api/legal/transfers` n'est pas fetché par le panneau) = increment UI futur. · **Console /admin S-053 ACTIVE** (super_admin Amine seedé, service-role key en env). · **Prix service = [PLACEHOLDER]** (sondage en cours). · **Coût d'inférence LLM à l'usage NON TARIFÉ** (« non chiffré » = pas coûté en €, ≠ sécurité ; flaggé « à chiffrer en devis ») — dette de complétude prix, pas un risque. · **Arabe en ligne = brouillon MSA, relecture native à faire** (raffinement, réversible).
[BLOQUE]  RAS.
[NEXT]    **BACKLOG VIDE (70/70), tout déployé.** Reliquats NON-bloquants restants : **① relecture native `messages/ar.json`** (l'arabe est en ligne ; raffinement prose/terminologie — corriger le fichier + redeploy ; re-trad si en.json évolue : proxy LiteLLM `deepseek-v4-flash` namespace par namespace + découpe récursive, valider parité+ICU `ar`+arguments AST+clés select). **② Veilles : persistance audit FAITE (vérifiée prod, 2 obs)** ; reste le **refresh UI live de `ResidencyPanel`** (fetch `/api/legal/transfers` → annoter les statuts affichés avec la base reconciliée + fraîcheur ; `CatalogProvenance` est déjà live). **③ Dette : TARIFER le coût d'inférence LLM à l'usage** (≠ sécurité — « non chiffré » = non chiffré EN €, non coûté ; aujourd'hui flaggé « à chiffrer en devis » comme S-033/S-041 ; feed prix tokens live = story future) ; **④ CI/CD : webhook GitHub→Coolify** (redeploy auto au push, aujourd'hui manuel — ⚠ retire le contrôle de déploiement délibéré, à arbitrer). **⑤ Prix service réel** après dépouillement du sondage Van Westendorp (remplacer `[PLACEHOLDER]`). **⑥ Lot 2 = agent de provisionnement/déploiement** (OAuth/MCP). Pattern i18n réutilisable consigné dans `progress.md` (Codebase Patterns).
[MEMO]    Coffre GLOBAL `~/.claude/secrets/secrets.env.dpapi` (load-secrets.ps1, **47 secrets**) : SSH (`serveurai@192.168.100.8`, clés `serveurai_mnemo`/`serveurai_key`), Coolify (`COOLIFY_URL`/`COOLIFY_PUBLIC_URL`, `COOLIFY_API_TOKEN`, `MNEMO_APP_UUID`=`by7kdehyeieujf6oxzzt1r0m`), Firecrawl (épuisé), Supabase serveur (`SRV_SUPABASE_*`, `SUPABASE_SERVICE_ROLE_KEY` en prod), LiteLLM (`LITELLM_BASE_URL`+`LITELLM_API_KEY` clé restreinte + `LITELLM_MASTER_KEY`), `MNEMO_ADMIN_*` (console /admin). Anti-leak : preuve = code HTTP, jamais la valeur ; ajout secret = presse-papier → DPAPI (jamais le chat). DÉCISIONS AMINE verrouillées : déterminisme (LLM propose, moteur calcule, jamais de coût inventé — DÉFCON 1) ; prix/choix JAMAIS hardcodés (feed live + seed daté + garde-fou) ; **i18n = next-intl, cookie sans préfixe, fr défaut + en puis ar/RTL, périmètre moteur MAXIMAL** ; **souveraineté = preset porte la stack** (toggle preferSovereign bump le preset) ; presets ADR-020 ; honnêteté brutale (tensions exposées) ; multi-segment (pas de référent unique). Mémoires CLÉS : `scraping-selfhosted-crawl4ai-searxng` · `mnemo-deploiement-coolify` · `mnemo-infra-credentials` · `prix-jamais-hardcodes-feed-live` · `jamais-graver-decision-perissable` · `strate-avis-critique-non-oriente` · `secrets-handling-protocol` · `autonomous-run-protocol`. « reprends en Ralph » relit `.ralph/prd.json`+`progress.md`+AGENTS.md AVANT d'agir.
== FIN PASSATION ==
```

---

## COMMENT REPRENDRE
1. Session dans `C:\Users\amans\OneDrive\Projets\Infra`.
2. Charger les accès : `. "C:\Users\amans\.claude\secrets\load-secrets.ps1"` → peuple `$env:*` (DPAPI, déchiffrable sous le compte/machine d'Amine). Dot-sourcer dans CHAQUE commande qui utilise un secret. Anti-leak : jamais afficher une valeur ; preuve = code HTTP / résultat.
3. Lire `.ralph/prd.json` + `.ralph/progress.md` + `AGENTS.md` AVANT d'agir (mode Ralph permanent).
4. Vérifs d'entrée : `npm run typecheck && npm run lint && npm test && npm run build` → vert. Prod : `curl -s -o /dev/null -w "%{http_code}" https://infra.ai-mpower.com/health` → 200.
5. Après tout commit de code mergé : **redéployer la prod** : `GET $COOLIFY_URL/api/v1/deploy?uuid=$MNEMO_APP_UUID&force=false` (Bearer `$env:COOLIFY_API_TOKEN` ; sur le LAN d'Amine `COOLIFY_URL`=IP marche, sinon `COOLIFY_PUBLIC_URL` ; PS 5.1 → TLS 1.2). Poll : `GET /api/v1/deployments/<deployment_uuid>` → `status`. Vérifier au **navigateur** ou via la route. ⚠ **Un commit DOC ne nécessite PAS de redéploiement.** ⚠ Build échoue à « Collecting build traces » = **OOM TRANSITOIRE** du build container (`next build` gourmand, PAS un bug code) → **relancer** ; logs build = `docker exec coolify-db psql -U coolify -d coolify -t -A -c "select logs from application_deployment_queues where deployment_uuid='<uuid>'" | jq -r ".[].output"`. ⚠ Migration Supabase prod = MANUELLE (SSH + psql stdin, cf. ALERTE), PAS auto au déploiement.
6. **i18n** : nouvelle chaîne UI → `useTranslations` + `messages/{fr,en}.json` (EN natif, accents FR sur majuscules). Test rendant un composant à `useTranslations` → importer depuis `@/test/render`. Le moteur reste PUR → renvoie un `Message {id,values}`, jamais de prose.

## DÉCISIONS PRODUIT VERROUILLÉES (ne pas re-litiger)
- Cœur = conseil → déploiement ; « recette ouverte, cuisine payante ». Production-ready, zéro dette, DÉFCON 1.
- ±30 % assumé ; **prix ET choix de composants JAMAIS hardcodés** (feed/veille live + seed de repli daté + garde-fou ; le LLM ne calcule jamais un coût).
- **Reco vivante = « tout vivant + filet »** ; LLM au niveau plateforme via proxy LiteLLM (super-admin GLOBAL, S-053).
- **Presets = modèle scoré/explicable** (ADR-020) ; **souveraineté portée par le preset** (toggle `preferSovereign` → bump LIGHT→MEDIUM→HARD, S-066).
- **Aucune option collectée ne reste sans effet** (DÉFCON 1, tests de garde).
- Trio de moats : ① Exit Escrow ② Fiduciary ③ Intelligence Network. **Honnêteté brutale** (tensions exposées). Scoring **10 dimensions** (`resilience` S-027, `geosov` S-045).
- **i18n (2026-05-28→30)** : next-intl, locale cookie sans préfixe d'URL, **fr (défaut) + en** puis **ar (RTL)**. Refactor moteur → descripteurs `Message` (périmètre MAXIMAL). Prompts réécrits niveau natif + localisés (S-059). Garde « zéro littéral ».
- **Scraping souverain (2026-05-30)** : Firecrawl cloud abandonné (crédits) → Crawl4AI (Coolify, public+token) + SearXNG (interne) + Ollama local.

## POINTEURS
- **Specs** : `docs/superpowers/specs/` (n°1 backup, n°2 résidence/DR, n°3 LLM/reco vivante — toutes livrées).
- **Ralph** : `.ralph/prd.json` (**70 stories, 70 FAITES — backlog vidé ; ÉPIC i18n S-057→S-060 COMPLET** : fr/en/ar, prose moteur intégralement i18n, prompts EN+locale, livrable/charte/recap/ensemble localisés, RTL ; arabe = brouillon MSA NON déployé, relecture native requise) · `.ralph/progress.md` (log + patterns détaillés, dont la migration i18n moteur complète + le pont `DisplayVerdict` + les helpers `formatPresetReason`/`formatModuleTask` + le `select` ICU + la garde `check:i18n` + les 3 exclusions data).
- **Pricing sourcé** : `docs/pricing/{media,backup,compute,residency}-cost-sources.md`. **ADR** : `docs/DECISIONS.md`.
- **Mémoires** : `~/.claude/projects/C--Users-amans-OneDrive-Projets-Infra/memory/MEMORY.md` (index). CLÉ pour l'infra scraping : `scraping-selfhosted-crawl4ai-searxng`.
- **Déploiement** : Coolify app `mnemo` uuid `by7kdehyeieujf6oxzzt1r0m`, `https://infra.ai-mpower.com`. Services scraping : `crawl4ai.ai-mpower.com` (Coolify `a12v92dlfhvnna5f14j1qoza`) + `searxng-strate` (brut). Supabase `https://db.ai-mpower.com`.
```
== FIN PASSATION ==
```
