# Progress — Mnémo (Ralph Loop)

## Codebase Patterns

> Conventions à suivre pour rester cohérent. À enrichir à chaque nouveau pattern.

- **Stack** : Next.js 15 App Router, React 19, TypeScript strict, Tailwind v3, Supabase (RLS), Vitest (unit), Playwright (e2e).
- **Langue** : UI + contenu en français (accents sur majuscules obligatoires) ; code/identifiants en anglais.
- **Moteur** : logique de reco en `lib/engine/` — fonctions **pures**, sans dépendance UI, testées unitairement. Portée depuis `design-reference/.../simulator-archi-base-memorielle-v2.html`. Fichiers : `types.ts` (unions littérales via `as const`), `modules.ts`, `presets.ts`, `preset.ts` (decidePreset), `layers.ts` (buildLayers), `cost.ts`, `scores.ts` (8 dim avec clé `key`), `diagnostics.ts` (compliance/risks/kmChecks), `recommend.ts` (orchestrateur + bonus modules), `index.ts` (barrel). Point d'entrée : `recommend(profile): Recommendation`.
- **Design** : tokens de `design-reference/mn_mo_brand_identity/DESIGN.md`, portés dans `tailwind.config.ts`. Polices Space Grotesk (titres, `font-display`), Inter (corps, `font-sans`), JetBrains Mono (données/chiffres/clés/chemins, `font-mono`). Cartes `rounded-card` (14px), champs `rounded-input` (10px), boutons `rounded-full`. Élévation `shadow-elevation`. FontSize sémantiques : `text-display-lg`, `text-headline-lg/md`, `text-body-lg/md/sm`, `text-code-md`, `text-label-caps`.
- **Primitives UI** : `components/ui/{Button,Card,Chip,Input,StatusDot}.tsx` + helper `lib/utils/cn.ts` (clsx+tailwind-merge). Type `Confidence` (high/medium/low → 🟢🟡🟠) dans StatusDot. Composants = props typées étendant les attributs HTML, pas de `any`, return type `ReactElement`.
- **Tests** : Vitest + jsdom + @testing-library/react + jest-dom. `test/setup.ts` importe `@testing-library/jest-dom/vitest`. `@vitejs/plugin-react@^4` (épinglé : v6 exige vite 8, incompatible vitest 2/vite 5).
- **Sécurité** : zéro secret hardcodé (env/coffre) ; RLS sur toutes les tables Supabase ; pas de `any` TS sans commentaire justificatif.
- **Sources** : tout chiffre de coût affiché lie sa source (URL + date) + pastille de confiance 🟢🟡🟠 + disclaimer « une IA peut se tromper ».
- **Price feed (F4, `lib/pricing/`)** : logique pure (`extract`/`feed.buildObservation`) séparée des effets (`firecrawl` = `fetch` injectable, jamais throw → repli ; `cache` = TtlCache, `now` injectable). Secrets (`FIRECRAWL_API_KEY`) lus uniquement côté serveur (`app/api/pricing`), jamais exposés au client. Détection de variation par empreinte d'un snapshot daté (`baseline.ts`) **généré par le code** (`scripts/capture-baseline.mts`, jamais à la main). Pages usage-based (0 figure) → confiance medium + note manuelle, pas d'invention.
- **Provisioning** : l'agent (Lot 2) ne crée jamais de compte ni ne saisit de carte — l'utilisateur crée le compte, l'agent optimise après (OAuth/MCP).
- **Intégration sizing → recommend (S-018)** : `recommend(profile, prices = NEUTRAL_MEDIA_PRICES)` — **prix injectés**, moteur **pur** (zéro import `lib/pricing`). `applyMultimodalSizing` (`cost.ts`) injecte les coûts **dans** les couches : C6 = pool GPU (UNE fois) + usage API ; C5 = stockage ; C4 = forfait embeddings (jamais le GPU → anti double-comptage). `computeSetupCost` (backlog `MediaNeed.backlog`, tarif `api.ingest`). `Recommendation` étendu (`sizing`/`setupCost`/`costSources`/`verdict`) — consommateurs lisent seulement (typecheck 0 sans modif). UI = défaut neutre tant que `ResultsView` n'injecte pas `getMediaPricesEur()` (câblage = S-022). Invariant `totalCost = baseCost + moduleCost` **préservé** (sizing neutre si pas de `mediaNeeds`). `buildEnsemble(profile, prices?)` threade les prix.
- **Pricing médias (`lib/pricing/media-seed.ts` + `media-feed.ts`, S-017)** : seed sourcé (DÉFCON 1) en **devises natives** (`MultimodalPriceEntry.currency` ; Scaleway € / OpenAI·Google·Runway $) — **jamais de € figé** ; conversion en € = **étage FX dédié** (`normalizeMediaPricesToEur`) au runtime, taux **live Frankfurter (BCE), repli** sur taux BCE daté (`SEED_USD_TO_EUR`). `getMediaPrices()` = natif ; `getMediaPricesEur(rate)` = € pour le moteur. Audit trail complet : `docs/pricing/media-cost-sources.md` (figure + unité + URL + date + confiance par fournisseur). Conversions d'unité (car./image→min) bakées avec confiance `low` + note « estimation, à confirmer par devis ». Feed Firecrawl (`refreshMediaPriceFreshness`) = fraîcheur des pages seulement (ne réécrit pas un prix). **Sourcing = recherche web réelle (pages officielles), NON TROUVÉ assumé, jamais inventé.** **Revue Amine** : référent embeddings = **Qwen3-VL-Embedding** (open Apache 2.0, #1 MMEB-V2), self-hosté sur le GPU déjà compté → forfait C4 = **0 €** (anti double-comptage), pas Voyage. Pour les modèles « best-of » → **vérifier le classement (MMEB/MTEB) + licence**, ne pas figer un fournisseur de mémoire.
- **Catalogue de composants injecté (`lib/catalog/`, S-035, reco vivante spec n°3)** : les CHOIX techniques (jadis figés dans `layers.ts`) deviennent **données injectées**, comme les prix (S-018). `types.ts` (`Catalog`/`CatalogSlot`/`ComponentCandidate`/`Provenance` live·seed·flagged/`ComponentSovereignty`) ; `catalog-seed.ts` (`seedCatalog(preset, profile)` transpose `layers.ts` À L'IDENTIQUE — alternatives en **tableaux** (pas de split fragile) → round-trip exact ; source = calibration interne datée, confiance `medium`, **PAS** une source web) ; `reconcile.ts` (`reconcileCatalog(live, seed, profile)` pur, jumeau `pricing/reconcile` : live valide→`live`, sinon repli **seed** marqué `flagged`+`confidence low` ; profil `secret`/régulé dur ⇒ rejet `api-third-party`) ; `index.ts` barrel. `buildLayers(preset, profile, catalog = seedCatalog(...))` lit le catalogue (id/name/couleur/**coût** restent calculés — le catalogue ne porte PAS les coûts, DÉFCON 1) ; `recommend(profile, prices, catalog?)` + `buildEnsemble(profile, prices?, catalog?)` threadent (baseline ; variants sur seed car le spread coût/score est indépendant du catalogue). **Réutilise `Confidence` (StatusDot, `import type`) + `CostSource` (engine)**. Tests : caractérisation d'identité (choix/notes/alternatives/coûts exacts LIGHT/MEDIUM/HARD × mm × bitemp) + reconcile. La **veille live** (S-036) remplacera le seed par des candidats sourcés web.
- **Dimensionnement multimédia (`lib/engine/sizing.ts`, S-016)** : `costMultimodalSizing(profile, prices) → Sizing`, **pure**, prix INJECTÉS (`MultimodalPriceTable`, implémentée/sourcée en S-017). **GPU compté UNE SEULE FOIS** : charges `sovereign` (ingest+generate, toutes modalités) → charge cumulée → **un** palier `gpu` (jamais N pools) ; `api` ne consomme pas le GPU souverain (coût à l'usage porté par `WorkloadLine.monthlyCost`, **0** pour `sovereign`). Facteurs de modélisation (`TIER_QUANTITY`/`STORAGE_GB_PER_UNIT`/`GPU_LOAD_PER_UNIT`/seuils) = **hypothèses ±30 %** (comme `VOLUME_FACTOR`), **jamais des prix**. Besoin inactif (`ingest`+`generate` = `none`) filtré → sizing neutre. `GpuTier` validé à 4 paliers (ADR-010). Tests = **propriétés** (monotonie, anti-double-comptage, `api`≠`sovereign`, `generate`>`ingest`) plutôt que valeurs en dur.

- **Résidence/DR — données vivantes multi-segment (`lib/pricing/residency-seed.ts` + `lib/legal/transfers.ts`, S-043)** : doctrine **« tout vivant + filet » étendue au pricing egress ET au juridique** (mémoire `jamais-graver-decision-perissable`). **Egress** = `EgressVector[]` **multi-segment first-class** (PAS de référent unique, retour Amine) : self-hosted/Hetzner, souverains UE (OVH/Scaleway/IONOS), secnumcloud (Outscale), hyperscalers repères (GCP/AWS), **devises natives**, chaque vecteur sourcé ; seed = **repli daté + baseline**, `reconcileEgressVector` (garde-fou ±60 %, réutilise `reconcile.ts`). **Juridique** = `lookupTransferBasis(from,to,ctx)` **pur + total** → état **DATÉ + RÉVISABLE** : retourne TOUJOURS `checkedAt` + `volatile` + `disclaimer` « ingénierie, PAS un avis juridique » ; flag `volatile` sur statuts instables (DPF, adéquation Maroc), flag `cloudAct`. `TransferStatus`/`TransferRegion` **canoniques** ici (S-044 importe, ne redéfinit pas). Self-hosted = **surface à sécuriser** : `INTER_SITE_SECURITY_SEED` flague (coût détaillé = S-061). Audit trail : `docs/pricing/residency-cost-sources.md` + `docs/legal/transfer-bases.md` (**NON CONFIRMÉ/NON TROUVÉ assumés**, jamais comblés). Veille live des statuts = S-062.

- **Résidence/DR — UI (S-048)** : même triptyque que le backup. **Wizard Bloc ②** : `ResidencyBlock.tsx` (jumeau `BackupBlock`) — la criticité DR pilote tout (palier `none/warm/hot` + actif-actif), affinage expert en `<details>`, **régions autorisées multi-continent** (`CheckboxCards`, finding C4), aperçu live + **alerte conflit** (`role="alert"`, jamais résolu en douce). **Résultats** : la **ligne réplication** vit dans `CostMap` (prop `residency?: ResidencyPlan`, masquée si `monthlyCost = 0`, comme backup) ; le **détail conformité** vit dans `ResidencyPanel.tsx` (topologie + transferts ✅/⚠️/⛔ + base légale datée + conflit, **rendu `null` si plan neutre**). **Injecter `getResidencyPrices()` dans `recommend`/`buildEnsemble`** côté Wizard ET ResultsView (sinon prix neutres → coût résidence 0 → budget-mètre incomplet + ligne absente). `hostingClassForProfile(zone)` choisit le vecteur d'egress sourcé. **Pièges Playwright** : `getByText` = substring insensible à la casse → utiliser `{exact:true}` / `getByRole("heading")` (collisions « Affinage expert », « Résidence & transferts »).

- **Sécurisation inter-site self-hosted (`costInterSiteSecurity`, S-061)** : poste de coût pour la **liaison inter-site** d'une infra **auto-hébergée** (`Profile.residency.selfHosted` → `hostingClassForProfile` = `self-hosted`). Contrat `InterSiteSecurityPrices` (passerelle self-hosted/mois + managé/utilisateur·mois) dans le moteur, seed sourcé dans `lib/pricing`. **Coût ajouté à la résidence UNIQUEMENT si `hostingClass='self-hosted'` ET multi-région** (le souverain managé porte la liaison sur son backbone). **Doctrine DÉFCON 1 du chiffrage** : figer **seulement les postes chiffrables vendeur** (VM passerelle, tarif managé) ; tout ce qui est **OPEX/main-d'œuvre** (supervision, durcissement) → flagger **« à chiffrer en devis »**, JAMAIS un montant inventé (≠ media unit-conversions qui partent d'un prix sourcé). Toujours **chiffrer l'alternative** (ici managée) pour comparaison — avis non orienté, jamais imposée. Logiciels open-source (WireGuard/mTLS) = **licence 0 €** (sourcé). Devise managé USD → étage FX runtime.

- **Veille live juridique (`lib/legal/`, S-062)** : réplique 1-1 le pattern de veille catalogue (S-036) sur les statuts de transfert. `live-transfers.ts` (PUR, I/O injectée) `proposeTransferLive`/`refreshTransferBases` ; `live-feed.ts` (I/O réelle `searchWeb`+`callLLM` + `TtlCache` 6 h, clés SERVEUR) ; route `GET /api/legal/transfers` (prompt `legal-veille` éditable /admin). **Garde-fou `reconcile.ts` PLUS conservateur que prix/catalogue** : sur du DROIT, un signal live **divergent** du repli daté n'est **jamais adopté** (statut seed conservé, confiance `low`, `volatile`, provenance `flagged`, « à revérifier par un conseil ») ; concordant → confirmé (`live`) ; indispo → seed. Toujours `checkedAt`+`disclaimer`+révisable. **On ne veille QUE les flux `volatile`** (`WATCHED_TRANSFER_FLOWS` = DPF UE-US, Maroc) ; anti-hallucination : `sourceUrl` du LLM DOIT ∈ résultats web. Audit trail `transfer_status_observations` (RLS, pivot circle) + builder pur `buildTransferObservations`. Le LLM ne rend JAMAIS un avis juridique.

- **`server-only` + découplage client/serveur (S-078)** : les modules **lecteurs directs de secret/service-role** portent `import "server-only"` en tête (vault server/credentials, supabase/admin, llm/client, pricing/scraper) → le **build Next échoue** si un Client Component les importe en VALEUR (un `import type` est effacé, donc sans effet). Les modules qui passent par `createAdminClient` sont couverts **transitivement** (ne pas redonder). **Piège Vitest** : `server-only` throw en Node → aliaser vers `test/server-only-stub.ts` dans `vitest.config.ts` (`resolve.alias`). **Quand le build casse sur `server-only`, lire la trace d'import** : elle pointe le couplage (ex. `scraper → media-feed → Wizard`) → **corriger à la racine** en extrayant la partie serveur (live/scraper) dans un module dédié `*-freshness`/`*-live` server-only, en gardant le module d'origine PUR (seed/types/calcul) et donc client-safe. Ne JAMAIS retirer `server-only` pour faire passer le build (ce serait masquer la fuite).
- **Régimes réglementaires modélisés vs affichage-seul (`lib/legal/regime-*`, `computeCompliance`, S-077)** : doctrine « les deux » (décision Amine). Un régime est **modélisé dans le moteur** (énum `REGULATIONS` + branche d'actions sourcées dans `computeCompliance`) **UNIQUEMENT** si ses obligations sont **sourçables rigoureusement** (URL officielle + date — DÉFCON 1) : aujourd'hui RGPD, CNDP, AI Act, HIPAA, secret-pro, **LGPD** (Brésil — articles ANPD/Planalto), **APPI** (Japon — obligations PPC, sans n° d'article car renumérotés). Tout autre régime découvert reste **affichage-seul** (`RegimeSuggestion.code = null`, porté comme donnée sourcée, n'altère pas le chiffrage déterministe). **Pour modéliser un nouveau régime** : (1) `REGULATIONS += "xxx"` (`types.ts`) ; (2) seed `code:"xxx"` sur les pays concernés (`regime-seed.ts`) ; (3) `mapRegimeCode` (mots-clés robustes) ; (4) `REGULATION_OPTIONS += {label,hint}` ; (5) branche `computeCompliance` avec actions **sourcées** ; (6) i18n `Engine.diagnostics.compliance.*` + `Options.regulation.*` ×3 ; (7) éventuel `+1` conf dans `scores.ts` si data-protection structurant. L'énum étant la source de vérité, share/recap/export propagent sans modification. **Ne JAMAIS inventer une obligation ou un n° d'article** — sourcer ou laisser affichage-seul.
- **i18n (`i18n/`, `messages/`, `middleware.ts`, S-057, épic multilingue)** : **next-intl v4, App Router, mode « SANS routing »** (décision Amine 2026-05-28) → la locale vit dans le **cookie `NEXT_LOCALE`** (PAS de segment `[locale]` d'URL → routes/e2e intacts). `i18n/config.ts` = **source de vérité** (`locales=[fr,en] as const`, `defaultLocale=fr`, `LOCALE_COOKIE`/`LOCALE_HEADER`, `localeNames` autonymes, garde `isLocale`) ; **ar (RTL) = S-060, ne PAS l'ajouter tant que le miroir RTL n'existe pas**. `i18n/request.ts` : `getRequestConfig` lit cookie → repli en-tête `x-next-locale` (1ᵉʳ accès) → repli `fr` ; messages via **chargeurs à chemins LITTÉRAUX** typés `Record<Locale,()=>Promise<{default:AbstractIntlMessages}>>` (**jamais** `import(\`…${locale}…\`)` qui rendrait `any`). `middleware.ts` : négociation Accept-Language **maison** (sans dépendance) + pose `x-next-locale` + cookie 1 an si absent ; `matcher` exclut `api`/`_next`/assets. **Catalogues `messages/{fr,en}.json` namespacés par surface** (`Metadata`, `Nav`, `Home.*`, `LocaleSwitcher`…) — fr ET en doivent rester **complets** (zéro chaîne en dur sur une surface migrée). Server Components → `getTranslations` (async) ; Client Components → `useTranslations` (exigent `NextIntlClientProvider`, déjà dans `layout`). **Rich text** (mot coloré, lien inline) → `t.rich('clé', { mark: (c) => <span/> })`. **Listes** → clés structurelles en code (`PROMISE_KEYS`…) + texte en messages (jamais d'array `t.raw` non typé). `<html lang>` = `await getLocale()` dans `layout`. **`LocaleSwitcher`** (`components/ui/`) : `<select>` natif étiqueté (accessible) → cookie + `router.refresh()`. **Playwright** : `use.locale="fr-FR"` (rendu FR déterministe — sinon Accept-Language `en-US` basculerait). Warnings build `import(t)` = dist interne next-intl (extractor), non bloquant.

## Validation par story (obligatoire avant passes=true)

1. `npm run typecheck` → 0 erreur
2. `npm test` → vert
3. `npm run lint` → 0 erreur
4. `npm run build` → passe
5. (si UI) parcours vérifié
→ puis commit `[S-XXX] Titre` + push origin main.

## Dette identifiée (à résorber)

- ~~`next lint` déprécié → migrer vers ESLint CLI~~ **RÉSOLU en S-014** (ESLint 9 flat config, `eslint .`, ADR-008).
- `npm audit` : 7 advisories modérées **dev/build-time** (esbuild via vite/vitest, postcss via next), pré-existantes, sans fix non-cassant — acceptées et tracées (ADR-004). CI : `npm audit --audit-level=high` (ne bloque pas sur modéré).

- **Wizard / formulaires** : état dans `hooks/useWizardProfile.ts` (localStorage `mnemo:profile:v1`, reprise auto). Composants génériques `components/wizard/{RadioCards,CheckboxCards,NumberStepper,ModuleSlider,Wizard}`. Options/labels FR dans `lib/wizard/options.ts`, profil par défaut dans `lib/wizard/defaultProfile.ts`. Éviter le générique sur union de tableaux (erreur TS « méthode non appelable ») → togglers dédiés (`toggleContentType`, `toggleRegulation`).
- **Tests UI** : `test/setup.ts` enregistre `afterEach(cleanup)` (sinon les rendus s'accumulent dans le DOM jsdom → faux échecs `findByText` multiples).
- **Lint** : apostrophes dans le texte JSX → utiliser l'apostrophe typographique « ’ » (corrige `react/no-unescaped-entities` ET respecte la typo française).
- **Note séquencement** : le wizard pointe vers `/resultats` (route créée en S-006 — 404 transitoire jusque-là, build OK).
- **Wizard 4 blocs (S-019)** : `Wizard.tsx` restructuré en 4 blocs (`BlockId` profil/infra/memoire/medias). Nouveaux composants `InfoBubble` (disclosure a11y « pourquoi + conséquence » **non orienté**) et `ContinuousSlider` (range sur paliers discrets, `aria-valuetext`). `useWizardProfile` gagne `setNote(block)` ; `freeNotes` (additif, pas de bump localStorage). Usages costables renommés dans `modules.ts` (`name`). `YesNo` : `role="group"` + `aria-label`. Bloc ④ Médias **structurel** (intro + note) — détail + budget-mètre en S-020. Aperçu live (preset+coût) en en-tête. **e2e mis à jour (3 « Suivant ») + vérifié navigateur 3/3.** Quand on touche le wizard → **mettre à jour `e2e/parcours.spec.ts`** (compteur d'étapes).

## Log

- 2026-06-01 — **S-090 ✅ (prix figés) + S-091 polish arabe ciblé.** **Prix** : sondage Van Westendorp terminé → Amine a CONFIRMÉ (AskUserQuestion, DÉFCON 1 — aucun montant inventé) les 3 paliers : Essentiel **290 €/mois**, Pro **690 €/mois**, Souverain+ « sur devis, dès 1 900 €/mois » (Diagnostic gratuit). Figeage = retrait des mentions provisoires : `Home.pricing.surveyTag` (« Tarif indicatif ») retiré des 3 langues + du rendu `app/page.tsx` (clé + bloc) ; `Home.pricing.note` dé-« indicatifs » ; `VerdictView.firmPriceNote` « sondage en cours » → « prix de vente du service ». **Arabe** : constat honnête — l'arabe (deepseek + validation technique) est DÉJÀ de bonne qualité MSA native, pas un brouillon ; polish du `Home.hero` (tiret cadratin —, accords, fluidité) + cohérence prix (retrait `المسح قيد التقدم`). Passe exhaustive des 22 namespaces NON faite (gain marginal vs risque ICU ; par lots si exhaustivité voulue). parité **1201×3** (surveyTag retiré), typecheck 0 / lint 0-0 / **674 tests** / build OK. **Dette auth S-089 reste bloquée** (Amine n'a pas eu le temps).
- 2026-06-01 — **S-087 + S-088 ✅ — Provisioning hybride F10 (Lot 2 · B) → Lot 2 B complet.** **S-087** `lib/provisioning/procedures.ts` PUR : `deriveProvisioningProcedures(catalog)` → par composant : `kind` (account si api-third-party/eu-hosted, self-host si sovereign), **lien officiel SOURCÉ** (`source.url` du candidat, jamais inventé), étapes que l'UTILISATEUR réalise (descripteurs i18n) + champs à relever. **S-088** `lib/provisioning/plan.ts` PUR : `deriveProvisioningPlan(catalog)` (tâche d'optimisation par couche + garde-fou) + **GARDE-FOU DUR** `isAllowedAgentAction`/`runAgentAction` : refuse `create-account`/`enter-card`/`financial-commitment` AVANT tout appel à l'executor (injecté/mocké) → l'agent ne crée jamais de compte, ne saisit jamais de carte (AGENTS.md §9). UI `components/provisioning/ProvisioningPanel.tsx` (ResultsView) : procédures + plan + rappel garde-fou. i18n `Engine.provisioning.*` (14) + `Provisioning` UI (8) fr/en/ar (parité 1202×3). Tests `provisioning.test.ts` (6 : procédures sourcées, plan, garde-fou refuse/autorise/non-throw). typecheck 0 / lint 0-0 / 660 tests / build OK. ⚠ **Exécution réelle MCP/OAuth = nécessite comptes vendor + apps OAuth (input Amine)** → executor mocké en test, branchement live à activer avec les creds (lié à la dette auth S-089).
- 2026-06-01 — **S-086 ✅ — Continuous Re-optimization (Lot 3 F15) → Lot 3 (F14/F15) complet.** `lib/optimize/reoptimize.ts` PUR : `proposeReoptimization(currentCost, candidates, constraints, thresholdPct=10)` → meilleure alternative iso-contraintes ou `null`. Garde-fous DÉFCON 1 : (1) **jamais** un candidat qui viole la souveraineté exigée (S-072) — l'hyperscaler moins cher est écarté ; (2) ne propose que si l'économie ≥ seuil (migrer a un coût) ; (3) `requiresHumanValidation: true` — l'humain tranche, jamais d'auto-application. Candidats injectés (veille prix/catalogue en aval). Tests `reoptimize.test.ts` (5). typecheck 0 / lint 0-0 / build OK.
- 2026-06-01 — **S-085 ✅ — Guaranteed Migration / In-Switch (Lot 3 F14).** `lib/migration/bundle.ts` PUR (jumeau Exit Escrow) : `buildMigrationBundle(profile, reco, {generatedAt})` → `{ files }` : `MIGRATION.md` (runbook double-run ancienne∥nouvelle + critères de bascule objectifs + **rollback garanti** + décommissionnement), `scripts/cutover.sh`, `scripts/rollback.sh`, `manifest.json` (cohérent reco) ; `zipMigrationBundle` (fflate). Runbook FR en dur (artefact généré, comme l'Exit bundle). DÉFCON 1 : cohérent reco, aucune exécution auto, aucun secret. UI `components/results/MigrationBundle.tsx` (jumeau ExitEscrow, download zip, import dynamique) dans ResultsView ; i18n `Results.migrationBundle` fr/en/ar (parité 1180×3). Tests `bundle.test.ts` (5 : fichiers, sections runbook, manifest, cohérence composants, rejouabilité ZIP). typecheck 0 / lint 0-0 / build OK.
- 2026-06-01 — **S-084 ✅ — Alertes vendor partagées (F13) → bloc Network F13 COMPLET.** `lib/network/alerts.ts` PUR : `buildVendorAlerts(changes)` transforme une variation de prix détectée par le feed (S-025) en **alerte SOURCÉE** (ancien→nouveau + devise + date + URL) diffusable aux consentants. Dedup par (vendor,item) en gardant la plus récente ; sévérité par ampleur (info<10 % / notable 10-30 % / major>30 %) ; direction increase/decrease ; ignore variations nulles + ancien prix ≤0. DÉFCON 1 : fait chiffré sourcé, jamais un conseil d'achat. Résumé = descripteur i18n `Engine.network.alert.summary` (select direction) fr/en/ar (parité 1176×3). Tests `alerts.test.ts` (5). typecheck 0 / lint 0-0 / build OK. NB : la diffusion broadcast effective aux membres = increment d'infra de notification (le moteur produit les alertes prêtes).
- 2026-06-01 — **S-083 ✅ — Recalibration continue (F13 Network actif).** `lib/network/recalibrate.ts` PUR : `computeRecalibration(observations, opts)` agrège les écarts coût réel/estimé (anonymisés, consentis) par poste → **PROPOSITION** d'ajustement de baseline (`suggestedFactor = 1 + médiane/100`). **Garde-fous DÉFCON 1** : (1) jamais d'auto-application — sortie = proposition tracée, l'humain valide avant toute modif d'un seed daté ; (2) `adopt=false` sous `minObservations` (défaut 5) ; (3) `adopt=false` si dispersion (IQR) > `maxSpreadPct` (défaut 40) = pas de consensus ; (4) **médiane** (robuste aux aberrations), pas moyenne ; confiance par paliers (low<10, medium 10-29, high≥30). Pas d'UI/migration/i18n (analyse interne ; consomme `real_cost_entries` S-082). Tests `recalibrate.test.ts` (6 : seuil, cohérence, médiane robuste, dispersion, paliers, groupement). typecheck 0 / lint 0-0 / build OK.
- 2026-06-01 — **S-082 ✅ — Collecte du coût réel + écart vs estimé (F13 Network actif, Lot 2 · D).** `lib/network/real-cost.ts` PUR : `compareRealVsEstimated(entries, tolérance=30 %)` → par poste : écart abs/%, statut within/over/under ; total agrégé + flag hors tolérance. Robuste au diviseur nul. Migration **`real_cost_entries`** (RLS pivot circle) + builder pur `buildRealCostRows` + `persistRealCostEntries` (server-only) + types `Database`. Route `POST /api/network/real-cost` (validation stricte sans `as` ; persistance **gatée par consentement** opt-in F9, non bloquante). UI `components/network/RealCostPanel.tsx` (client, ResultsView) : postes dérivés des couches de la reco (+ mise en route), saisie du réel, comparaison live colorée (primary/error/tertiary), case consentement, POST. i18n namespace `RealCost` fr/en/ar (parité 1175×3). Tests `real-cost.test.ts` (7 : statuts, total, diviseur nul, builder). typecheck 0 / lint 0-0 / build OK. ⚠ migration prod manuelle : `real_cost_entries`. Recalibration (agrégat → baselines) = S-083.
- 2026-06-01 — **S-081 ✅ — UI dashboard monitoring (IHS + MEL) + persistance RLS → bloc F12 Monitoring COMPLET.** `lib/monitoring/signals.ts` (pur) `deriveHealthSignals(reco)` : dérive resilience/compliance (scores radar 0–10 → 0–100) + budget (coût estimé vs plafond du palier) ; **availability/freshness/drift restent NON mesurés** (signaux d'exploitation à brancher — honnêteté DÉFCON 1 : santé de CONCEPTION, pas d'exploitation). `components/monitoring/MonitoringPanel.tsx` (client, dans ResultsView mode expert) : calcul 100 % client (moteurs purs), jauge IHS + statut coloré (tokens primary/tertiary/error/outline-variant) + sous-scores + items MEL + couverture `measured/total` ; POST snapshot `/api/monitoring` (audit non bloquant). Migration **`health_metrics`** (RLS pivot circle, jumeau observations) + builder pur `buildHealthMetricRow` + `persistHealthMetric` (server-only) + types `Database`. Route `POST /api/monitoring` (validation stricte sans `as` : type guards status/dimension/score). i18n namespace `Monitoring` (UI) + résolution `Engine.monitoring.*` via useEngineText, fr/en/ar (parité 1161×3). **BUG ICU corrigé (rétroactif S-080)** : `select` n'admet PAS de tiret dans un sélecteur → `MelDispatch` `go-if`/`no-go` → `goIf`/`noGo` (le test S-080 ne le voyait pas car il ne RENDAIT pas le Message ; l'UI l'a révélé). Tests : signals (6), observation (2), monitoring-panel (1, render). typecheck 0 / lint 0-0 + garde / **642 tests** / build OK. ⚠ **Migrations prod à appliquer MANUELLEMENT** (SSH/terminal web) : `health_metrics` (S-081) + `regime_code_lgpd_appi` (fix S-077). Dérivation des signaux d'EXPLOITATION réels (uptime) + coût réel = F13 (S-082+).
- 2026-06-01 — **S-080 ✅ — Dispatch Deviation Guide (MEL), F12 Monitoring.** `lib/monitoring/mel.ts` PUR, dérivé du `HealthReport` (S-079) : `deriveDeviationGuide(report)` → un item par dimension **dégradée** (warn/critical) = `{ dispatch, guidance, corrective }`. Analogie MEL aviation : `dispatch` ∈ `go-if` (exploitation sous condition) | `no-go` (mode protégé). Règle documentée **ADR-022** : `critical` sur availability/resilience/compliance ⇒ `no-go` (base inaccessible / perte de données / non-conformité) ; sinon `go-if`. Garde-fou : aucune conduite émise sur une dimension non observée. Descripteurs `Message` (`Engine.monitoring.mel.*`), i18n fr/en/ar (13 clés, parité 1154×3). Tests `mel.test.ts` (7). typecheck 0 / lint 0-0 / 633 tests / build OK. UI dashboard (IHS + MEL) + persistance = S-081.
- 2026-06-01 — **S-079 ✅ — Moteur Infra Health Score (IHS), F12 Monitoring (Lot 2 · C).** `lib/monitoring/health.ts` PUR (signaux injectés, zéro I/O) : `computeHealthScore(input)` → IHS composite 0–100 + sous-scores + statut par seuil + alertes. **6 dimensions** (availability, resilience, compliance, budget, freshness, drift), pondération ordinale assumée (availability=3, resilience/compliance=2, reste=1), seuils ok≥80 / warn≥50 / critical<50 — tout documenté **ADR-021**. **Garde-fou DÉFCON 1** : une dimension non mesurée est EXCLUE du composite (jamais comptée 0), statut `unknown` ; `null` si rien mesuré ; rapport expose `measured/total`. Sorties = descripteurs `Message` (`Engine.monitoring.health.*`), zéro prose. i18n fr/en/ar (11 clés, parité 1141×3). Tests `health.test.ts` (15 : seuils, pondération, exclusion non-mesurés, monotonie, clamp, alertes, descripteurs i18n). typecheck 0 / lint 0-0 + garde / 626 tests / build OK. **Dérivation des signaux réels (uptime, coût réel…) = S-081/S-082** ; ici on livre le contrat de score.
- 2026-06-01 — **S-078 ✅ — Durcissement `server-only` (dette auth, partie non bloquante) + dette de couplage corrigée.** Paquet `server-only` installé, importé en tête des **5 lecteurs directs** de secret/service-role : `lib/vault/server.ts` (KEK), `vault/credentials.ts`, `lib/supabase/admin.ts` (service-role), `lib/llm/client.ts` (LITELLM_API_KEY), `lib/pricing/scraper.ts` (SEARXNG/CRAWL4AI). Les modules de persistance (`legal/persist`, `catalog/persist`, `legal/regime-persist`, `prompts/store`) restent couverts **transitivement** (ils importent `createAdminClient`, lui-même `server-only`) → pas de redondance. **Stub Vitest** : `server-only` lève une erreur à l'import hors react-server → `test/server-only-stub.ts` + alias `resolve.alias["server-only"]` dans `vitest.config.ts` (le garde-fou réel reste appliqué au build Next). **Le garde-fou a RÉVÉLÉ une dette de couplage réelle, corrigée à la racine** : `media-feed.ts` (importé par les Client Components Wizard/ResultsView/MediaNeedsBlock/BackupBlock pour `getMediaPricesEur`) tirait `scraper.ts` via `refreshMediaPriceFreshness` → tout le sous-graphe serveur (scraper + llm/client) était bundlé côté client. Extrait `refreshMediaPriceFreshness` + type `MediaPriceFreshness` dans **`lib/pricing/media-freshness.ts`** (server-only) ; `media-feed.ts` redevient **PUR/client-safe** (FX + normalisation + seed, `mediaPriceSources` pur conservé). `media.test.ts` : import de `refreshMediaPriceFreshness` re-pointé. typecheck 0 / lint 0-0 + garde i18n / tests verts / build OK (le build est le juge : il échoue si un Client Component importe un module `server-only`). **Reste de la dette auth (S-089) BLOQUÉ sur inputs Amine** (SMTP + apps OAuth).
- 2026-06-01 — **S-077 ✅ TRANCHE 5 (dernière) — Régimes phares hors-UE MODÉLISÉS (LGPD/Brésil + APPI/Japon).** Clôt S-077 (T1→T4 livrés sessions précédentes : seed sourcé 16 juridictions, veille `regime-discovery`, feed+route `/api/legal/regimes`+audit RLS `regime_observations`, UI `RegimeDetector`). **T5** applique la décision Amine #2 (« les deux ») : on **modélise** les régimes phares hors-UE qu'on peut **sourcer rigoureusement** (DÉFCON 1), le reste demeure affichage-seul. **Extension d'énum** `REGULATIONS += ["lgpd","appi"]` (`lib/engine/types.ts`) → propage automatiquement aux consommateurs basés sur `REGULATIONS`/`REGULATION_OPTIONS` (share `encode.ts` via `isMember`, `recap.ts`/`model.ts` via `labelsOf`). **Seed** `regime-seed.ts` : `code` mappé (`bresil→lgpd`, `japon→appi`) → désormais **pré-cochables** par le détecteur. **`mapRegimeCode`** (regime-discovery) : LGPD/Lei 13.709/APPI → énum. **`computeCompliance`** (`lib/engine/diagnostics.ts`) gagne 2 branches d'**actions de conformité SOURCÉES** : LGPD (6 actions ancrées sur les articles vérifiés ANPD/Planalto — Art. 41 encarregado, 18 droits, 37 registre, 38 RIPD, 33 transferts internationaux, 48 communication d'incident) + APPI (6 obligations confirmées via la PPC, **sans numéro d'article incertain** — finalité d'utilisation, mesures de sécurité, fourniture à des tiers, droits des personnes, notification de fuite oblig. depuis l'amendement 2022, transferts transfrontaliers). **`scores.ts`** : `+1` conformité par régime data-protection structurant (cohérent avec le `+1` RGPD). i18n `Engine.diagnostics.compliance.{lgpd*,appi*}` (12) + `Options.regulation.{lgpd,appi,*Hint}` (4) en fr/en/ar (parité, accents majuscules FR). **Sourçage DÉFCON 1 (WebSearch + Planalto/ANPD/PPC, 2026-06-01)** : articles LGPD confirmés sur source primaire ; obligations APPI confirmées (numéros d'article volontairement omis car renumérotés par les amendements 2020/2022 → non confirmés). Tests : `diagnostics.test.ts` (nouveau, 5) + cas ajoutés `regime-discovery`/`regime-seed`/`regime-detector` + e2e `regimes.spec.ts` (LGPD/APPI sélectionnables + détecteur présent). typecheck 0 / lint 0-0 + garde i18n / **611 tests** / build OK. **→ S-077 COMPLET.**
- 2026-05-31 — **S-074 ✅ — Tarification de l'inférence LLM à l'usage (au 1M tokens in/out, directive Amine).** Lève la dette « LLM à l'usage non chiffré » (= non COÛTÉ, ≠ sécurité ; S-033/S-041). **Sourcing réel (WebSearch+WebFetch officiels, 2026-05-31)** : DeepSeek V4 Flash $0.14/$0.28 (défaut proxy, open/souverain), Mistral Small 4 $0.10/$0.30 + Large 3 $0.50/$1.50 (UE), OpenAI GPT-5 mini $0.25/$2.00 (hyperscaler). **`lib/engine/llm-usage.ts`** (pur) : contrat `LlmTokenPrices` (€, multi-segment : defaultApi + options) + `NEUTRAL` (0) + `estimateLlmUsage` (volume : reqPerDay×30 × 2500 in/600 out par requête, ±30 %) + `costLlmUsage` avec **gating anti-double-comptage** (HARD/preferSovereign → auto-hébergé → coût 0, sur GPU/compute déjà compté ; sinon API : tokens × prix/1M). **`lib/pricing/llm-token-seed.ts`** : seed devises natives USD + FX→€ (`SEED_USD_TO_EUR` partagé médias), sources officielles datées, confiance medium « à rafraîchir par veille » (doctrine prix-jamais-hardcodes). **Intégration** : `recommend(…, llmPrices=NEUTRAL)` → `applyLlmUsage` plie le coût dans C6 (note compute « LLM non chiffré » retirée) ; `Recommendation.llmUsage` ; source dans `costSources` ; neutre par défaut → invariant `totalCost` préservé ; `buildEnsemble` threadé ; `ResultsView`/`Wizard` injectent `getLlmTokenPrices()`. **CostMap** : ligne dédiée « Inférence LLM à l'usage » (modèle, k tokens/mois, source, ±30 %, gated >0). +7 tests (estimateur monotone, gating self-host, formule API, intégration neutre→invariant + injecté→coût+source). `docs/pricing/llm-token-cost-sources.md`. parité 987×3, typecheck 0, lint 0/0 + garde, build OK. **RESTE batch** : S-073 (multi-continent + fournisseurs par pays + redondance) ; S-075 (CI/CD toggle UI).
- 2026-05-31 — **S-072 ✅ — Veille = choix utilisateur en CONTRAINTE ABSOLUE (directive Amine « crawl4ai doit obligatoirement respecter les choix »).** Constat : le toggle **`preferSovereign` n'était PAS respecté** par la veille (choix utilisateur ignoré !) ; le reconcile ne filtrait que `secret`/`secret-pro`/`hipaa`. Créé `lib/catalog/constraints.ts` = **source de vérité unique** appliquée à la **requête** (orientation web), au **prompt LLM** (RÈGLES DURES « reject any component that violates these ») ET au **garde-fou `reconcileCatalog`** : `deriveCatalogConstraints` (`requireSovereign` = secret OU secret-pro/hipaa OU **preferSovereign**), `candidateViolatesConstraints` (souveraineté exigée ⇒ **jamais d'API tierce** → repli seed `flagged`), `constraintQueryHint`/`constraintPromptBlock`. **DÉCISION (non-overreach)** : on ne rejette QUE sur des **choix EXPLICITES** ; le RGPD seul + données internes n'interdit PAS une API tierce (transferts encadrés par SCC/DPA possibles) → la zone oriente la recherche mais ne rejette pas en dur (la résidence/les transferts sont encadrés par le modèle dédié). `proposeForSlotLive` (query+prompt) en anglais alignés. +4 tests (`constraints.test.ts` : preferSovereign rejette API tierce, secret/hipaa idem, RGPD+interne accepte, query/prompt portent les contraintes) → **506 tests**. typecheck 0, lint 0/0 + garde, build OK. **Prérequis du multi-continent (S-073)** : la découverte de fournisseurs par pays devra réutiliser ces contraintes.
- 2026-05-31 — **Veille juridique S-062 — refresh UI live dans `ResidencyPanel`** (`f08376a`) : le panneau fetch `/api/legal/transfers` au montage et annote chaque flux affiché avec la base **reconciliée** (statut/base légale/note + fraîcheur ; `flagged` distinct, jamais adopté en douce) ; indispo → seed (gracieux). 3 clés `liveChecking`/`liveFresh`/`liveFlagged` (fr/en/ar, parité 978×3). `CatalogProvenance` était déjà live → la consommation UI des veilles est complète.
- 2026-05-31 — **Veilles S-036/S-062 — persistance de l'audit trail câblée (« increment suivant » documenté) + correction d'une trace erronée.** Les builders d'observations (`buildCatalogObservations`/`buildTransferObservations`) existaient mais **aucune route ne les insérait**. Ajout de `lib/catalog/persist.ts` (`persistCatalogObservations`) + `lib/legal/persist.ts` (`persistTransferObservations`) : insertion service-role (`createAdminClient`), **gatée « live/flagged seulement »** (un catalogue/statut 100 % seed n'est PAS une observation → zéro écriture inutile), **jamais bloquante ni throw** (échec Supabase/clé absente → repli silencieux, la veille répond quand même), `circle_id` null en anonyme. Branchées dans `app/api/catalog/live` (await après assemblage) et `app/api/legal/transfers` (await après veille). +8 tests (mock client minimal : seed→0 écriture, live→N lignes, client null→0, erreur insert→0, ne lève jamais) → **502 tests**. typecheck 0, lint 0/0 + garde, build OK. **CORRECTION DÉFCON 1** : la « dette tokens LLM non chiffré » était **mal libellée** (par moi) — « non chiffré » = **non chiffré en € (non coûté)**, PAS « non encrypté ». Réalité : le coût d'inférence LLM à l'usage n'est pas encore tarifé dans le modèle (flaggé « à chiffrer en devis », cf. S-033/S-041) ; **aucune dette de sécurité**. Passation corrigée. **RESTE veilles** : le branchement UI live de la veille juridique dans `ResidencyPanel` (les transferts affichés viennent encore du seed via `lookupTransferBasis` ; `/api/legal/transfers` existe + persiste désormais mais n'est pas encore fetché par le panneau) = increment UI futur.
- 2026-05-31 — **S-060 ✅ TRANCHE 2 — Arabe `ar` activé (brouillon MSA via LLM projet + validé techniquement) → ÉPIC i18n COMPLET.** **Génération du brouillon** (choix Amine : « draft via le LLM du projet puis relecture ») : `messages/ar.json` produit via le proxy LiteLLM (`deepseek-v4-flash`), namespace par namespace avec consigne stricte (préserver placeholders ICU `{x}`, mots-clés + **clés** de `select`/`plural`, pluriels arabes corrects zero/one/two/few/many/other, tags `<>`, termes tech) ; gros namespaces (Engine/Results/Deliverable) découpés récursivement (évite HTTP 502 + pertes de clés). **VALIDATION TECHNIQUE (toute verte)** : parité **975/975** sur les 3 locales ; **ICU parse `ar` 0 échec** sur les 975 chaînes ; comparaison des **arguments ICU** en/ar via AST formatjs = **0 divergence** ; **clés `select` préservées** ; les `plural` correctement étendus aux catégories arabes. **Activation** : `i18n/config.locales` += `ar`, `localeNames.ar="العربية"`, `promptLanguageNames.ar="Arabic"`, `isLocale` += ar, `request.ts` chargeur `ar`. RTL déjà géré (tranche 1 : `localeDir`/propriétés logiques/police/îlots LTR mono). **e2e** : test `ar` (bascule → `dir=rtl` + `lang=ar` + script arabe rendu dans le h1 ; retour fr → LTR) — **6/6 verts** (desktop+mobile). typecheck 0, lint 0/0 + garde, **494 tests**, build OK. ⚠ **DÉFCON 1 — NON DÉPLOYÉ** : le contenu arabe est un **brouillon MSA généré**, techniquement valide mais **PAS encore relu par un natif** → prod reste à `7b7eeac` (fr/en) ; **faire relire `messages/ar.json` par un arabophone natif avant d'exposer `ar` publiquement** (la qualité prose/terminologie est le seul reliquat ; tout le reste est livré + vérifié). **→ ÉPIC i18n S-057→S-060 COMPLET (70/70 stories), arabe en attente de relecture native avant déploiement public.**
- 2026-05-31 — **S-060 🚧 TRANCHE 1 ✅ — Infrastructure RTL (sans encore activer `ar` → neutre pour fr/en).** Pose les rails du miroir RTL, vérifiable et sûr avant la traduction. **`i18n/config.ts`** : `RTL_LOCALES` (set `{ar}`) + `localeDir(locale): "ltr"|"rtl"` (accepte une chaîne, sûr pour toute valeur). **`app/layout.tsx`** : `<html dir={localeDir(locale)}>` (ltr pour fr/en). **Migration physique→logique** (Tailwind 3.4, propriétés logiques) : `text-left/right`→`text-start/end` (13 fichiers), `pl-5/6`→`ps-5/6` (ResidencyPanel/ResidencyBlock/page), `left-0`→`start-0` (InfoBubble/page), inline `marginLeft`→`marginInlineStart` (EnsembleView). **Audit confirmé** : zéro `ml-/mr-/pr-/border-l/r/rounded-l/r/space-x/flex-row-reverse` directionnel résiduel. **`globals.css`** : (a) `:lang(ar)` → pile de polices arabes (fallback système Noto Sans Arabic/Geeza Pro/Segoe UI/Tahoma, pas de téléchargement) ; (b) `[dir="rtl"] .font-mono { direction:ltr; unicode-bidi:isolate }` → les données mono (coûts/scores/clés/€) restent LTR dans le flux RTL (acceptance). En LTR les propriétés logiques == physiques → **zéro changement visuel fr/en**. typecheck 0, lint 0/0 + garde, **494 tests**, build OK. **Reste S-060 tranche 2** : `messages/ar.json` (traduction native), ajouter `ar` à `i18n/config.locales`/`localeNames`/`promptLanguageNames`/`isLocale`, e2e `ar` (RTL + bascule).
- 2026-05-31 — **S-059 ✅ TERMINÉE — RÉSIDUELS 2/4, 3/4, 4/4 traités (épic i18n : reste S-060).** **Résiduel 2 — `lib/wizard/recap.ts` chrome** : `buildChoiceRecap` gagne un traducteur injecté `recapText: RecapLabel` (namespace `Wizard.recap`) ; headings (4), labels (20), valeurs (« managé », Oui/Non via select, backup `criticité {criticality, select…}, {copies, plural…}`, modalités `{modality, select audio/video/images}`, mode `{mode, select sovereign/api}`, « Aucun module/besoin », `moduleItem {name} (niv {level}/{max})`) → clés ; `Wizard.tsx` passe `useTranslations("Wizard.recap")`, `recap.test` adapté (stub `stubRecap` réinjectant `name`). **Résiduel 3 — contexte LLM + messages API** : `lib/llm/precision-notes.ts` préfixes « Autre » → ANGLAIS (cohérence du prompt anglais ; sortie pilotée par `{{language}}`). **Constat (vérifié) sur les messages JSON `app/api/**`** : ils ne sont **JAMAIS affichés** — les clients localisent leur propre erreur (`SharePanel` → `t("shortError")`, `SurveyForm` → état `error` localisé, `AssistantPanel` → `t("unavailable")`, narrate → repli silencieux) → chaînes serveur-internes, **PAS** une lacune user-facing (cohérent avec l'exclusion `app/api` de la garde). **Résiduel 4 — `FIDUCIARY_CHARTER`** : contenu (title/intro/revenueModel + 5 engagements) déplacé dans le namespace **`Fiduciaire`** (source UNIQUE partagée page /fiduciaire + livrable, anti-drift) ; `charter.ts` ne porte plus que la STRUCTURE (`FIDUCIARY_COMMITMENT_KEYS` + `FIDUCIARY_LAST_UPDATED`) ; page /fiduciaire itère les clés (`t("commitment.<key>.title/detail")`) ; `buildDeliverable` gagne `fiduciaryText: DeliverableLabel` (namespace `Fiduciaire`) → bullets charte depuis les clés ; `ExportButtons` passe `useTranslations("Fiduciaire")` ; `export.test`/`fiduciary.test` adaptés (charte lue des messages fr). parité **975/975**. typecheck 0, lint 0/0 + garde, **494 tests**, build OK, e2e i18n 4/4. **→ S-059 `passes:true`. Reste épic i18n : S-060 (arabe + RTL).**
- 2026-05-31 — **S-059 🚧 RÉSIDUEL 1/4 ✅ — Prose moteur `ensemble.ts` i18n (le manque corrigé).** `EnsembleVariant.{label,intent}` + `assumptions` → `Message` ; `EnsembleSpread.uncertaintyLabel` → `Message` (valeurs chiffrées + `select` ICU sur `agreement` pour le mot d'accord ET la phrase de synthèse). `EnsembleAgreement` reste une **union** (discriminant : `AGREEMENT_TONE` chip + branches `select`). `VARIANT_META`/`describeUncertainty` supprimés (plus de prose composée dans le moteur) → `buildVariant` émet `msg("ensemble.<id>.label/intent")`, `assumptionsOf(id)` émet `msg("ensemble.<id>.assumption.<n>")` (compte par variant aligné sur les clés). **Messages** `Engine.ensemble.*` fr/en natif (uncertainty + 3 variants × label/intent/assumptions). Le mot d'accord, jadis interpolé brut (`Results.ensemble.agreement` = « Accord {agreement} » → « Accord fort » même en EN), passe en **`select`** dans `Results.ensemble.agreement` ET `Deliverable.meta.agreementValue`. **Consommateurs** : `EnsembleView` (résout label/intent/assumptions des variants + uncertaintyLabel via `useEngineText`), `ResultsView` (bandeau scénario actif `resolveEngine(activeVariantData.label)`), `buildDeliverable` (résout v.label + uncertaintyLabel + agreement méta). Tests ensemble (unit asserten id/values ; composant via provider fr). parité **929/929**, rendu ICU fr/en×3 accords validé. typecheck 0, lint 0/0 + garde, **494 tests**, build OK. **Reste résiduels 2-4** : recap.ts chrome, messages app/api + contexte LLM, FIDUCIARY_CHARTER.
- 2026-05-31 — **S-059 🚧 PARTIE 2 ✅ — Chrome du LIVRABLE (MD/PDF) localisé + CORRECTION d'un manque i18n moteur.** **`buildDeliverable`** (lib/export/model.ts) gagne un traducteur injecté **`deliverableText: DeliverableLabel`** (namespace `Deliverable`) : titres de sections (Profil/Stack/Scores/Carte de coûts/Ensemble/Conformité/Risques/Charte/Pourquoi/Catalogue), libellés de profil (15), méta (preset/score/coût/accord), valeurs (Oui/Non/inclus/Total estimé/Fourchette/niv./€/€-mois), provenance (live/seed/flagged), disclaimer — tous via clés. `renderMarkdown`/`renderPdf` inchangés (rendent l'objet `Deliverable` déjà localisé). `ExportButtons` passe `useTranslations("Deliverable")` ; le const `DISCLAIMER` de model.ts supprimé (→ clé). `messages.Deliverable.*` fr/en (parité **910/910**). Tests export/fiduciary asserten les CLÉS (stubs `tEngine`/`tKey`). typecheck 0, lint 0/0 + garde, **494 tests**, build OK. **CORRECTION HONNÊTE (DÉFCON 1)** : mon affirmation « la prose MOTEUR est INTÉGRALEMENT i18n » (tranches 7/8) était **inexacte** — le module **`lib/engine/ensemble.ts` a été MANQUÉ** : `EnsembleVariant.{label,intent}` (VARIANT_META) + `EnsembleSpread.{agreement ("fort/modéré/faible"),uncertaintyLabel}` restent des **chaînes FR** (rendues dans EnsembleView + le livrable). C'est une prose moteur à convertir en `Message` (même pattern que modules/verdict). **RÉSIDUELS i18n RESTANTS (tracés, NON silencieux)** : (1) **ensemble** (label/intent/agreement/uncertaintyLabel → Message) ; (2) **`lib/wizard/recap.ts`** chrome rendu FR ; (3) messages JSON `app/api/**` + contexte LLM ; (4) **`FIDUCIARY_CHARTER`** (revenueModel/commitments, données FR rendues dans le livrable + page fiduciaire). → à finir avant S-060.
- 2026-05-31 — **S-059 🚧 PARTIE 1 ✅ — Prompts réécrits niveau natif (anglais structuré) + réponses LLM localisées (locale → routes).** Cœur de l'ask Amine (« prompts ridicules → comme un vrai natif »). **DÉCISION (documentée)** : gabarits rédigés en **ANGLAIS structuré de qualité native** (ROLE · TASK · RULES · OUTPUT) — les modèles suivent le mieux des instructions anglaises — et la **langue de SORTIE user-facing est pilotée par le placeholder `{{language}}`** (rempli depuis la locale active). Sorties JSON (intake, catalog-veille, legal-veille) = **langue-agnostiques** → pas de `{{language}}`. **`lib/prompts/registry.ts`** : les 5 `DEFAULT_PROMPTS` réécrits (placeholders `{{…}}` + clauses DÉFCON 1 PRÉSERVÉS, en EN : « INVENT NOTHING », « NO number », « NEVER invent a URL », « NOT legal counsel »…) ; `PROMPT_META` gagne le placeholder `language` (narration, assistant). **`i18n/config.ts`** : `promptLanguageNames` (locale → nom anglais : fr→French, en→English) — distinct des autonymes du sélecteur. **Builders** : `buildNarrateMessages(ctx, template?, language="French")` + `buildChatMessages(…, language="French")` injectent `{{language}}` ; le contexte interne (profil, notes anti-injection, message user, `adjustContext` d'intake, `formatWebResults` vide) traduit en anglais pour la cohérence du prompt (sortie reste pilotée par `{{language}}`). **Routes** `narrate`/`chat` : lisent `raw.locale` (validé `isLocale`, repli `defaultLocale`) → `promptLanguageNames[locale]` passé au builder. **Clients** : `ResultsView` (narrate) + `AssistantPanel` (chat) envoient `locale: useLocale()` dans le body (deps effets/callback ajustées). **Tests** : substrings DÉFCON 1 mis à jour vers l'EN (registry/intake/narrate/assistant) + 2 tests route narrate prouvant le flux locale→langue (`locale:en`→« write EVERY value in English » ; `locale:zz`→repli French). typecheck 0, lint 0/0 + garde verte, **494 tests** (+3), build OK. **DÉFCON 1 intact** : les validateurs serveur déterministes (`parseNarration`/`parseIntakeProfile`/`reconcile`) bornent toujours la sortie quel que soit le prompt → un prompt change la QUALITÉ, jamais la correction. **Reste S-059 (PARTIE 2)** : livrable MD/PDF + verdict localisés (en-têtes/disclaimer de `buildDeliverable`) ; **résiduels S-058** : `recap.ts` chrome rendu FR + messages JSON `app/api/**`. Puis S-060 (arabe/RTL).
- 2026-05-31 — **S-058 ✅ reliquat CLOS — TRANCHE 8 (chantier c) : garde « zéro littéral » + e2e fr↔en + corrections UI surfacées.** Dernier chantier du reliquat moteur. **(1) Garde CI** `scripts/check-no-hardcoded-ui-strings.mjs` (script de scan, plus robuste qu'un `react/jsx-no-literals` ingérable) : signale toute chaîne littérale **accentuée** (signal français quasi infaillible ; clés/identifiants i18n = ASCII) dans `components/**` + `app/**` RENDUS, **hors `app/api/**`** (réponses JSON serveur → S-059) et tests ; commentaires strippés ; allowlist vide. Câblée dans `npm run lint` (`eslint . && node scripts/...`) + script `check:i18n` → couvre CI sans toucher `ci.yml`. **(2) Corrections UI réelles surfacées par la garde/scan** : `StatusDot` (libellés de confiance + title « Confiance : » → namespace i18n `StatusDot`, `useTranslations` OK en RSC+client ; `CONFIDENCE_DOT` = donnée couleur) ; `app/resultats/page.tsx` (metadata + header « Votre infrastructure souveraine » étaient **bruts** — non détectés car SANS accents — → `generateMetadata` + `getTranslations`, clés `Results.{metaTitle,metaDescription,pageEyebrow,pageTitle}`) ; `LayerStack` (« Alternatives : » + « inclus » → `Results.{alternativesLabel,included,perMonth}` ; `layer.note`/`alternatives` restent données brutes catalogue) ; `ui.test.tsx` basculé vers `@/test/render`. **(3) Extraction LLM-context** : les 3 préfixes « Autre » (contexte de narration, NON rendus) sortis de `ResultsView` vers un helper pur `lib/llm/precision-notes.ts buildOtherPrecisionNotes(profile)` (+4 tests) → la couche LLM les localisera en S-059. **(4) e2e `e2e/i18n.spec.ts`** (desktop + mobile, 4 verts) : bascule accueil fr→en→fr (cookie `NEXT_LOCALE` + `<html lang>` + chrome bilingue, `{exact:true}` pour éviter la collision footer) ; **verdict rendu EN** (clic « See my verdict » → `/resultats?mode=verdict` → « Your verdict » + gain « proven » EN) = **preuve end-to-end que l'i18n MOTEUR atteint le navigateur**. PIÈGE mobile : `selectOption` avant hydratation ne câble pas l'`onChange` → attendre un élément client visible d'abord. **Parité messages fr/en = 863/863 clés.** typecheck 0, lint 0/0 + garde verte, **491 tests** (+4), build OK. **RÉSIDUELS TRACÉS (honnêteté DÉFCON 1, NON silencieux) — hors périmètre défini de S-058, à traiter ensuite** : (i) `lib/wizard/recap.ts buildChoiceRecap` produit du **chrome rendu encore FR** (headings « Résultat/Profil & conformité/… », ~17 labels, valeurs « Oui/Non », « managé », « criticité X, N copie(s) », « Aucun module activé », modalités « Audio/Vidéo/Images », « souverain/API ») — non capté par la garde (scope = components+app, pas lib/ qui contient de la donnée FR légitime) ; ChoiceRecap les rend bruts → **tranche chrome-recap dédiée** ; (ii) messages JSON des routes `app/api/**` (admin/narrate/share/sondage) + prose de contexte LLM (`precision-notes`, prompts `narrate`) = FR, **localisés en S-059** quand la locale atteint le serveur. **→ Reste épic i18n : S-059 (prompts natifs + locale aux routes LLM + livrable/PDF localisés ; inclut recap-chrome + messages API + contexte LLM), S-060 (arabe + RTL).**
- 2026-05-30 — **S-058 🚧 reliquat moteur — TRANCHE 7 ✅ : VERDICT complet (chantier b). La prose MOTEUR est désormais INTÉGRALEMENT i18n.** Derniers champs textuels du `Verdict` convertis en `Message` : `pain`, `gain`, `firmPriceTier`, `nextStep` (`risk` était déjà fait en T2a). **`recommend.ts buildVerdict`** : suppression des constantes prose `PAIN_BY_ACTIVITY`/`PAIN_DEFAULT`/`FIRM_PRICE_TIER_PLACEHOLDER` (+ import `Activity` devenu inutile) → `pain: msg("verdict.pain", { activity })`, `gain: msg("verdict.gain")`, `firmPriceTier: msg("verdict.firmPriceTier")`, `nextStep: setupCost>0 ? msg("verdict.nextStep.withSetup") : msg(".default")`. **PIÈGE ICU résolu** : une clé de `select` ICU **ne peut pas contenir de tiret** (`cabinet-regule` → `EXPECT_SELECT_ARGUMENT_SELECTOR_FRAGMENT`) → le moteur normalise le discriminant `profile.activity.replace(/-/g,"_")` ; la branche `other` couvre l'activité `other` ET tout futur ajout (= ancien défaut). **`DisplayVerdict`** (lib/llm/narrate, forme résolue tout-chaînes) **inchangé** : `ResultsView` résout désormais les 5 champs (`pain/risk/gain/firmPriceTier/nextStep`) vers `DisplayVerdict` au mode verdict + alimente la base narration (`VerdictView` et `mergeVerdictNarration` n'opèrent toujours que sur des chaînes). **`assistant.ts serializeRecoFacts`** : `firmPriceTier` résolu via le `resolve` déjà injecté. **`buildSimulationLog`** (lib/conversion) inchangé (colonne `verdict: unknown` → stocke les descripteurs, audit reproductible + locale-agnostique). **`messages/{fr,en}.json`** : `Engine.verdict.{pain (select sur activité), gain, firmPriceTier, nextStep.{withSetup,default}}` (EN natif). **Tests** : `modules-i18n.test.ts` réécrit SANS `createTranslator` (navigation JSON pure + stub — `createTranslator` impose un typage de clés trop strict pour tsc ; pattern « assert id/values + complétude JSON ») ; `recommend-sizing.test.ts` asserte les `id`/`values` du verdict (plus la prose). Rendu ICU des 7 branches `pain` validé fr+en hors test. typecheck 0, lint 0/0, **487 tests**, build OK. **Reste S-058** : chantier (c) garde ESLint « zéro littéral » + e2e bascule fr↔en. **Puis S-059** (prompts natifs + locale aux routes LLM + livrable/PDF localisés), **S-060** (arabe + RTL).
- 2026-05-30 — **S-058 🚧 reliquat moteur — TRANCHE 6 ✅ : modules (chantier a).** Conversion de toute la prose des modules avancés en descripteurs `Message` (i18n moteur). **Types** : `ModuleLevel.{label,desc}`, `EngineModule.{name,why,baseTask}` + `ActiveModule.{name,levelLabel,levelDesc,baseTask}` → `Message` (`layers`/`effort` restent des codes techniques, pas de prose). **`modules.ts`** : `MODULES` ne porte plus de chaîne FR → `msg("modules.<id>.{name|why|baseTask}")` + helper `lvl(id,n)` pour `Engine.modules.<id>.level.<n>.{label,desc}`. **DÉCISION (documentée)** : le champ `ActiveModule.task` (phrase pré-composée DANS `computeActiveModules` = `[Niveau X/Y, label] baseTask … desc`) — **jamais lu** (grep exhaustif : UI/export/bundle/recap/assistant/tests, seul `recommend.ts` l'écrivait) ET prose assemblée dans le moteur pur (anti-pattern i18n) — est **supprimé** au profit d'un **helper pur `formatModuleTask(active, resolve)`** (modèle jumeau de `formatPresetReason`) qui compose via le gabarit `Engine.modules.taskTemplate` ; le contenu (plan d'action par module) est **préservé** comme `baseTask`+niveaux localisables (rien purgé, prêt à câbler dans un livrable futur). **`scores.moduleBonus`** : `modName` (valeur ICU) ne peut plus être passé (mod.name est un `Message`, pas une chaîne) → on transmet **`modId`** et le catalogue résout le nom via un **`select` ICU inline** ; parité avec `modules.<id>.name` garantie par un **test anti-drift** (les noms du select === `modules.<id>.name` en fr ET en). **Consommateurs câblés** : `ModuleSlider` (`useEngineText` → name/why/levels), `CostMap` (`resolveEngine(mod.name)`), `export/model.ts` + `exit/bundle.ts` (`resolve(m.name)`), `lib/wizard/recap.ts` (`buildChoiceRecap` gagne un param `resolveEngine: EngineResolver` ; `Wizard.tsx` passe `useEngineText()`, le test un stub). **`messages/{fr,en}.json`** : namespace `Engine.modules.*` complet (5 modules × name/why/baseTask + 18 niveaux) + `taskTemplate`, EN **natif** (pas calque) + `moduleBonus` en `select`. **Tests** : nouveau `modules-i18n.test.ts` (4) via `createTranslator` next-intl — résolution fr+en de tous les champs, **parité anti-drift**, rendu réel de `formatModuleTask`, bonus modules sans fuite d'id brut ; `recap.test.ts` adapté (stub resolver + assertion sur le nom résolu). typecheck 0, lint 0/0, **487 tests** (+4), build OK. **Reste reliquat S-058** : (b) VERDICT complet (`pain`/`gain`/`nextStep`/`firmPriceTier` → `Message` résolus vers `DisplayVerdict`) + S-059 ; (c) garde ESLint « zéro littéral » + e2e bascule fr↔en. Pattern établi : `Message{id,values}` + `msg()`, UI `useEngineText()`, pur = résolveur `EngineResolver` injecté, champ composé = helper pur, valeur ICU dépendant d'une prose = `select` + test de parité.
- 2026-05-29 — **S-064 ✅ — Saisie libre « Autre » dans le configurateur (activity + zone + region), DÉFCON 1.** Demande Amine : les champs « Autre » n'offraient aucune saisie pour préciser, et la précision devait être prise en compte. Conçu par moi (cahier des charges) + implémenté par sous-agent (activity/zone) puis **complété par moi (region)** + **gate qualité** (le sous-agent avait mal taggé « S-063 » = autre story → corrigé en S-064 ; n'avait pas fait region → ajouté). **`Profile.otherText?: Partial<Record<"activity"|"zone"|"region", string>>`** (optionnel → invariant : sans précision, coûts/comportement **strictement inchangés**). `useWizardProfile.setOtherText`. **`OtherTextField`** (i18n `Wizard.other.*` fr/en) rendu **inline** sous le RadioCards quand la valeur === `"other"` (activity/zone) ; pour `region` dans le bloc infra quand `residency.primaryRegion === "other"` OU `allowedRegions` inclut `"other"`. **Prise en compte (canal CONTEXTE-seulement, jamais un chiffre)** : récap `buildChoiceRecap` (« Autre : <précision> » pour activity/zone), **narration** (`narrationContext.notes` pour les 3, garde-fou `isCleanNarration`), **assistant** (`serializeRecoFacts(reco, resolve, otherPrecisions{activity,zone,region})`). **DÉFCON 1 : le moteur `lib/engine/` reste PUR et chiffre sur l'énum (`other` = défaut), AUCUN coût inventé depuis du texte libre** — la précision nourrit la *recommandation/explication*, pas le prix déterministe (voie `freeNotes`+intake disponible pour qui veut affiner des champs bornés). +tests (recap : précision intégrée / texte résiduel ignoré / invariant ; assistant : greffe + invariant ; wizard : input visible au clic « Autre » + persistance ; hook : round-trip otherText). typecheck 0, lint 0/0, **446 tests**, build OK.
- 2026-05-29 — **S-058 🚧 EN COURS — i18n exhaustive (épic, périmètre Amine « TOUT y compris refactor moteur »).** Décision Amine : le moteur PUR ne doit pas porter de prose i18n → il renvoie des **descripteurs `Message` sérialisables `{ id, values }`** ; l'UI les résout en ICU fr/en (`lib/i18n/engine.ts useEngineText`, namespace `Engine`) ; les fonctions pures non-UI (`buildDeliverable`) reçoivent un **résolveur injecté** `EngineResolver` (UI fournit `useEngineText`, tests un stub `(m)=>m.id`). Tests réécrits en assertions `id`+`values` (plus robustes que la prose). **Exécution en incréments vérifiés (typecheck/lint/tests/build verts + commit chacun), PAS de big-bang** (DÉFCON 1 sur ~400 tests + traductions EN natives). **Pattern de migration d'un champ moteur** : (1) type `string`→`Message` dans `types.ts` ; (2) la fonction émet `msg(id, values)` (discriminants `select` = chaînes "yes"/"no"/énum, jamais booléen ; pluralisation via `{n, plural,…}`) ; (3) consommateurs UI résolvent via `useEngineText` ; (4) `buildDeliverable` applique `resolve(...)` sur ce champ ; (5) catalogues `Engine.<...>` fr/en (EN natif, pas calque) ; (6) tests → `id`/`values`. **Pattern de migration d'un composant** : `useTranslations('<Namespace>')` + tests via **`@/test/render`** (provider next-intl, **ré-exports EXPLICITES** — `export *` masque le `render` custom selon le bundler, piège confirmé). **PLAN D'INCRÉMENTS** : ✅1 fondation (`message.ts` `Message`/`msg`/`EngineResolver` ; `lib/i18n/engine.ts` ; `test/render.tsx`) · ✅2 `ScoreDimension` (label/why + `bonuses[]`) + `Results.scoreShort` · ✅3 `Layer.name` (choice/note/alternatives = données catalogue, restent `string`) + résolveur dans bundle Exit Escrow/serializeRecoFacts (param `resolve` optionnel défaut `(m)=>m.id`) · ⏳4 modules (`EngineModule`/`ActiveModule` ; attention : `ActiveModule.task` composé de baseTask+label+desc dans recommend.ts:60 ; `recap.ts` PUR utilise `m.name` → injecter résolveur) · ⏳5 `presetReason` + diagnostics (`compliance`/`risks`/`kmChecks`) · ⏳6 backup (prose `BackupPlan`/`VectorDecision.reason`) · ⏳7 résidence/transferts (`TransferFlag` legalBasis/note, `conflict`, `interSiteSecurity.note`, `CostSource.label`, `WorkloadLine.estimate`) · ⏳8 `Verdict` · **PIVOT Amine : chrome visible AVANT la fin de la prose moteur.** ✅chromeA homepage complète (`QuickProfile`+`Intake`) · ✅chromeC écran verdict (`VerdictView`) + page résultats (`ResultsView`, namespace `Results` ; `scenarioActive` en `t.rich`) — chrome bilingue ; le contenu moteur (verdict pain/risk/gain, scores why, compliance/risks) reste prose FR jusqu'aux incréments moteur · ⏳ chrome résultats RESTANT : `CostMap` (INTRIQUÉ avec énums backup/résidence FR → après incréments moteur 6/7, ou mixte assumé), `EnsembleView`, `CatalogProvenance`, `ResidencyPanel`, `ExportButtons`, `ExitEscrow`, `AssistantPanel`, `LivePriceStatus`, `PriceFreshness` · ✅chromeD axe « options » : `options.ts` clé-based (`OptionDef` labelKey/hintKey) + namespace `Options` fr/en + hook `useOptions()` + `optionLabelKey`/résolveur `OptionLabel` injecté dans le pur (`buildDeliverable` gagne param `optionLabel`, `buildChoiceRecap` aussi) ; consommateurs câblés (Wizard, ResidencyBlock, ResultsView volume, ExportButtons) ; tests via `@/test/render` + stubs `(k)=>k`. **Choix du configurateur/résidence/résultats/export = bilingues.** · ⏳ chrome wizard RESTANT (`Wizard.tsx` : `BLOCKS` titres/desc, `INFO` infobulles, labels de champs, `NoteField`, `INTAKE_FIELD_LABELS`, nav « Suivant/Précédent », profils-types) + blocs `BackupBlock`/`MediaNeedsBlock`/`ResidencyBlock`(chrome)/`ModuleSlider`/`ChoiceRecap`/`BudgetMeter`/`InfoBubble`/primitives (aria) · ⏳ pages (`/configurateur`,`/resultats`,`/fiduciaire`,`/sondage`,`/admin`) + `SurveyForm`/`AdminPrompts` · ⏳ garde ESLint zéro-littéral + e2e fr↔en (CostMap/LayerStack/EnsembleView/ResidencyPanel/CatalogProvenance/VerdictView/ExportButtons/AssistantPanel) · ⏳11 intake/admin/sondage/fiduciaire + pages (scaffolding, messages d'erreur, `lib/wizard/options.ts`) · ⏳12 garde ESLint « zéro chaîne en dur » + e2e fr↔en + revue EN finale. **Reste après S-058 : S-059 (prompts LLM + en-têtes/disclaimer livrable + PDF localisés — partiellement amorcé via `resolve`), S-060 (arabe + RTL).**
- 2026-05-29 — **S-057 ✅ — Fondation i18n (next-intl v4, App Router, mode SANS routing → locale par cookie). OUVRE l'épic MULTILINGUE (S-057→060).** Décision Amine 2026-05-28 : l'app était FR-only par convention ; on ouvre fr + en (ar/RTL = S-060). **Choix d'archi structurant (tranché dans l'intitulé de la story)** : locale **par cookie `NEXT_LOCALE`** + détection Accept-Language au 1ᵉʳ accès — **PAS de segment `[locale]` d'URL** → routes existantes + e2e intacts (disruption minimale). **`i18n/config.ts`** : source de vérité (`locales=[fr,en] as const`, `defaultLocale=fr`, `LOCALE_COOKIE`, `LOCALE_HEADER=x-next-locale`, `localeNames` autonymes, garde `isLocale`). **`i18n/request.ts`** : `getRequestConfig` lit cookie → repli en-tête `x-next-locale` (posé par le middleware pour le 1ᵉʳ rendu, le cookie tout juste posé n'étant pas lisible sur la même requête) → repli `fr` ; messages chargés via **`Record<Locale, () => Promise<{default: AbstractIntlMessages}>>` à chemins LITTÉRAUX** (TS résout le type JSON → **zéro `any`/`as`**, cf. CLAUDE.md). **`middleware.ts`** : négociation Accept-Language maison (tri q-values, base de tag, repli fr ; pas de dépendance externe) ; pose `x-next-locale` sur la requête + cookie 1 an si absent/invalide ; `matcher` exclut `api`/`_next`/assets (point dans le chemin). **`messages/{fr,en}.json`** namespacés par surface (`Metadata`, `Nav`, `Home.{hero,why,difference,how,pricing,diagnostic,ctaFinal,footer}`, `LocaleSwitcher`) — **fr + en complets pour l'accueil**. **`LocaleSwitcher`** (`components/ui/`, client) : `<select>` natif **étiqueté** (accessible clavier + lecteurs d'écran d'emblée), persiste le cookie via `document.cookie` puis **`router.refresh()`** (= reload moderne, la request config relit le cookie au nouveau rendu RSC), `useTransition`. **`next.config.mjs`** wrappé `createNextIntlPlugin("./i18n/request.ts")`. **`app/layout.tsx`** async : `getLocale()` → **`<html lang>` dynamique** + `NextIntlClientProvider` + `generateMetadata` localisé. **`app/page.tsx`** migré async `getTranslations` : **ZÉRO chaîne en dur** (hero en **`t.rich` avec balise `<mark>`** pour le mot coloré ; listes pilotées par **clés structurelles** `PROMISE_KEYS`/`DIFFERENTIATOR_KEYS`/`STEP_KEYS`/`TIERS`+`features`, drapeaux `featured`/`surveyTag`/`hasCta` testés via l'opérateur `in` ; « Strate » laissé décoratif = nom propre). **`playwright.config.ts`** : `use.locale="fr-FR"` → rendu FR **déterministe** (audience par défaut). **VALIDATION** : typecheck 0, lint 0/0, **tests 434 passed / 12 skip**, build OK (`/` devient **ƒ dynamique** car lit le cookie, Middleware 34,2 kB). **RUNTIME vérifié (curl, parcours UI)** : FR par défaut (« ne devrait jamais quitter vos murs » + `lang=fr`), EN via cookie (« should never leave » + `lang=en`), Accept-Language `en-US` → `lang=en` + `Set-Cookie NEXT_LOCALE=en` posé, sélecteur `aria-label="Langue"` présent. **Learnings** : (a) mode « sans routing » = **zéro régression de routes** : seul `/` passe en rendu dynamique (lecture cookie) → les e2e n'assertent aucune chaîne de `page.tsx` (ils ciblent `QuickProfileForm`/`IntakeField`, encore FR → migration exhaustive = S-058), donc verts ; (b) le 1ᵉʳ accès exige le **relais par en-tête** (`x-next-locale`) car le cookie posé en réponse n'est pas lisible par `getRequestConfig` sur la même requête ; (c) chargeurs de messages à **chemins littéraux** (pas de template `import(\`../messages/${locale}.json\`)` qui retournerait `any`) → DÉFCON typage respecté ; (d) `use.locale=fr-FR` dans Playwright neutralise la détection navigateur (sinon `en-US` basculerait le rendu) → suite déterministe ; (e) warnings build `import(t)` = **dist interne next-intl** (extractor), non bloquant. **Reste épic i18n : S-058 (extraction exhaustive fr/en de TOUTE l'UI), S-059 (prompts/PDF localisés), S-060 (arabe + RTL).**
- 2026-05-29 — **S-062 ✅ — Veille LIVE des statuts juridiques de transfert (jumeau de la veille catalogue S-036, DÉFCON 1 maximal sur du droit).** Un statut juridique ne se grave pas : S-043 avait posé les états datés + révisables (flag `volatile`), S-062 branche la veille qui rafraîchit + réconcilie. **`transfers.ts`** : `TransferProvenance` (seed/live/flagged) + `TransferBasis.provenance?` + **`WATCHED_TRANSFER_FLOWS`** (les paires `volatile` : DPF UE-US sous pourvoi CJUE, adéquation Maroc). **`reconcile.ts`** (PUR, jumeau `catalog/reconcile`) — **cœur DÉFCON 1** : `reconcileTransferBasis(seed, live, checkedAt)` — veille `null` → repli seed ; signal **concordant** → confirmé (provenance `live`, fraîcheur rafraîchie, source live, note « confirmé ») ; signal **divergent** → **JAMAIS adopté en silence** (statut seed conservé, confiance `low`, `volatile`, provenance `flagged`, note « ⚠ signal divergent … à revérifier par un conseil » + URL citée). Toujours `checkedAt` + `disclaimer` + révisable. **`live-transfers.ts`** (orchestration PURE, I/O injectée, jumeau `live-catalog`) : `proposeTransferLive` (recherche sources officielles Firecrawl + lecture LLM → signal sourcé ; **anti-hallucination : `sourceUrl` DOIT ∈ résultats** ; statut ∈ {ok,restricted,forbidden}) + `refreshTransferBases` (par flux : seed via `lookupTransferBasis` ; **si `volatile` → veille+reconcile, sinon seed intact** ; parallélisé, repli seed garanti, ne lève jamais). **`live-feed.ts`** (I/O réelle `searchWeb`+`callLLM` + `TtlCache` 6 h, clés **serveur**). **Prompt `legal-veille`** ajouté au registre (S-053 : `DEFAULT_PROMPTS` + `PROMPT_META`) → **éditable en /admin automatiquement** (la console itère `PROMPT_KEYS`) ; clauses DÉFCON 1 (statut sourcé, **PAS un avis juridique**, n'invente JAMAIS d'URL/fait, défaut prudent `restricted`). **Route `GET /api/legal/transfers`** (Node, clé serveur jamais exposée, `loadActivePrompt('legal-veille')` repli défaut). **Audit trail** : migration `20260529120000_transfer_status_observations` (**RLS activée**, pivot `circle_id`, helper `is_circle_member`, `source_url` **nullable** = résidence stricte légitime) + types Supabase (Row/Insert + `Database`) + builder PUR `buildTransferObservations` (`observation.ts`, jumeau `catalog/observation`). +17 tests (reconcile 3 ; observation 4 ; live-transfers 9 dont anti-hallucination d'URL, LLM KO→null, recherche vide→null, statut hors énum→null, **flux stable → aucune veille**, exception→repli seed, défaut = 2 flux volatils ; registry +1) + RLS gated 2 skip → **434 + 12 skip**. typecheck 0, lint 0/0, build OK (`/api/legal/transfers` ƒ). **Learnings** : (a) sur du DROIT, la divergence live ≠ adoption : on **flague pour revérification** (jamais d'écrasement silencieux d'un statut sourcé) — la réconciliation juridique est plus conservatrice que celle des prix/catalogue ; (b) ne veiller QUE les flux `volatile` (DPF/Maroc) = économie d'appels + cohérence (les statuts stables RGPD chap. V n'ont pas besoin de veille) ; (c) le pattern S-036 (PUR + I/O injectée + cache + reconcile + audit builder + RLS gated) se réplique 1-1 sur le juridique → zéro invention d'architecture. **À FAIRE EN PROD (mêmes patterns différés que S-036/S-053)** : appliquer la migration au Supabase self-hosté ; l'insertion d'audit depuis la route (anon/circle) + le branchement de la veille dans l'UI `ResidencyPanel` (rafraîchir les transferts affichés) = increment suivant (builder ready + testé + RLS gated). **Reste backlog : épic i18n S-057→060 (recommandé en session fraîche dédiée).**
- 2026-05-29 — **S-061 ✅ — Sécurisation inter-site self-hosted CHIFFRÉE (poste complet, prix sourcés, DÉFCON 1).** Prolonge l'épic Résidence : S-043 avait flagué la « surface à sécuriser » du self-hosted ; ici on chiffre le poste. **Profile** : `residency += selfHosted?` (choix EXPLICITE d'infrastructure — la vraie souveraineté) → `hostingClassForProfile(p)` renvoie `self-hosted` quand `residency.selfHosted` (avant : jamais — dérivé zone seulement). **Contrat** (`lib/engine/residency.ts`, le moteur reste pur) : nouveau type `InterSiteSecurityPrices` (`selfHostedGatewayPerMonth` + `managedPerUserPerMonth`) **remplace** l'ancien `interSiteSecurityPerMonth: MultimodalPriceEntry` ; `NEUTRAL_RESIDENCY_PRICES` maj. **`costInterSiteSecurity(users, securedSites, prices)` pur** : self-hosted = passerelle chiffrée × sites (primaire + réplicas, monotone) ; `alternative` managée chiffrée = tarif/u × users (**avis NON orienté, jamais imposée** — décision Amine) ; `securedSites ≤ 1` → `null`. **GARDE-FOU DÉFCON 1 (cœur de la story)** : seuls les postes **chiffrables vendeur** sont figés — VM passerelle **Hetzner CX22 3,79 €/mois**, WireGuard/IPsec + mTLS (PKI open-source step-ca) = **licence 0 €**, alternative managée **Tailscale Standard 8 $** / **Cloudflare Zero Trust 7 $ (free ≤ 50 users)** ; la **supervision + le durcissement** = OPEX/main-d'œuvre interne → **flaggés « à chiffrer en devis », JAMAIS un montant au doigt mouillé** (mémoire `jamais-graver-decision-perissable`). **Intégration** (`costResidency`) : poste ajouté **uniquement si `hostingClass='self-hosted'` ET multi-région** (souverain managé / hyperscaler ⇒ liaison sur backbone privé du fournisseur, pas de poste à part) → folded dans `residency.monthlyCost`/`setupCost`/`costSources` → remonte au total via `applyResidency` (C6) + `setupCost` recommend ; **invariant neutre préservé** (prix neutres → 0). `ResidencyPlan += interSiteSecurity?` (optionnel). `ResidencyCost += interSiteSecurity?`. **Exit Escrow** : `manifest.residency` sérialise le plan complet → `interSiteSecurity` ride automatiquement ; `residencySection` (runbook DR) gagne un bloc **« Sécurisation de la liaison inter-site »** dérivé (rendu si self-hosted multi-site, avec l'alternative managée présentée → donnée réellement consommée, pas morte). **Sources fraîches relevées 2026-05-29** (recherche web réelle, pages officielles ; réconciliation : la page Tailscale officielle confirme **Standard 8 $**, le snippet de recherche « 6 $ » écarté — DÉFCON 1) → `docs/pricing/residency-cost-sources.md` section S-061 réécrite. **Devise managé USD** → étage FX au runtime (pas de conversion € gravée). +8 tests (6 `residency` : hostingClass self-hosted hors zone UE, mono-site→null, **monotonie + alternative chiffrée + sources + note devis**, plan multi-région intégré au coût+sources, non-self-hosted→pas de poste, invariant neutre ; 2 `bundle` : runbook self-hosted documente la liaison + l'alternative, managé→pas de bloc) → **417 + 10 skip**. typecheck 0, lint 0/0, build OK (`/resultats` 13,7 kB). **Learnings** : (a) un poste mêlant infra chiffrable (VM) et OPEX non sourçable (main-d'œuvre) → ne figer QUE le vendeur, flagger l'OPEX « à chiffrer en devis » (jamais un € inventé) = la seule voie DÉFCON 1 ; (b) sécuriser n'a de sens qu'en **self-hosted** (le managé souverain porte la liaison sur son backbone) → conditionner le poste à `hostingClass`, pas à la seule multi-région ; (c) présenter l'**alternative managée chiffrée** (avis non orienté) la rend consommée (runbook) → pas de donnée morte. **DETTE TRANSPARENTE (non bloquante, séquençage moteur→UI comme backup S-028→S-030)** : le toggle `selfHosted` dans le wizard (`ResidencyBlock`) + la surface du détail inter-site/alternative dans `ResidencyPanel`/`CostMap` = increment UI futur ; moteur/coût/bundle complets et testés. **Reste backlog : S-062 (veille juridique live), épic i18n S-057→060.**
- 2026-05-29 — **S-048 ✅ — UI Bloc ② « Résidence & continuité » + Résultats (radar 10 + réplication + transferts) + e2e. CLÔT l'épic Résidence (S-043→S-048).** **Moteur** : `hostingClassForProfile(p)` pur (zone → vecteur d'egress sourcé : `ue`/`maroc` → `sovereign-eu`, `us`/`other` → `hyperscaler`), câblé dans `recommend` (`deriveResidencyPlan(…, hostingClassForProfile(profile))`) → **lève la dette** « S-048 le câblera au profil » du commentaire de `deriveResidencyPlan`. **UI Bloc ②** : `components/wizard/ResidencyBlock.tsx` (jumeau de `BackupBlock`) — `RadioCards` criticité DR (Aucune/Tiède/Chaude) + `InfoBubble` neutre + `<details>` affinage expert (région primaire `RadioCards`, **régions autorisées multi-continent** `CheckboxCards` = **finding C4**, résidence stricte `YesNo` via `deriveResidencyTopology().noTransfer`, actif-actif `YesNo`, RPO/RTO) + aperçu live (`deriveResidencyPlan` injecté : « ajoute ≈ X €/mois » + setup + topologie) + **alerte conflit** résidence × DR (`role="alert"` + leviers, JAMAIS résolu en douce) + disclaimer. Câblé dans `Wizard.tsx` (section après « Sauvegarde & résilience ») + **injection `getResidencyPrices()` dans `recommend`** → le **budget-mètre intègre la résidence** (totalCost). `options.ts` : `REGION_OPTIONS` + `DR_TIER_OPTIONS`. **Résultats** : `CostMap` gagne `residency?: ResidencyPlan` → **ligne réplication** (DR/actif-actif + N régions en attente + RTO + €/mois, pastille 🟢, sources cliquables, ligne setup « + amorçage des réplicas ») ; **`components/results/ResidencyPanel.tsx`** (NEUVE) = topologie régions + **table des transferts** (✅ Autorisé / ⚠️ Encadré / ⛔ Interdit + base légale datée + note volatile + Cloud Act) + RPO/RTO régionaux + alerte conflit + disclaimer (**rendu `null` si plan neutre** : pas de DR, pas de transfert, pas de conflit). `ResultsView` injecte `getResidencyPrices()` dans `recommend`/`buildEnsemble`/`narrationContext`, passe `residency={activeResult.residency}` à `CostMap`, rend `<ResidencyPanel plan={activeResult.residency} />`. **Radar 10 axes déjà visible** (S-045, `RadarChart` lit `data.length`). **Doc** : `README.md`/`PRD.md`/`PASSATION.md` « 9 dimensions » → « 10 » (DÉFCON 1, zéro trace obsolète). +9 tests (`hostingClassForProfile` ; 4 `ResidencyBlock` : DR none → pas d'expert, change DR → `{drTier}`, DR hot → expert + aperçu, conflit Maroc strict+hot → alerte+leviers ; 3 `ResidencyPanel` : neutre → `null`, transfert UE→US Encadré + base légale, conflit ; 1 `results` : backup critique → panneau Résidence visible) → **409 + 10 skip**. **e2e** « profil régulé multi-région → réplication + transfert UE→US Encadré (RGPD chap. V) + radar 10 axes » (DR Chaude + région US ajoutée via l'expert) → **desktop + mobile, 25 + 1 skip**. typecheck 0, lint 0/0, build OK (`/resultats` statique 13,7 kB · `/configurateur` 12,2 kB). **ÉPIC RÉSIDENCE COMPLET (S-043→S-048).** Learnings : (a) `getByText` Playwright fait du **substring insensible à la casse** → collisions (« Affinage expert » ⊂ « …l'affinage expert reste possible » du backup ; « Résidence & transferts » ⊂ le label de score `geosov`) → cibler `{exact:true}` ou `getByRole("heading")` ; (b) avec `hostingClass=sovereign-eu` le 1ᵉʳ vecteur seed est OVH (egress 0 €) → la réplication egress = 0 mais le **compute des régions en attente** porte le coût (la ligne réplication s'affiche quand même) ; (c) `ResidencyPanel` rendu `null` sur plan neutre = pas de bruit pour les profils sans DR (cohérent `CostMap` qui masque la ligne si `monthlyCost = 0`) ; (d) la résidence stricte (`noTransfer`) rend les transferts intra-juridiction → **table transferts vide** : pour exercer un flux conforme il faut une région distincte autorisée + `noTransfer` faux (rgpd seul). **Reste backlog : S-061 (sécu inter-site self-hosted), S-062 (veille juridique live), épic i18n S-057→060.**
- 2026-05-29 — **S-047 ✅ — Exit Escrow DR : runbook de bascule régionale + IaC multi-région (spec n°2 §8).** `buildExitBundle` lit `recommendation.residency`. **`BundleManifest.residency`** (ResidencyPlan) ajouté → source de vérité (cohérence manifest↔plan, comme `backup`). **`runbook.md`** gagne `residencySection` DÉRIVÉE du plan réel : région primaire, RPO/RTO **régionaux**, **ordre de priorité des régions** (topologie), coût récurrent + mise en route, **table de conformité des transferts** (✅ ok / ⚠️ restreint / ⛔ interdit + base légale + disclaimer « ingénierie, PAS un avis juridique »), bloc **conflit + leviers** si `hasConflict`, **procédure de bascule régionale en 5 étapes** (RTO cible, promotion réplica/retrait pool actif-actif, repointage DNS, vérif réplication, réintégration). **`terraform/dr.tf`** (multi-région : `primary_region`/`replica_regions` + réplication actif-actif/actif-passif) généré **seulement si des réplicas existent** (cohérence manifest↔plan ; profil sans DR → pas de `dr.tf` → `EXPECTED_FILES` inchangé, zéro régression). README maj (dr.tf + DR dans le runbook). +5 tests (manifest↔plan ; `dr.tf` + procédure bascule ; sans-DR → pas de `dr.tf` ; transfert inter-région listé + disclaimer ; conflit Maroc strict + leviers) → **400 + 10 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) générer `dr.tf` **conditionnellement** (réplicas présents) préserve le jeu de fichiers attendu pour les profils sans DR → aucun test existant cassé ; (b) le runbook reste 100 % dérivé du plan (zéro texte en dur), comme la section backup S-029 → cohérence manifest↔runbook garantie ; (c) la table des transferts dans le runbook réutilise les `TransferFlag` (statut + base légale datée + disclaimer) produits en S-044. Reste epic Résidence : **S-048** (UI Bloc ② Résidence & continuité + zone multi-région finding C4 + Résultats : ligne réplication + transferts + radar 10 visible + e2e + nettoyage) — la dernière, surface UI.
- 2026-05-29 — **S-046 ✅ — Intégration résidence/DR dans `recommend` (+ ensemble) + REFACTO contrat (zéro-dette).** `recommend(…, residencyPrices = NEUTRAL_RESIDENCY_PRICES)` calcule `deriveResidencyPlan(profile, residencyPrices, computePrices, preset)` → **`Recommendation.residency` (champ REQUIS) rempli**. **`cost.ts` `applyResidency`** : coût récurrent résidence/DR (réplication egress + régions en attente) injecté **DANS C6** (ordre `applyCompute`→`applyMultimodalSizing`→`applyBackup`→`applyResidency`) ; `monthlyCost ≤ 0` → couches inchangées (**invariant `totalCost = baseCost + moduleCost` préservé**). `setupCost += residency.setupCost`. **`computeScores` reçoit le plan réel** → `geosov` calculé sur la vraie résidence + `resilience` étendu DR. `costSources += residency.costSources`. **`ensemble.ts`** threade `residencyPrices` (baseline + 3 variants) → le spread reflète tous les postes. **REFACTO ZÉRO-DETTE (purineté moteur)** : le **CONTRAT** de prix résidence (`ResidencyPriceTable`/`EgressVector`/`HostingClass`/`NEUTRAL_RESIDENCY_PRICES`/`selectEgressVector`/`vectorsForClass`) — placé par erreur dans `lib/pricing` en S-043 — est **déplacé dans `lib/engine/residency`** (le moteur ne doit JAMAIS importer `lib/pricing`, comme `BackupPriceTable` vit dans `backup.ts`) ; `residency-seed.ts` importe le contrat du moteur + le re-exporte, et garde le SEED (`RESIDENCY_PRICE_SEED`) + le garde-fou live (`reconcileEgressVector`, qui utilise `reconcilePrice`). +6 tests `recommend-residency` (champ rempli ; coût injecté dans C6 + totalCost↑ ; invariant neutre ; 10ᵉ dim geosov ; sources DÉFCON 1 ; ensemble baseline.residency rempli) → **395 + 10 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) `Recommendation.residency` requis OK car `recommend` est le **seul constructeur** (typecheck 0 sans toucher les appelants) ; (b) le contrat de prix doit vivre **dans le moteur** (pur, injecté), le seed dans `lib/pricing` — corrigé à la racine, pas de rustine ; (c) invariant préservé par construction (prix neutres → `applyResidency` no-op). Reste epic Résidence : S-047 (Exit Escrow DR), S-048 (UI Bloc ② + radar 10 + transferts + e2e).
- 2026-05-29 — **S-045 ✅ — Scoring 9→10 : `geosov` (neuf) + `resilience` étendu DR + `sov` recadré (refonte, méthode S-027 8→9).** **types.ts** : `SCORE_KEYS` += `geosov` (10ᵉ, ordre stable, geosov en dernier). **scores.ts** : `computeScores` gagne un param **`residencyPlan` injecté** (défaut `deriveResidencyPlan(p)`, score price-indépendant — recommend passera le vrai plan en S-046) ; nouvelle dimension **`geosov`** = `ResidencyPlan.geoSovScore` (label « Souveraineté géographique (résidence & transferts conformes) », why = région + nb transferts + ⚠ conflit) ; **`resilience` étendue DR** (bonus actif-actif +1.5 / hot +1 / warm +0.5, why cite le RTO régional) ; **`sov` RECADRÉ** (retrait du bonus géographique zone UE/Maroc — désormais capté par `geosov` ; label « …(portabilité) », why = vault + Exit Escrow). **RadarChart inchangé** (lit `data.length` → « Radar des 10 dimensions » auto). Libellés « 9 »→« 10 » : `ResultsView` (SHORT_LABELS +`geosov` « Géo-souv. » + titre « Profil sur 10 dimensions »), `export/model.ts` + `export.test`, `app/resultats` metadata, `e2e/strate` (radar 10 axes). Tests `toHaveLength(9)→10` (engine ×2, recommend-sizing, profile-mappers). **Piège résolu** : le test « resilience croît avec la criticité » cassait (`expected 10 to be less than 10`) car le bonus DR — dérivé par défaut de la criticité backup — poussait `high` et `critical` au plafond 10 → collision. Fix : le helper `resilienceOf` **isole l'axe backup** (`residency: drTier none` par défaut) ; +1 test dédié « le DR régional renforce la résilience » (standard : none 5 < hot 6). +1 test net → **389 + 10 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) une dimension de score qui combine 2 axes (backup + DR) sature le plafond → **isoler chaque axe dans son test** (DR neutre pour tester la criticité, et inversement) ; (b) `computeScores` reste pur en injectant `residencyPlan` (défaut dérivé) → zéro changement des appelants existants (recommend câblera le vrai plan en S-046) ; (c) déplacer le géographique de `sov` vers `geosov` évite le double-comptage de la souveraineté. Reste epic Résidence : S-046 (intégration recommend + `Recommendation.residency`), S-047 (Exit Escrow DR), S-048 (UI).
- 2026-05-29 — **S-044 ✅ — Moteur résidence/DR pur (deriveResidencyPlan + costResidency + conflit), spec n°2 §3-§7.** Consomme la fondation S-043. **types.ts** : `Region` (= `TransferRegion` lib/legal, alias canonique), `DrTier`, `Residency` (+ `Profile.residency?`), `RegionRole`, `RegionAssignment`, `TransferFlag` (réutilise `TransferStatus` lib/legal), `ResidencyPlan` — **PAS** ajouté à `Recommendation` (c'est S-046, build vert préservé, même méthode que backup S-026→S-028). **`lib/engine/residency.ts`** (pur, déterministe, prix INJECTÉS, défaut neutre) : `deriveResidencyTopology` (primaryRegion via `zoneToRegion` ue→eu ; `noTransfer` dérivé régulé/secret ; `drTier` dérivé criticité backup, relevé d'un cran si latence fast & >50 users ; secondaryRegion **intra-juridiction sous résidence stricte** ; topologie none/warm/hot/actif-actif) ; `costResidency` (régions en attente via **`computeSovereignCompute`** S-041, fractions warm 0.3 / hot 1.0 / actif-actif 1.0 ; réplication egress via `selectEgressVector` S-043 × volume × churn 10 %, **bidirectionnelle ×2 en actif-actif** → garantit hot<actif-actif ; `setupCost` = 1ᵉʳ transfert complet ; **none + mono-région → 0**, invariant `totalCost`) ; `buildTransfers` via `lookupTransferBasis` (lib/legal) → `TransferFlag` avec note **datée + volatile + disclaimer + flag Cloud Act** ; `buildConflict` (résidence stricte × DR dans une juridiction **mono-région** maroc/other → conflit + 3 leviers, JAMAIS résolu en douce — décision Amine #6) ; `geoSovScore` 0-10 (alimente la 10ᵉ dim, câblée S-045). VOLUME_TO_GB aligné sur backup (±30 %). +11 tests (dérivation/mapping/noTransfer/drTier ; transferts UE→US restricted & intra-juridiction sans flux ; conflit Maroc vs UE multi-région ; **monotonie none<warm<hot<actif-actif** ; invariant neutre ; sources egress+compute ; geoSov borné [0,10] & stricte>restreint) → **388 + 10 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) réutiliser `computeSovereignCompute` pour les régions repli (pas un forfait) = cohérent S-041, monotonie naturelle ; (b) l'actif-actif strictement > hot via **réplication bidirectionnelle** (×2), pas via une fraction compute >1 (honnête : 2 sites en charge synchronisés) ; (c) conflit résidence×DR = on **expose** (mono-région juridiction + leviers), le moteur n'arbitre jamais ; (d) `Region` aliasé sur `TransferRegion` (lib/legal) → zéro duplication d'union, le juridique reste la source canonique. Reste epic Résidence : S-045 (scoring 9→10 geosov), S-046 (intégration recommend), S-047 (Exit Escrow DR), S-048 (UI).
- 2026-05-29 — **S-063 ✅ — Purge des codenames perso du catalogue/diagnostics/scores (remontée Amine).** Amine, en revoyant la stack proposée : « Frontmatter universel Xavier v1.0.0 — personne à part moi ne connaît Xavier ». Audit : codenames PERSO gravés en dur dans des chaînes **user-facing** — `catalog-seed.ts` (C0 « Frontmatter universel Xavier v1.0.0 » ; C5 « option Amine »/« option Meydeey »/« option C Chris »/« (Amine) »/« (option Chris) » ; C6 « (Meydeey) »), `diagnostics.ts` kmChecks (« (Xavier v1.0.0) », « (Chris/Xavier) », « (doctrine Amine) », « (MasterNote pattern Amine) »), `scores.ts` label radar « stress » (« (méthode Meydeey) »). **PURGÉ** en gardant la tech RÉELLE reconnaissable (Apache AGE, Graphiti, XTDB, Postgres+pgvector…) : C0 → « Contrat de métadonnées (frontmatter YAML + JSON Schema) » ; attributions perso retirées ; « MasterNote pattern Amine » → « pattern note-maîtresse » ; label radar → « Stress-testabilité empirique ». **Round-trip `buildLayers` préservé** (caractérisation S-035 : layers[0]=C0 non asservi, seul `catalog.test.ts:70` mis à jour vers « Postgres + Apache AGE (graphe, ADR-011) OU Graphiti+Neo4j »). **Test de garde** `lib/engine/__tests__/no-personal-codenames.test.ts` (2) : parcourt `seedCatalog` (3 presets × 3 profils : multimodal/bitemporel/secret) + `computeScores` (label+why) + `computeKMChecks`/`computeRisks`/`computeCompliance` → regex `\bXavier\b|\bChris\b|\bMeydeey\b|\bAmine\b` jamais trouvée ; vérifie aussi que C0 est générique. **Grep final** : restes uniquement en COMMENTAIRES de code (« décision Amine ») + 1 email de fixture test — pas user-facing, légitimes. Le seed reste calibration interne datée mais NEUTRE ; les vrais composants sourcés viennent de la veille live (S-036). +2 tests → **377 + 10 skip**. typecheck 0, lint 0/0, build OK. Learning : un seed « transposé À L'IDENTIQUE » d'un design interne (S-035) peut **importer des codenames privés** dans les recos client → garde anti-codename systématique sur les chaînes user-facing (cf. doctrine composant RECONNAISSABLE + sourcé). Reste epic Résidence : S-044→048 ; + S-061 (sécu inter-site), S-062 (veille juridique live).
- 2026-05-29 — **S-043 ✅ — Fondation epic Résidence/DR : pricing egress inter-région + bases légales de transfert (sourcés), recadré par 2 retours Amine.** **Retour 1 (multi-segment)** : « tu es trop rigide, tu veux un référent alors que je ne construis pas pour un seul segment » → abandon du pattern backup « 1 référent + repères ». **`lib/pricing/residency-seed.ts`** : `EgressVector` (provider/hostingClass/sovereign/interRegionEgress) + `ResidencyPriceTable` (egressVectors[] + interSiteSecurityPerMonth) couvrant le **spectre first-class** : self-hosted (Hetzner, 20 To inclus → 0,001 €/Go overage), souverains UE (OVH 0 €, Scaleway 0,01 €/Go, IONOS 0,030 €/Go), secnumcloud (Outscale 0 €), hyperscalers repères (GCP 0,05 $/Gio, AWS 0,02 $/Go), **devises natives**, chaque vecteur sourcé (URL+date+confiance). **Retour 2 (le plus important — doctrine)** : « tu fetch le web pour poser des décisions qui demain peuvent être invalidées » → **doctrine « tout vivant + filet » étendue au pricing egress ET au JURIDIQUE**. Le seed est un **REPLI DATÉ + baseline**, jamais une décision gravée : `reconcileEgressVector` (garde-fou ±60 %, réutilise `reconcile.ts`) ; Scaleway egress déjà live via `live-feed.ts` ; preuve de volatilité au sourcing (OVH egress devenu gratuit janv. 2026, Scaleway révise, DPF sous pourvoi, Maroc sans adéquation). **`lib/legal/transfers.ts`** : `lookupTransferBasis(from,to,ctx)` **pur + total** → `TransferBasis` **DATÉ + RÉVISABLE** (jamais de verdict sans `checkedAt` + `volatile` + `disclaimer` « ingénierie, PAS un avis juridique ») ; flag `volatile` sur **DPF UE-US** (sous pourvoi CJUE C-703/25 P) et **adéquation Maroc** (absente) ; flag `cloudAct` si données régulées → US ; `noTransfer` → `forbidden`. `TransferStatus`/`TransferRegion` **canoniques ici** (S-044 importera). **Self-hosted = surface à sécuriser** (décision « les 3 ») : `INTER_SITE_SECURITY_SEED` flague + fourchette (WireGuard 0 € GPLv2 → managé Tailscale/Cloudflare 7-18 $/u/mois), chiffrage détaillé = **S-061**. Docs : `docs/pricing/residency-cost-sources.md` + `docs/legal/transfer-bases.md` (audit trail, sources officielles, **NON CONFIRMÉ assumés** : Azure, intra-EU GCP, Infomaniak overage — jamais comblés). **Veille live des statuts juridiques = S-062** (LLM + Firecrawl, registre prompts S-053). +19 tests (10 seed : sources datées, spectre multi-segment, garde-fou live/flagged/fallback ; 9 légal : statuts, volatile, cloudAct, disclaimer+date sur TOUTES les combos, totalité) → **375 + 10 skip**. typecheck 0, lint 0/0, build OK. **3 stories tracées** : S-061 (sécu inter-site), S-062 (veille juridique live), **S-063 (purge codenames perso « Xavier v1.0.0 »/« Chris » du catalog-seed/diagnostics — remontée Amine, à faire ensuite)**. Learnings : (a) pour un produit multi-segment, modéliser le **spectre des profils en first-class sourcé**, pas un référent élu + figurants ; (b) une donnée **périssable** (prix OU statut juridique) ne se grave JAMAIS comme vérité permanente → état daté + révisable + veille live + repli + garde-fou (cf. mémoire `jamais-graver-decision-perissable`) ; (c) le juridique porte un flag `volatile` + disclaimer systématiques, jamais un statut « définitif ». Reste epic Résidence : S-044→048 (S-044 consomme `residency-seed` + `lib/legal/transfers`).
- 2026-05-28 — **ÉPIC i18n / MULTILINGUE OUVERT (scope, S-057→S-060) — décision Amine.** Constat franc : l'app était **FR-only par convention** (CLAUDE.md/AGENTS.md « UI et contenu en français ») — **aucun i18n n'a jamais été spécifié ni construit** (ni sélecteur de langue, ni clés i18n). Amine ouvre le multilingue (+ remonte que les prompts sont « ridicules », à soigner comme un natif). Choix « épic i18n complet » (vs prompts-seuls). **Scope committé dans le PRD** : **S-057** fondation next-intl (locale cookie `NEXT_LOCALE` SANS préfixe URL + détection Accept-Language ; LocaleSwitcher ; messages fr/en ; migration accueil/layout en preuve ; ne casse pas les routes/e2e) → **S-058** extraction EXHAUSTIVE des clés UI + garde-fou « zéro chaîne en dur » (ESLint/scan CI) → **S-059** prompts réécrits niveau NATIF (gardant placeholders + clauses DÉFCON 1) + LOCALISÉS (locale passée aux routes LLM → réponses user-facing dans la langue de l'utilisateur ; livrable/PDF localisés) → **S-060** arabe + RTL (logical properties, dir=rtl, police arabe ; marché Maroc). **Recommandation : BUILD en session fraîche dédiée** (refonte UI transverse + qualité DÉFCON 1 des traductions). Décisions techniques figées : next-intl (Next 15 RSC), cookie-locale sans préfixe (disruption minimale), fr défaut + en, ar/RTL en dernier. La demande « prompts soignés » est intégrée à S-059 (pas de polish FR jetable séparé puisque l'épic complet a été choisi).
- 2026-05-28 — **S-040 ✅ — Assistant Q&A contextuel (5ᵉ usage LLM ; la story la plus sensible DÉFCON 1).** Chat sur /resultats qui répond à partir de la recommandation AFFICHÉE + recherches web sourcées. **Cœur pur `lib/llm/assistant.ts`** : `serializeRecoFacts(reco)` = bloc FAITS autoritatifs (preset, coût total, setup, bandes verdict, score moyen, couches+coûts) — **exactement les chiffres à l'écran** ; `buildChatMessages(question, history, recoFacts, webResults, template?)` greffe FAITS + résultats web (placeholders) dans le prompt système (éditable, défaut = registre), threade l'historique, question en dernier. **Prompt `assistant` ajouté au registre (S-053)** → éditable dans /admin (cohérence : aucun prompt en dur). **Route `/api/llm/chat` étoffée** : `{ question, history?, recoFacts? }` → `searchWeb` Firecrawl (repli [] si clé absente) + `callLLM` → `{ ok, answer, sources }` ; **LLM KO → repli `{ ok:false, reason }` (200, jamais d'exception)** ; bornage question/historique/faits. **`AssistantPanel`** (client) branché dans la vue experte de `ResultsView` sur `activeResult` : envoie `serializeRecoFacts(reco)`, affiche les bulles + **sources cliquables** + disclaimer ; repli gracieux. **GARANTIE DÉFCON 1 par CONSTRUCTION** : seuls les montants des FAITS sont autorisés (injectés), toute info hors reco s'appuie sur le web fourni (URL citée, jamais inventée), disclaimer IA. **LIMITE assumée (honnêteté brutale)** : un chat en texte libre ne peut PAS être prouvé « zéro chiffre inventé » mathématiquement → garanti par le contexte (faits autoritatifs + interdiction d'inventer + sources + disclaimer), documenté comme tel. Tests : +4 assistant (serializeRecoFacts expose les chiffres + couches ; buildChatMessages injecte FAITS/web/règles + historique + override ; sans web → consigne FAITS-only), +3 route (400 sans question ; OK→answer+sources+FAITS dans le prompt ; KO→repli 200) → **356 + 10 skip** ; e2e +1 (question → réponse/repli sans crash). typecheck 0, lint 0/0, build OK. Learnings : (a) envoyer les FAITS = les chiffres AFFICHÉS (pas un recalcul serveur avec d'autres prix) garantit la cohérence assistant↔écran ; (b) la garantie « zéro chiffre inventé » vit dans la CONSTRUCTION du contexte + le disclaimer, pas dans une preuve sur la prose (limite honnête) ; (c) réutiliser le store de prompts (S-053) pour l'assistant = un seul mécanisme d'édition, zéro prompt en dur résiduel. **ÉPIC LLM COMPLET** (S-034→040, 052, 053). Reste : épic Résidence/DR S-043→048.
- 2026-05-28 — **S-053 ✅ (code) — Console admin super-admin : prompts système versionnés, sortis du code dur.** DÉCISION Amine : **option C (tout éditable, responsabilité humaine)** — le super-admin réécrit librement le gabarit, AUCUN garde-fou de contenu sur le prompt. Sûr car les **validateurs serveur déterministes** (parseIntakeProfile / parseNarration / reconcileCatalog) bornent TOUJOURS la sortie : un prompt dégradé baisse la qualité, jamais la correction. **Migration `20260528120000_admin_prompts.sql`** : `super_admins(user_id)` (rôle GLOBAL hors cercle) + helper `is_super_admin()` SECURITY DEFINER ; `prompts(prompt_key, version, content, is_active, author, …)` + index unique partiel (1 active/clé) ; RLS (select/insert/update super-admin only ; l'app publique lit en service-role). **`lib/prompts/registry.ts`** (pur) : `PROMPT_KEYS` = intake/narration/catalog-veille (clés à consommateur RÉEL aujourd'hui ; livrable + agent-déploiement = Lot 2, non enregistrés pour éviter une config morte cf. S-049) ; `DEFAULT_PROMPTS` = gabarits par défaut à **placeholders** `{{…}}` ; `composePrompt(template, vars)` remplace les placeholders CONNUS (inconnu laissé tel quel → pas d'injection ; le code seul fournit `vars`). **`lib/prompts/store.ts`** (serveur) : `loadActivePrompt` (service-role, repli null), `listPromptVersions`, `activateNewVersion` (désactive l'ancienne puis insère, historique) — repli partout, aucune exception. **`lib/supabase/admin.ts`** : client service-role (repli null si clé absente). **Builders refactorés** en gabarit + composePrompt : `buildIntakeMessages(text, base?, template?)`, `buildNarrateMessages(ctx, template?)`, `proposeForSlotLive` via `deps.systemPrompt` — défaut = registre (les valeurs dynamiques : énumérations, profil, notes, textes de base, restent greffées par le CODE via placeholders). **Routes** intake/narrate/catalog-live chargent le prompt actif (repli défaut). **Console `/admin`** : page serveur (garde super-admin via session + `super_admins`), `AdminPrompts` (login Supabase email/mdp + 403 si non super-admin + éditeur : sélecteur de clé, version active, placeholders, activation de version) ; `/api/admin/prompts` GET/POST (RLS-enforced via session, audit = versions). **Fix typage** : `TableShape` gagne `Relationships: []` (contrat `GenericTable` de @supabase/supabase-js v2 — sinon requêtes typées `<Database>` en `never` ; bénéficie à toutes les tables). Tests : +9 registry (composePrompt connu/inconnu/passe-unique, clauses DÉFCON dans les défauts, isPromptKey, override builders), +3 store (repli client null), tests intake/narrate existants verts (gabarit défaut inchangé) → **349 + 10 skip**. typecheck 0, lint 0/0, build OK. **ACTIVATION PROD à finaliser AVEC Amine** : appliquer la migration au Supabase self-hosté + seed `super_admins` + `SUPABASE_SERVICE_ROLE_KEY` dans Coolify ; sans ça → dégradation gracieuse (gabarits par défaut). Learnings : (a) option C est sûre PARCE QUE les garanties vivent dans les validateurs serveur (déterministes), pas dans le prompt → l'admin ne peut pas casser DÉFCON 1 ; (b) gabarit à placeholders = l'admin édite la prose, le code greffe les données critiques (énumérations) → séparation des rôles ; (c) `Relationships: []` requis par supabase-js v2 pour le typage des requêtes (dette latente révélée par le 1ᵉʳ select typé) ; (d) repli `loadActivePrompt`→null→défaut = le store peut être vide/absent sans casser l'app. Reste : S-040 (assistant), épic résidence S-043→048.
- 2026-05-28 — **S-056 ✅ — Dette de supervision résorbée : paramètres de dimensionnement VIVANTS + sourcés (option A, décision Amine).** Constat Amine : le calcul reste déterministe (bon pour repro/fiduciaire/DÉFCON 1) mais les HEURISTIQUES (charge GPU/modèle, empreinte stockage, seuils) étaient figées → un composant 2× plus efficient n'était pas reflété dans le chiffrage. **Doctrine « tout vivant + filet » étendue aux PARAMÈTRES.** Nouveau **`lib/engine/sizing-params.ts`** : type `SizingParams` (tierQuantity/storageGbPerUnit/gpuLoadPerUnit/gpuTierThresholds) + **`BASELINE_SIZING_PARAMS`** (valeurs historiques de sizing.ts, datées = défaut + repli) ; **`SIZING_FACTOR_BOUNDS` [0.25, 4]** + `isValidFactor` (garde-fou : aberrant → repli) ; **`deriveSizingParams(catalog?, baseline?)`** lit le candidat C6 (`gpuLoadFactor`) et C5 (`storageFactor`), consomme le facteur s'il EXISTE + passe les bornes + ≠ 1 (sinon repli baseline), renvoie `{params, applied[]}` (applied = facteur + nom + source, traçabilité). **`ComponentCandidate.sizingParams?`** (catalog/types) ajouté. **`sizing.ts`** : constantes figées retirées → `costMultimodalSizing(profile, prices, params=BASELINE)` consomme les params injectés (défaut baseline → **tous les tests sizing existants verts sans modif**). **`reconcileCatalog`** assainit les facteurs d'un candidat live (aberrant → retiré → repli baseline). **`recommend`** : `deriveSizingParams(catalog)` → `costMultimodalSizing(…, params)` + sources appliquées ajoutées à `costSources` (DÉFCON 1). **Snapshot reproductible** : les facteurs voyagent DANS le candidat, déjà gelé dans `catalog.json`/`manifest.catalog` (S-037) → bundle rejouable, zéro nouveau code de gel. Tests : +6 sizing-params (isValidFactor, dérivation baseline/valide/aberrant/neutre, effet palier GPU `gpuLoadFactor 0.5` → dedicated-large→dedicated-small + coût inférieur, storageFactor, déterminisme), +2 reconcile (facteur valide conservé / aberrant retiré) → **337 + 10 skip**. typecheck 0, lint 0/0, build OK. **Pas d'effet prod tant que la veille (live-catalog LLM) n'émet pas de facteur** → baseline (capacité latente, pipe + garde-fou en place ; l'émission de facteurs sourcés par le LLM = extension future, bornée par le garde-fou). Le LLM ne calcule JAMAIS ; le moteur reste pur sur paramètres révisables. Learnings : (a) extraire les heuristiques en `SizingParams` avec défaut baseline = zéro régression (tous les tests passent inchangés) ; (b) attacher les facteurs au ComponentCandidate fait que le **snapshot S-037 les gèle gratuitement** (rejouable) ; (c) garde-fou de bornes (`isValidFactor`) à 2 niveaux — reconcile (assainit le live) ET deriveSizingParams (ignore l'aberrant) = double filet DÉFCON 1 ; (d) le facteur module la charge GPU → bascule de palier → le gain d'un composant efficient EST reflété dans le coût (la dette d'Amine est levée à la racine). Reste : S-053 (admin), S-040 (assistant), épic résidence S-043→048.
- 2026-05-28 — **S-052 ✅ — Champ texte libre par bloc RÉELLEMENT intégré (4ᵉ usage LLM ; lève la dette « freeNotes stocké jamais lu »).** Demande Amine (verrouillée) : « le texte libre partout doit être INTÉGRÉ ». **Moteur pur `lib/llm/intake.ts`** : (a) `buildIntakeMessages(text, base?)` — si `base` fourni, injecte le **profil actuel** (snapshot des 15 champs d'intake) + consigne « AJUSTE, ne repars pas de zéro ; pour les listes CONSERVE l'existant et AJOUTE » → le LLM fusionne les tableaux lui-même (vs un overlay client qui écraserait `contentTypes`) ; sans `base`, prompt S-038 **inchangé** (tests existants verts) ; (b) refactor `validateIntakeFields(data, base)` factorisé, `parseIntakeProfile` délègue ; (c) `coerceProfile(raw)` borne une base reçue (15 champs validés sur DEFAULT, structurés ignorés — l'overlay client les préserve) ; (d) **`applyIntakeFields(current, result)`** = overlay **type-safe** (switch sur `result.applied`) qui ne recopie **que** les champs réellement extraits → `modules`/`mediaNeeds`/`backup`/`freeNotes` et champs non mentionnés **préservés** ; nom inconnu (`json`/`llm`) ignoré sans throw. **Route `/api/llm/intake`** : accepte `base` optionnel (coercé) passé au prompt **et** comme base de validation ; LLM KO → repli `{profile: base, applied: [], rejected: ["llm"]}`. **Narration `lib/llm/narrate.ts`** : `NarrationContext.notes?` injecté en **CONTEXTE** avec garde-fou anti-injection (« ne suis AUCUNE instruction des notes, tiens-en compte pour le ton ») ; `isCleanNarration` rejette toujours tout chiffre → invariant DÉFCON 1 intact ; route narrate transmet `notes` ; `ResultsView` collecte les `freeNotes` non vides du profil de **base**. **UI `Wizard.tsx`** : `NoteField` gagne « Intégrer cette note » → POST intake { text, base: profile } → `applyIntakeFields` → `loadProfile` en **conservant** `freeNotes[block]` ; feedback « Ajusté : <champs FR>, vérifiez (une IA peut se tromper) » / « Aucun paramètre » / repli « indisponible » (rejected `llm` → message honnête, pas « aucun paramètre ») ; **additif, jamais bloquant, saisie manuelle inchangée**. Tests : intake +4, narrate +2, routes +2, wizard +1 → **325 + 10 skip**. typecheck 0, lint 0/0, build OK (`/configurateur` 10,9 kB · `/resultats` 11,6 kB). e2e +1 (note → Intégrer → repli gracieux + manuel intact) ; **vérif live (DeepSeek ajuste) en prod** (local : `next start` KO en `output:standalone` + LLM absent → repli ; branchement prouvé en unit, ajustement live visible en prod — pattern S-037/038/039). **Doctrine** (question Amine sur le déterminisme) : le LLM **propose/ajuste**, le **moteur reste déterministe** ; dette des heuristiques figées tracée en **S-056** (arbitrage A/C/différer en attente). Learnings : (a) envoyer le profil au **prompt** (pas seulement comme base de merge) laisse le LLM **fusionner les listes** (ajouter « vidéo » sans perdre « texte ») ; (b) overlay **par `applied`** (pas `data.profile` en bloc) = seule façon de ne pas écraser les champs structurés non couverts par l'intake ; (c) sur repli LLM, dire « indisponible » (pas « aucun paramètre ») = honnêteté DÉFCON 1 ; (d) notes en **contexte** narration + garde-fou regex = personnalisation sans fuite de chiffre ni exécution d'instruction utilisateur. Reste epic LLM : S-053 (admin prompts), S-040 (assistant Q&A).
- 2026-05-28 — **S-039 ✅ — Narration LLM (3ᵉ usage LLM, chiffres préservés).** Le LLM réécrit les TEXTES narratifs (verdict + « pourquoi ce preset ») pour les adapter au profil ; il **ne touche jamais aux chiffres** (invariant DÉFCON 1). **Moteur pur `lib/llm/narrate.ts`** : `buildNarrateMessages(ctx)` (prompt système qui INTERDIT tout chiffre/montant/score « affichés ailleurs » + interdit d'inventer un gain chiffré « seul −risques prouvé » + injecte les textes de base) ; `parseNarration(content, base)` → par champ, garde le texte réécrit s'il est **non vide ET propre** (`isCleanNarration` : rejette `€|euros?|/10|%|$`), sinon **repli sur le texte de base** ; `mergeVerdictNarration(verdict, texts)` ne réécrit QUE pain/risk/gain/nextStep → **bandes de coût/prix ferme INTACTS** (double garde-fou : structurel + regex). **Route `app/api/llm/narrate`** (clé serveur only) : callLLM → parseNarration ; **LLM KO → repli textes de base**. **UI `ResultsView`** : `narrationContext` mémoïsé sur le profil de **BASE** (pas la projection → pas de re-narration au drag du slider) ; effet fetch → `setNarration` ; mode verdict applique `mergeVerdictNarration` (+ chip « Texte adapté par IA, chiffres inchangés » dans `VerdictView`) ; mode expert applique `narration.presetReason` (seulement si `activeVariant===null` → cohérent avec S-055). **Branchement progressif** : rendu statique immédiat → narration quand elle arrive → repli silencieux si KO. Tests : narrate 5 (isClean, parse rejette montant+vide, JSON illisible→base, **invariant bandes préservées même narration adverse**, prompt), route 3 (400 sans base, OK→montant rejeté, KO→base), `results.test` reverte (narration jsdom KO → repli statique) → **315 + 10 skip**. **e2e desktop 10/10**. typecheck 0, lint 0/0, build OK. **Périmètre honnête** : verdict + « pourquoi preset » narrés ; les **InfoBubbles du wizard** réutilisent le même moteur mais ne sont pas encore branchées (surface large, faible valeur) — différé, noté dans le PRD. Learnings : (a) garantir « chiffres préservés » par **construction** (le merge ne touche que 4 textes) PLUS un garde-fou regex (rejet de tout `€`/`/10`) = double filet DÉFCON 1 ; (b) clé de narration = profil de **base** mémoïsé → évite de spammer le LLM à chaque tick de slider ; (c) ne narrer que la baseline (pas les scénarios actifs) reste cohérent avec la bascule S-055. Reste epic LLM : S-040 (assistant Q&A), S-052 (texte libre par bloc), S-053 (admin prompts).
- 2026-05-28 — **S-038 ✅ — Intake libre → Profile (2ᵉ usage LLM, epic reco-vivante).** L'utilisateur décrit son besoin en langage naturel (ou voix) → DeepSeek **extrait** des paramètres → on les **valide/borne** → configurateur pré-rempli. **Cœur pur `lib/llm/intake.ts`** : `buildIntakeMessages(text)` (prompt système qui **énumère strictement** les valeurs autorisées de chaque champ + interdit d'inventer « OMETS si absent/ambigu » + rappelle « tu ne calcules AUCUN coût ») + `parseIntakeProfile(content, base=DEFAULT_PROFILE)` → `{profile, applied, rejected}` : extrait le 1ᵉʳ objet JSON (tolère ```json fences/texte autour), **valide chaque champ contre les unions du moteur** (`pickEnum`/`pickEnumArray`/`pickUsers` borné [1,100000]/`pickBool`), valeur **hors-bornes/inconnue → REJETÉE, défaut prudent conservé** (DÉFCON 1 : le LLM propose, le serveur valide ; jamais sous-dimensionné). Sets `as const satisfies readonly X[]` = synchronisés avec `types.ts` au compile. **Route `app/api/llm/intake`** (Node, clé LITELLM serveur only) : `callLLM(buildIntakeMessages)` → `parseIntakeProfile` ; **LLM KO → repli profil par défaut + `rejected:["llm"]`** (saisie manuelle toujours dispo). **`components/intake/IntakeField`** : textarea « décrivez votre besoin » + **dictée Web Speech API si dispo** (typage `declare global` minimal, sans `as`/`any`) → POST → **persiste le Profile dans `localStorage` (STORAGE_KEY)** → `router.push("/configurateur")` (nav inter-route = réhydratation propre du wizard). Monté sur l'**accueil** sous `QuickProfileForm` (séparateur « ou en langage naturel »). Tests : intake 7 (extraction, rejet hors-bornes, fences, filtrage tableau, bornage users, JSON illisible→défaut), route 3 (400 sans texte, LLM OK→validé, LLM KO→repli ; `callLLM` mocké `vi.hoisted` → déterministe hors env), IntakeField 3 (désactivé si vide, succès→persist+push, erreur→repli sans nav) → **307 + 10 skip**. **e2e +1** (intake → /configurateur pré-rempli, repli garanti) → **e2e desktop 10/10**. typecheck 0, lint 0/0, build OK (`/api/llm/intake` ƒ, `/resultats` statique). Learnings : (a) `as const satisfies readonly Union[]` = liste de validation qui **casse au compile** si une valeur diverge du type → zéro dérive ; (b) closure `take` perd le narrowing de `obj` → capturer dans une `const data` non-null ; (c) navigation **inter-route** (accueil→configurateur) réhydrate le wizard depuis localStorage sans reload (vs même-route qui ne remonte pas) ; (d) Web Speech API typable proprement via `declare global` (pas de `as`). **Doctrine respectée** (cf. question Amine sur le déterminisme) : le LLM extrait/propose, le **calcul reste déterministe** ; dette de supervision des heuristiques tracée en **S-056**. Reste epic LLM : S-039 (narration), S-040 (assistant Q&A), S-052, S-053.
- 2026-05-27 — **S-037 ✅ — RECO VIVANTE BRANCHÉE (le LLM entre enfin dans le chemin reco).** Demande Amine après le constat « ce n'est pas DeepSeek qui génère » : brancher la veille catalogue (S-036) dans l'UI. **(A) `ResultsView`** : nouvel état `liveCatalog`/`catalogPending` ; `useEffect([base])` POST le profil de base sur `/api/catalog/live` (clés Firecrawl+LLM **serveur only**, le navigateur ne parle qu'à notre route) → `setLiveCatalog`. `effectiveCatalog = useMemo(liveCatalog ?? seedCatalog(decidePreset(projected).preset, projected))` **injecté** dans `recommend(projected, prices, effectiveCatalog, …)` ET `buildEnsemble(…)` → **rendu initial seed (repli immédiat) puis recalcul sur le live** dès réponse (échec/indispo → reste seed). **(B) `CatalogProvenance.tsx`** (jumeau de `LivePriceStatus`) : par couche C0..C6, `StatusDot` confiance + nom (lien source) + chip provenance (Vérifié en direct / Calibration datée / À revérifier) + `checkedAt` + `assembledAt` + note « veille en cours » tant que `pending`. **(C) Snapshot figé** : `buildDeliverable(profile, reco, ensemble, now?, catalog?)` ajoute la section « Catalogue retenu (provenance des choix) » (7 lignes : composant + provenance + confiance) + joint les **sources web du catalogue** aux sources cliquables ; `buildExitBundle(profile, reco, now?, catalog?)` ajoute `manifest.catalog` (assembledAt/source/slots composant+provenance+confidence+sourceUrl) + fichier **`catalog.json`** rejouable. `ExportButtons`/`ExitEscrow` gagnent la prop `catalog` ; `ResultsView` passe `exportCatalog = activeVariant===null ? effectiveCatalog : undefined` (les **scénarios** de l'ensemble explorent sur leur propre seed → on ne leur attache pas le catalogue vivant, honnête). **Câblage non cassant** : `catalog`/`now` en derniers params optionnels → les appels existants (et leurs tests) compilent inchangés ; markdown/PDF rendent la nouvelle section **gratuitement** (rendu générique des sections). Tests : +2 export (section+sources figées ; rétro-compat sans catalogue), +2 bundle (`manifest.catalog`+`catalog.json` ; rétro-compat), +2 `catalog-provenance` (7 provenances seed ; note pending), e2e +1 (provenance affichée) → **294 + 10 skip**. **e2e desktop 9/9** (dont export MD/PDF/zip = PDF assaini S-054 + bundle catalogue gelé exercés réellement). typecheck 0, lint 0/0, build OK (`/resultats` reste **statique** 11,3 kB). **⚠ Vérif live réelle (DeepSeek) à faire en prod** : en local sans clés Firecrawl/LiteLLM, `/api/catalog/live` renvoie le seed (repli) → le branchement seed→live est prouvé, mais la **génération live par DeepSeek** se vérifie sur la prod Coolify (clés présentes, S-034). Learnings : (a) catalogue effectif `live ?? seed` = même pattern que les prix (repli immédiat, pas d'écran blanc) ; (b) ajouter une `DeliverableSection` suffit pour apparaître en MD **et** PDF (modèle pur partagé) ; (c) figer le catalogue dans `manifest.catalog`+`catalog.json` rend le bundle **rejouable** (on sait quel composant, d'où, à quelle date) ; (d) ne pas attacher le catalogue vivant aux scénarios d'ensemble (ils tournent sur seed) = export honnête. **Débloque S-040 (assistant) ; reste epic LLM : S-038 intake, S-039 narration, S-052 texte libre, S-053 admin prompts.**
- 2026-05-27 — **S-054 ✅ + S-055 ✅** (corrections demandées par Amine « au passage » d'une reprise Ralph). **S-054 — PDF émojis cassés** : le livrable PDF affichait « Ø=ÜÄ … » sur les actions de conformité. **Racine** : `lib/engine/diagnostics.ts` préfixe chaque action/risque d'un émoji littéral hardcodé (📄 📋 🔁 🇲🇦 …) ; ces chaînes alimentent l'UI (UTF-8 → OK) **et** le PDF ; or jsPDF en police standard Helvetica encode en **WinAnsi/CP1252** (ADR-003) → l'émoji 📄 (U+1F4C4, paire surrogate UTF-16 `D83D DCC4`) est réémis octet par octet en CP1252 = « Ø=ÜÄ ». **Correctif dans la couche PDF** (`lib/export/pdf.ts`, `toWinAnsi` exporté) : retire tout code point hors CP1252 (garde ASCII 0x20–0x7E + Latin-1 0xA0–0xFF + les 27 spéciaux 0x80–0x9F € « » — ’ … via un Set Unicode) et compacte les espaces libérés ; appliqué à `writeWrapped`/`writeRow`/bloc sources (`writeBullet` couvert via `writeWrapped`). **Émojis conservés en UI + Markdown** (UTF-8) — seul le PDF est contraint (cohérent ADR-003 « pas de TTF emoji à embarquer »). Tests +3 (`export.test` : cas exacts remontés « 📄 Rédiger une AIPD » → « Rédiger une AIPD », drapeaux/ZWJ/VS retirés ; préservation typo FR é è à ç ’ « » — € + espace insécable ; zéro code point > 0x2122 après assainissement). **S-055 — Bascule entre solutions** : `EnsembleView` rendu interactif — props `activeId`/`onSelect`, 4 cartes-**boutons** sélectionnables (`Votre recommandation` [id null, depuis `ensemble.baseline`] + 3 scénarios), `aria-pressed` + badge « Affichée » sur l'active, `role="group"`. `ResultsView` : état `activeVariant` ; `activeProfile`/`activeResult` (référence par défaut, sinon `ensemble.variants.find(id).profile/recommendation`) **pilotent TOUTE la vue experte** (en-tête preset/score/coût + `presetReason`, `factorsCost`, radar, `media`, `LayerStack`, `CostMap`, conformité/risques, `ExportButtons`, `ExitEscrow`). **Honnêteté U12 préservée** (réconciliation) : bandeau « vous explorez le scénario X — profil enregistré non modifié » + bouton « Revenir à ma recommandation » ; le profil localStorage n'est **jamais** muté (la bascule est éphémère, l'export/Exit Escrow reflètent le scénario exploré tant qu'il est actif). `EnsembleView` passe de `<Card>` à `<section aria-label>` (role `region`, testable). Tests : `ensemble.test` réécrit (3 : référence+3 scénarios, clic → `onSelect("cost")`, `aria-pressed`+badge sur l'actif) + `results.test` +1 (bascule → bandeau + `aria-pressed` true → retour). Validation des **deux** : typecheck 0, lint 0/0, **288 + 10 skip**, build OK (`/resultats` reste **statique** 10,4 kB). **Vérif navigateur groupée prévue avec S-037 avant redéploiement.** Learnings : (a) un bug d'émoji « Ø=ÜÄ » = signature d'un surrogate UTF-16 lu en CP1252 → corriger dans la couche de rendu contrainte, pas en amont (préserve UI/MD) ; (b) `buildEnsemble` exposait déjà `variant.profile` ET `variant.recommendation` → la bascule recalcule la page entière sans refaire tourner le moteur ; (c) bascule = preuve concrète que reco + PDF sont **100 % déterministes** (aucun LLM dans le chemin — confirmé à Amine : DeepSeek câblé S-034 mais dormant tant que S-037+ ≠ faits).
- 2026-05-27 — **S-051 ✅** Refonte presets scorée + explicable + réconciliation (incohérence remontée par Amine ; décision **option 2+1**). Diagnostic confirmé : `expected` des `PRESET_PROFILES` = étiquette d'auteur DIVERGENTE des règles (ex. Coach étiqueté MEDIUM mais classé LIGHT par l'ancienne cascade). **Nouveau `decidePreset`** (2 temps, pur) : (1) **déclencheurs durs → HARD** (secret/cabinet-regule/hipaa/secret-pro/audit&bitemp, inchangé) ; (2) sinon **score de besoin** départage LIGHT/MEDIUM — **volume dominant** (séparateur : `<1 Go`=0 → personnel ; `1–10 Go`=+4 → corpus d'organisation) + sensibilité (public0/confidentiel1/interne2) + débit + users + voix + croissance + budget + activité ; **seuil LIGHT ≤ 3**. **Réconciliation** : poids/seuil calibrés pour que les 6 profils-types calculent EXACTEMENT leur `expected` → test de garde `engine.test` (`decidePreset(p).preset === p.expected` ∀) → **le caveat S-004 « ne jamais tester contre expected » est LEVÉ**. **Explicable** : `PresetDecision` expose `{preset,reason,score,drivers}` ; `reason` cite score+seuil+drivers (« Besoin intermédiaire (score 7 > 3) : volume 10–100 Go (+6), interne (+2)… »), affiché via `presetReason`. ADR-020. Conséquences (fixtures légitimement mises à jour, le modèle est désormais plus honnête) : `bundle.test` `light` rendu réellement minuscule (l'ancien — 5 users/multi-voix/croissance forte — est MEDIUM à juste titre) ; `results.test` défaut → MEDIUM (DEFAULT_PROFILE confidentiel+1–10 Go). Tous les cas `decidePreset` historiques restent verts SANS modif (modèle calibré dessus). Tests : +`preset.test.ts` (10) + réconciliation engine.test → **282 + 10 skip**. typecheck 0, lint 0/0, build OK. **Vérifié navigateur** (`/configurateur` : en-tête « PRESET : MEDIUM » + récap « Vos choix »). Learnings : (a) calibrer un score sur les étiquettes-cibles existantes = réconciliation sans casser les cas unitaires ; (b) **volume = meilleur séparateur LIGHT/MEDIUM** (toutes les attentes LIGHT avaient volume `lt1`) ; (c) `next start` KO avec `output: standalone` + **`networkidle` jamais atteint en dev** (websocket HMR) → capture en `domcontentloaded` (dette script `shoot-page.mjs` notée).
- 2026-05-27 — **S-050 ✅** Récap des choix sous le budget-mètre (demande Amine : « un récap de toutes les options choisies »). `lib/wizard/recap.ts` (**pur**) : `buildChoiceRecap(profile, reco)` → 4 groupes (Résultat / Profil & conformité / Charge & échelle / Usage & contenu) avec **valeurs effectives** dont les **dérivées** (preset retenu, nb de serveurs `reco.compute`, criticité+copies `reco.backup`, modules actifs+niveaux, résumé médias). Chaque item porte `impactsCost` : **transparence DÉFCON 1** — ● = pilote directement une ligne de coût mensuel (utilisateurs/volume/croissance/débit/latence/contenu/médias/modules/backup) ; ○ = oriente la stack/scores/conformité sans ligne de coût directe (zone/régimes/sensibilité/audit/bitemporel/compétences/voix/budget-plafond). Réutilise les libellés `options.ts` (pas d'invention). `components/wizard/ChoiceRecap.tsx` (présentational, `<dl>` sémantique + légende ●/○) câblé sous le `BudgetMeter` dans l'`aside` sticky du Wizard. Tests : +4 `recap.test.ts` (couverture champs + dérivées, flags ●/○ corrects, modules/médias inactifs sans invention, modules actifs reportés). typecheck 0, lint 0/0, **271 + 10 skip**, build OK (/configurateur statique). Vérif jsdom : le Wizard rend `ChoiceRecap` sans erreur (wizard.test). Learning : marqueur `impactsCost` plus honnête qu'un simple « tout coûte » — distingue le coût direct des choix qui orientent via le preset (cohérent avec l'avis non orienté).
- 2026-05-27 — **S-049 ✅** Intégrer `growth` + `latency` (options collectées mais jamais calculées — dette DÉFCON 1 remontée par Amine). Audit confirmé : `growth`/`latency` saisis (`Wizard.tsx`) + affichés (`export/model.ts`) mais **0 usage moteur** (vs `reqPerDay`, lui calculé). Câblés comme **modificateurs de capacité** dans `computeSovereignCompute` (même pattern que bigVolume/manyUsers/highReq) : `growth==="high"` → +1 nœud (réserve de capacité, montée en charge anticipée) ; `latency==="fast"` **ET sous charge** (gros volume ou fort débit) → +1 nœud (latence basse sous sollicitation = marge). **Gating sur la charge** ⇒ pas d'inflation des petits profils/défaut → **aucun test compute existant cassé** (MEDIUM de base reste à 1 ; le delta 2098 de recommend-sizing intact car compute neutre = 0 €). Effets visibles même à prix compute neutres (le **compte d'instances** reflète le choix). Diagnostics `computeRisks` : `latency=fast`+LIGHT et `growth=high`+LIGHT → avertissements. Appliqués hors LIGHT (managé = scaling élastique). Tests : +2 compute (monotonie growth ; latence sous charge vs neutre petit profil) + nouveau `options-integrees.test.ts` (4 : growth high>low, latence sous charge, risques LIGHT) → **267 + 10 skip**. typecheck 0, lint 0/0, build OK. **Dette transparente notée** : `latency→GPU` non câblé (le GPU est piloté par la génération média ; l'inférence LLM/embeddings reste « non chiffrée », dette S-033) — latence pousse le **palier compute (serveurs)**, suffisant et honnête pour S-049. Learnings : (a) réutiliser le pattern de scaling compute existant = intégration monotone + zéro magie ; (b) **gater le bonus latence sur la charge** évite d'inflater le défaut (`latency:fast`) et préserve tous les tests ; (c) prouver l'effet sur `compute.instances[].count` (calculé même à prix neutres) = garde anti-dette sans injecter de prix.
- 2026-05-24 — Itération 0 : fondation. git init, remote Afristrat/mnemo (vide), rangement maquettes → design-reference/, génération artefacts Ralph. Stack actée.
- 2026-05-24 — **S-001 ✅** Scaffold Next.js 15.5 + React 19 + TS strict + Tailwind v3 + ESLint + Vitest + polices next/font. Build OK (5 s, 4 pages statiques), typecheck 0, lint 0, test 1/1. Fichiers : package.json, tsconfig, next.config.mjs, tailwind.config.ts, postcss, .eslintrc.json, vitest.config.ts, app/{layout,page,globals.css}, lib/__tests__/smoke.test.ts. Learning : next/font fonctionne (réseau build dispo).
- 2026-05-24 — **S-002 ✅** Design system. Tokens DESIGN.md → tailwind.config (couleurs Material, fontSize sémantiques, rayons card/input, shadow-elevation). Primitives Button/Card/Chip/Input/StatusDot + cn(). Tests 6/6 (jsdom). typecheck 0, lint 0, build OK. Learning : conflit ERESOLVE @vitejs/plugin-react v6↔vitest 2 résolu en épinglant plugin-react@^4 (pas de --force).
- 2026-05-24 — **S-003 ✅** Moteur de reco porté en TS strict (10 fichiers lib/engine). recommend() pur et déterministe. typecheck 0, lint 0, build OK, test 6/6 (tests moteur = S-004). Learning : `noUncheckedIndexedAccess` n'est pas dans `strict` → indexation tableau sûre ; défauts modules en littéral explicite pour éviter `as`.
- 2026-05-24 — **S-004 ✅** Tests moteur (14 tests, total 20/20). Couvre decidePreset (HARD/LIGHT/MEDIUM), computeScores (conf=10 HARD, conf=4 LIGHT+audit requis, audit=10 bitemp requis), profileCostFactors (=465), recommend (invariants 7 couches/8 dim, bonus module conflict, déterminisme). typecheck 0, lint 0, build OK. **Learning capital : le champ `expected` des PRESET_PROFILES est une étiquette d'auteur, PAS le résultat calculé — « Coach » tombe en LIGHT par les règles. Ne jamais tester contre `expected`.**
- 2026-05-24 — **S-005 ✅** Wizard 6 étapes (16 params + modules + récap), persistance localStorage, profils-types, preset décidé inline. Route `/configurateur`. Tests 22/22 (dont 2 wizard). typecheck 0, lint 0, build OK. Learnings : togglers dédiés vs générique union ; `afterEach(cleanup)` obligatoire ; apostrophe typographique en JSX.
- 2026-05-25 — **S-006 ✅** Page `/resultats` : radar SVG maison (lib/charts/radar.ts, pur+testé), LayerStack 7 couches, CostMap (bandes ±30 % via costBand + pastilles 🟢🟡🟠 + sources datées via lib/pricing/sources.ts), slider de projection (volume + users, recalcul live), scores, compliance/risks. Tests 30/30 (radar 5, costBand 2, results 1). typecheck 0, lint 0, build OK. Learnings : radar SVG = zéro dépendance charting (pas de souci SSR canvas) ; sources seed réelles issues de la calibration, à rafraîchir par price feed S-008. Prochaine : S-007 (ensemble multi-config).
- 2026-05-25 — **S-007 ✅** Ensemble multi-config (F5). `lib/engine/ensemble.ts` (pur, déterministe) : `buildEnsemble(profile)` → baseline + 3 membres (sovereignty / cost / speed) via overrides ciblés, chaque membre passé dans `recommend()`, puis `computeSpread()` = dispersion (costRange/costRangePct, scoreRange, presetsSpan, agreement fort/modéré/faible + `uncertaintyLabel` explicite). Analogie prévision d'ensemble météo. UI : `components/results/EnsembleView.tsx` (présentational, prop `ensemble`) intégré dans ResultsView via `useMemo(buildEnsemble)` sur le profil projeté, placé après « Se projeter ». Tests 44/44 (+13 engine ensemble, +1 UI). typecheck 0, lint 0, build OK (/resultats 4.26 kB). Learnings : (a) overrides sécurité honnêtes — on ne baisse JAMAIS la `sensitivity` sous le choix utilisateur ; si elle force HARD, tous les membres convergent en HARD → spread faible = signal honnête de robustesse ; (b) modules max/off construits depuis `MODULES` (pas de littéral fragile, pas de `as`) ; (c) composant présentational (prop `ensemble`) plutôt que hook interne → testable sans localStorage.
- 2026-05-25 — **S-008 ✅** Price feed Firecrawl (F4). `lib/pricing/` : `extract.ts` (extraction figures $/€ + anti-bruit + empreinte djb2 + `detectVariation`, pur), `firecrawl.ts` (client `fetch` injectable, v2/scrape, jamais throw → repli), `cache.ts` (`TtlCache` 6 h, `now` injectable), `baseline.ts` (snapshots datés capturés via le vrai code), `feed.ts` (`buildObservation` pur + `refreshAllPricing` parallèle + cache + repli seed). Route `app/api/pricing/route.ts` (nodejs, dynamic, clé serveur jamais exposée). UI `components/results/PriceFreshness.tsx` (fetch `/api/pricing`, statuts vérifié/à-revérifier/indispo + bouton rafraîchir) intégrée dans ResultsView. Script `scripts/capture-baseline.mts` (capture les empreintes via l'extracteur réel). Tests 60/60 (+16). typecheck 0, lint 0, build OK (`/api/pricing` = ƒ dynamic). **Validation LIVE** : `next start` + `curl /api/pricing` → 5 sources scrapées, fingerprints conformes au snapshot, UTF-8 OK (€). Learnings : (a) anti-bruit indispensable — Mistral expose un compteur animé « $00123456789… » filtré par MAX_DIGITS=9 ; (b) Qdrant = usage-based → 0 figure → confiance medium + note manuelle (honnête, pas d'invention) ; (c) baseline généré PAR le code (`node --experimental-strip-types`) = zéro divergence extracteur/référence, zéro saisie manuelle ; (d) `.mts` hors include tsconfig (`**/*.ts` ne matche pas `.mts`) → pas typechecké, OK ; (e) `import type` erasé par strip-types → l'alias `@/` type-only ne gêne pas le script node.
- 2026-05-25 — **S-009 ✅** Livrable export (F6). `lib/export/` : `model.ts` (`buildDeliverable` pur → structure neutre profil/preset/stack/scores/coûts/ensemble/conformité/risques + sources dédupliquées + disclaimer ; libellés FR réutilisés de `wizard/options`), `markdown.ts` (rendu MD, sources en liens cliquables), `pdf.ts` (jsPDF, Helvetica/WinAnsi = typo FR complète, `textWithLink` cliquable, pagination auto). UI `components/results/ExportButtons.tsx` (MD + PDF, **jsPDF en import dynamique au clic** → /resultats reste à 133 kB au lieu de 261). Dép : `jspdf`. Tests 64/64 (+4, dont génération PDF en node). typecheck 0, lint 0, build OK. Décisions tracées : `docs/DECISIONS.md` ADR-003 (PDF réel via jsPDF) + ADR-004 (7 advisories npm dev/build-time acceptées, pas de fix cassant). Learnings : (a) jsPDF gonfle le bundle → import dynamique obligatoire ; (b) WinAnsi/CP1252 couvre toute la typo FR (é « » € —) avec les fonts standard, pas de TTF à embarquer ; (c) modèle pur partagé MD/PDF = un seul point de vérité testable.
- 2026-05-25 — **S-010 ✅** Exit Escrow (F7, moat ①). `lib/exit/bundle.ts` (`buildExitBundle` pur → manifest.json + README + docker-compose paramétré + terraform main/tfvars + vault squelette + runbook MEL/backup 3-2-1 + scripts re-embed/backup), `lib/exit/zip.ts` (fflate `zipSync`, chemins « / » = arbo). UI `components/results/ExitEscrow.tsx` (téléchargement .zip, bundle+fflate en import dynamique). Dép : `fflate` (0 vuln). Tests 69/69 (+5, dont round-trip zip/unzip + cohérence manifest↔reco). typecheck 0, lint 0, build OK. ADR-005. Learnings : (a) `fflate` zéro-dép > jszip ; (b) `Uint8Array.from(bytes)` pour BlobPart valide sans `as` (TS strict refuse `Uint8Array<ArrayBufferLike>`) ; (c) manifest.json = ancre testable de cohérence avec la reco ; (d) vault n'expose JAMAIS de valeur de secret.
- 2026-05-25 — **S-011 ✅** Fiduciary Mode (F8, moat ②). `lib/fiduciary/charter.ts` (charte = donnée pure, source unique : revenueModel conseil+déploiement, 5 engagements dont zéro commission cachée). Page `/fiduciaire` (divulgation accessible, statique). Charte intégrée au livrable export (`model.ts` section « Charte fiduciaire »). Liens d'accès depuis l'accueil + carte Exit Escrow. Tests 74/74 (+5) : contenu charte, présence livrable, **invariant structurel** (aucune clé/ libellé `commission|affiliation|kickback…` dans Profile/Layer/Module + reco = pure fonction du profil). typecheck 0, lint 0, build OK. Learning : prouver « aucune reco conditionnée à une commission » = invariant testable sur les clés/labels + déterminisme, plutôt qu'un test d'absence dans le source.
- 2026-05-25 — **S-012 ✅** Rails Supabase (F9, moat ③). Migration `supabase/migrations/20260525010854_init_rails.sql` : 5 tables (circles, memberships, network_consents, configurations, cost_observations) avec pivot `circle_id`, **RLS activée sur TOUTES** + policies `to authenticated`, helpers `is_circle_member`/`is_circle_owner` SECURITY DEFINER (anti-récursion), trigger `handle_new_user` (cercle perso à l'inscription), consentement opt-in **horodaté**, schéma coût réel prêt (vide Lot 1). Couche `lib/supabase/{types,client,server}.ts` (@supabase/ssr, `getUser()` serveur, clé publishable seule côté client). `lib/network/consent.ts` (builder pur horodaté). `.env.example` MAJ (nouvelle terminologie clés). Tests : consent unit + **intégration RLS LIVE** (4/4 : isolation cross-tenant, trigger, consentement, anon bloqué) gated `SUPABASE_TEST_*` → skip en CI. Total 76 + 4 skip ; typecheck 0, lint 0, build OK. RLS vérifiée en base (5/5 relrowsecurity=t). ADR-006. Learnings : (a) policies récursives → helpers SECURITY DEFINER `search_path=public,pg_temp` ; (b) test d'intégration gated = preuve réelle sans casser le CI ; (c) Next inline NEXT_PUBLIC_* seulement en référence littérale → pas de `process.env[name]` dynamique côté client ; (d) UI auth = Lot 2 (hors acceptance F9).
- 2026-05-25 — **S-013 ✅** Playwright E2E + CI. `playwright.config.ts` (webServer build+start, projets desktop-chromium + mobile-chrome, baseURL :3000). `e2e/parcours.spec.ts` : parcours wizard→résultats (5× Suivant + lien), 3 exports (MD/PDF/zip via `waitForEvent('download')`, skip mobile), charte fiduciaire. `.github/workflows/ci.yml` (job quality : typecheck/lint/test/build + `npm audit --audit-level=high` ; job e2e : playwright + artefact). Dép : `@playwright/test` + chromium. E2E 5 passés + 1 skip (mobile export). ADR-007. typecheck 0, lint 0, build OK, unit 76+4skip. **Piège majeur** : `reuseExistingServer:!CI` a réutilisé un `next start` RÉSIDUEL (PID fantôme) d'une build S-008 → 404 trompeurs sur /fiduciaire + boutons d'export absents. **Toujours libérer le port 3000 avant un run e2e local** (`Get-NetTCPConnection -LocalPort 3000 | Stop-Process`).
- 2026-05-25 — **S-014 ✅** CLAUDE.md projet + README + CI + migration ESLint CLI. `CLAUDE.md` réécrit pour décrire l'app réelle (commandes, archi lib/components/app, conventions, pointeurs) — l'ancien décrivait les maquettes. `README.md` d'onboarding (prérequis, env, Supabase local, parcours, qualité, structure). CI déjà posée en S-013 (`.github/workflows/ci.yml`). **Dette `next lint` résorbée** : ESLint 9 + flat config `eslint.config.mjs` (FlatCompat → `next/core-web-vitals`, ruleset inchangé), script `eslint .`, `.eslintrc.json` supprimé, `next.config` `eslint.ignoreDuringBuilds:true`. 2 warnings de config (export anonyme) corrigés. ADR-008. Validation finale : typecheck 0, lint 0/0, unit 76+4skip, build OK, e2e 5+1skip. **LOT 1 COMPLET (14/14).**
- 2026-05-25 — **Infra Supabase local (hors story, demande utilisateur)** : `npx supabase init` + `start` (Docker). `supabase/config.toml` généré (untracked, sera intégré en S-012). Credentials locaux dans `.env.local` (git-ignoré). ⚠ CLI 2.101 = nouveau système de clés (`sb_publishable_…` / `sb_secret_…`) au lieu des JWT anon/service_role → en S-012, mettre à jour `.env.example` (anciens noms) + le client `@supabase/ssr` en conséquence. Services `imgproxy` + `pooler` arrêtés en local (non bloquant pour DB/Auth/REST).
- 2026-05-26 — **S-015 ✅** Migration des types (refonte Strate, ex-Mnémo). `audit`/`bitemporal` : `Requirement` (3 états) → `boolean` (mapping `required→true`, `desired→false`, `no→false` ; cf. **ADR-009**). Nouveaux types `MediaNeed`/`Sizing`/`WorkloadLine`/`Verdict`/`GpuTier`/`BlockId` définis (consommés S-016/S-018). `Profile.mediaNeeds?`/`freeNotes?`. `Requirement` supprimé (zéro résidu). UI : composant **`YesNo`** binaire remplace `RadioCards`+`REQUIREMENT_OPTIONS` ; `export/model` affiche Oui/Non. **localStorage `v1→v2`** (anti-corruption : anciennes chaînes truthy). Tests : +`types.test.ts` (4) → **80 passés + 4 skip** ; typecheck 0, lint 0/0, build OK. **Exécuté en mode Ralph + double revue (Sparring Partner + Avocat du Diable)** : a rattrapé (a) `types.test.ts` manquant, (b) descriptions presets trompeuses (Coach « audit souhaité » / PME « bitemporel pour ADR » / Chercheur « bitemporel utile » → reformulées, DÉFCON 1), (c) localStorage v2, (d) conventions JSDoc (MediaNeed `none+none` = inactive à filtrer ; GpuTier provisoire). **Dette portée en aval (notée ADR-009) :** S-016 = filtrer MediaNeed inactive + **valider la granularité `GpuTier`** avant tout consommateur ; S-018 = décider si `p.audit` alimente la dimension `audit` (pilotée par `bitemporal` seul aujourd'hui) ; S-019 = a11y `YesNo` (`aria-labelledby`). **Learning : worker subagent crashé (erreur serveur 500) à mi-parcours (moteur fait, mais `export/model`, `Wizard.tsx`, 6 tests non traités) → l'orchestrateur reprend et FINIT manuellement ; toujours re-typechecker pour cartographier le reste.**
- 2026-05-26 — **S-016 ✅** Module `sizing.ts` (cœur du dimensionnement, refonte Strate). `costMultimodalSizing(profile, prices) → Sizing` pure/déterministe : besoins multimédias (2 volets mémoriser+créer × modalité × souverain/api × palier) → ressources. **GPU mutualisé compté UNE FOIS** (charge `sovereign` cumulée → 1 palier `gpu`), `api` hors GPU souverain (coût à l'usage porté par la ligne), stockage indexé sur le volume, `embeddingsMultimodal` si médias actifs, `workloads` traçables (`contributesTo` C4/C5/C6). Prix **injectés** via contrat `MultimodalPriceTable`/`MultimodalPriceEntry` (sourcé en S-017, DÉFCON 1 : zéro valeur monétaire en dur). `GpuTier` validé à 4 paliers. **Extension additive spec §4.3** : `WorkloadLine.monthlyCost` (évite parsing de `estimate` / duplication des quantités en S-018 → 1 source de vérité). ADR-010. Tests table-driven **27** (modalités × volets × modes × paliers ; monotonie, anti-double-comptage GPU, `api`≠`sovereign`, `generate`>`ingest`, stockage croissant, neutre sur vide, source présente, déterminisme) → **107 passés + 4 skip**. typecheck 0, lint 0/0, build OK (/resultats 7.56 kB). Learnings : (a) tester des **propriétés** (monotonie via boucle `none<light<medium<intensive`, coût GPU = prix d'UN palier ≠ somme) > valeurs magiques ; (b) `Sizing.gpu` n'a pas de champ `source` → la traçabilité « PriceSource sur chaque coût » (spec §5.6) se prouve sur les entrées de la table injectée (et `Recommendation.costSources` en S-018) ; (c) `setupCost`/`Verdict` restent S-018 (dépendent du champ backlog d'intake, S-020) — scope S-016 = récurrent uniquement.
- 2026-05-26 — **S-017 ✅** Pricing médias sourcé (DÉFCON 1, refonte Strate). **Sourcing web réel** (4 agents parallèles, pages officielles) → `docs/pricing/media-cost-sources.md` (audit trail : figure exacte + unité + URL + date + confiance, repères + NON TROUVÉ assumés). `lib/pricing/media-seed.ts` : table `MultimodalPriceTable` en **devises natives** (Scaleway € : GPU L4/H100 + Object Storage ; OpenAI/Google/Runway $ : whisper/tts/Vision/gpt-image/Gen-4). `lib/pricing/media-feed.ts` : étage **FX** (`fetchUsdToEur` live Frankfurter/BCE, repli taux BCE daté 1,1634 ; `normalizeMediaPricesToEur` pur), `getMediaPrices()` natif / `getMediaPricesEur(rate)` € pour le moteur, `refreshMediaPriceFreshness` (Firecrawl, fraîcheur seulement). Tests **13** (sources sur chaque coût non nul, libellé estimation sur confiance low, devises natives, normalisation FX + repli, monotonie GPU €, intégration costMultimodalSizing) → **120 passés + 4 skip**. typecheck 0, lint 0/0, build OK. ADR-011. **Amendement S-016 piloté par retour Amine** : `MultimodalPriceEntry.currency` ajouté (ne pas figer le € converti ; afficher natif+converti possible) → fixture sizing.test mise à jour, tout reste vert. Learnings : (a) **devise native + étage FX sourcé** > montant € baké (fidélité source, taux rafraîchissable via API BCE-backed) ; (b) distinguer conversion de **devise** (FX centralisé) et conversion d'**unité** (car./img→min, bakée + confiance low) ; (c) dispatch de sous-agents de recherche = sourcing parallèle efficace, mais l'orchestrateur **vérifie** (URL + figure) et assigne la confiance ; (d) le feed Firecrawl pour des prix dérivés = signal de **fraîcheur**, jamais réécriture auto (anti-DÉFCON 1).
- 2026-05-27 — **S-024 ✅ — REFONTE STRATE COMPLÈTE (S-015→S-024, 10/10).** E2E parcours + nettoyage. `e2e/strate.spec.ts` : 3 parcours (90 s→verdict ; bloc Médias génération vidéo souveraine→**budget rouge + levier** ; round-trip /configurateur→/resultats) **desktop + mobile** → 11/11 (+1 skip mobile-export, avec `parcours.spec.ts`). Slider e2e via `focus()`+`press("End")`. Nettoyage : **aucun code mort** (wizard réécrit en place, pas d'orphelin 6-steps ; `Requirement` supprimé en S-015 → 2 commentaires historiques seulement ; usages renommés). typecheck 0, lint 0/0, **163 + 8 skip**, build OK, e2e **11 + 1 skip**. ADR-018. **Toutes les stories du prd.json passes=true** → `<promise>COMPLETE</promise>`. Restent hors-prd : déploiement refonte, backlog S-025+ (backup, multi-continent/multi-loi), renommage Strate, sondage→prix→homepage.
- 2026-05-27 — **S-022 ✅** Résultats : mode verdict + CostMap étendu (refonte Strate UI). `ResultsView` injecte `getMediaPricesEur()` (coûts multimodaux **dans** C4/C5/C6 — câblage UI de S-018) + **mode `verdict`|`expert`** (lu de `?mode=verdict` via `window.location` → `/resultats` reste **statique**, pas de `useSearchParams`/Suspense) + toggle. `VerdictView` (besoin/risque/gain neutres, prix ferme `[PLACEHOLDER]`, coût ±30 %, mise en route, prochaine étape). `CostMap` étendu : ligne setup + section « Apport multimédia (déjà compris) » 🟢 GPU 1× / embeddings / stockage · 💳 API + disclaimer devis. Tests : ResultsView verdict/expert + VerdictView → **163 passés + 8 skip**. Vérifié **navigateur** (verdict). typecheck 0, lint 0/0, build OK (/resultats statique). ADR-017. Learning : lire `?mode` via `window.location` (pas `useSearchParams`) garde la page **statiquement prérendue** sans Suspense boundary.
- 2026-05-27 — **S-021 ✅** Chemin 90 s (refonte Strate UI). `lib/quick/profile-mappers.ts` (**pur**) : 4 réponses qualitatives (`who`/`data`/`priority`/`media`) → `Profile` complet par défauts **prudents et documentés** (régulé/secret ⇒ audit+bitemporel ; RGPD par défaut ; jamais sous-dimensionné). `components/quick-profile/QuickProfileForm.tsx` : 4 questions → écrit le profil dans la **même source de vérité** (`localStorage` `STORAGE_KEY`, écriture **synchrone** avant nav) → `/resultats?mode=verdict` ; « Affiner » → `/configurateur` pré-rempli. `app/page.tsx` = entrée 90 s par défaut (nom « Mnémo » conservé — renommage Strate = story dédiée §15). Tests : mappers 8 + form 3 (`useRouter` mocké `vi.hoisted`) → **161 passés + 8 skip**. Vérifié **navigateur** (accueil 90 s). typecheck 0, lint 0/0, build OK. ADR-016. Le **rendu** du mode verdict sur /resultats = S-022 (ici : chemin + paramètre). `scripts/shoot-page.mjs` générique ajouté. Learning : mocker `next/navigation` `useRouter` via `vi.hoisted` ; écrire localStorage **synchrone** avant `router.push` (sinon course avec l'effet de persistance du hook).
- 2026-05-27 — **S-020 ✅** Bloc Médias + budget-mètre (refonte Strate UI). `MediaNeedsBlock` : par modalité (audio/vidéo/images), volets **mémoriser** + **créer** (paliers `MMTier` via `ContinuousSlider`), toggle 🟢 souverain / 💳 API, champ **backlog** (→ setupCost). **Aperçu live** : `costMultimodalSizing` + `computeSetupCost` (prix € injectés, défaut `getMediaPricesEur()`) → « ≈ X €/mois d'infra, dont GPU… » + stockage + mise en route + disclaimer ±30 %. `lib/wizard/budget.ts` (`evaluateBudget`, **pur**) : vert/jaune/rouge + cause dominante (GPU génération / API médias / stack de base) + 1 levier — jamais le prix de vente. `BudgetMeter` (sticky) câblé dans le Wizard ; prix réels injectés → header + budget-mètre reflètent le multimodal. Tests : budget 7 + media-block 6 → **150 passés + 8 skip**. typecheck 0, lint 0/0, build OK (/configurateur 8,23 kB). ADR-015. Learning : cause/levier du budget-mètre = **factuels et neutres** (un fait + une option), jamais d'injonction (principe avis non orienté).
- 2026-05-27 — **S-019 ✅** Wizard 4 blocs (refonte Strate UI). `Wizard.tsx` : 6 étapes → 4 blocs (Profil&contraintes / Infra pure / Usage-Mémoire / Médias). Créés : `InfoBubble` (disclosure accessible « pourquoi + conséquence » **factuelle, non orientée** — principe revue Amine), `ContinuousSlider` (range sur paliers discrets ordonnés, `aria-valuetext`). `useWizardProfile` + `setNote(block)` ; `freeNotes` additif (`DEFAULT_PROFILE.freeNotes={}`, pas de bump localStorage). Usages costables renommés (`modules.ts` `name` : Traçage des revirements / Mémoire infalsifiable / Décisions horodatées / Plan de panne / Détecteur de conflits). `YesNo` : `role="group"` + `aria-label` (dette a11y S-015 levée). Bloc ④ Médias structurel (intro+note ; détail = S-020). Tests : wizard 4 blocs + a11y YesNo (4) + hydratation round-trip renderHook (3) → **137 passés + 8 skip**. e2e parcours adapté (3 « Suivant ») **vérifié LIVE 3/3 desktop**. typecheck 0, lint 0/0, build OK (/configurateur 5,33 kB). ADR-014. Learnings : (a) toucher le wizard ⇒ **mettre à jour `e2e/parcours.spec.ts`** (compteur d'étapes) — sinon dette CI ; (b) le titre du bloc courant apparaît dans l'indicateur ET le `h2` → `getAllByText` en test ; (c) apostrophes JSX → typographiques `’` (lint `react/no-unescaped-entities`).
- 2026-05-27 — **S-023 ✅** Conversion/data (refonte Strate, moat data + acquisition). Migration `20260526120000_conversion_data.sql` : `simulation_log` (anonyme-friendly, `share_token` pour rapport partageable) + `lead_capture` (e-mail PII), **RLS activée sur les deux**. `simulation_log` : insert anon+auth, select membres du cercle ; **RPC SECURITY DEFINER `get_simulation_by_token`** (rapport par jeton sans exposer la table). `lead_capture` : insert ouvert, select **propriétaire de cercle** seulement (PII). Types `lib/supabase/types.ts` étendus (rows/inserts + Functions). Builders **purs** `lib/conversion/log.ts` (`buildSimulationLog`, `buildLeadCapture`, `isValidEmail`) + 5 tests unitaires. Test RLS gated (4) **vérifié LIVE 8/8** (avec les 4 de S-012) : isolation cross-tenant simulations+leads, rapport par jeton anon, anon bloqué. Routes HTTP + UI exit-intent **déférées à S-024** (mécanisme complet+prouvé ici). typecheck 0, lint 0/0, build OK, suite 132+8skip. ADR-013. **Learning : `supabase db reset` peut casser `auth.uid()` (échec des tests RLS, même pré-existants) → restaurer via `supabase stop && start` avant vérif live.**
- 2026-05-26 — **S-018 ✅** Intégration sizing → couches dans `recommend` (refonte Strate). `recommend(profile, prices = NEUTRAL_MEDIA_PRICES)` : **prix injectés** (moteur pur), appelle `costMultimodalSizing` + `applyMultimodalSizing` (coûts **dans** C4/C5/C6, GPU compté UNE fois en C6), `computeSetupCost` (backlog), `mediaCostSources`, `buildVerdict`. `Recommendation` étendu (`sizing`/`setupCost`/`costSources`/`verdict`), `MediaNeed.backlog?`, type `CostSource`, `NEUTRAL_MEDIA_PRICES`. `ensemble` threade les prix. Tests **7** (`recommend-sizing` : défaut neutre = invariant préservé ; GPU 1× en C6 pas C4 ; surcoût total = somme injectée ; setupCost backlog ; verdict bandes ±30 % + placeholder ; costSources dédupliquées) → **127 passés + 4 skip**. typecheck 0, lint 0/0, build OK. ADR-012. **Décision Amine** : champ `backlog` avancé en S-018 (donnée + calcul), UI de saisie restant S-020 → setupCost fidèle sans placeholder. Learnings : (a) **prix injectés + défaut neutre** = moteur pur ET tests/ensemble existants verts sans churn (profils sans `mediaNeeds` → sizing neutre → invariant `totalCost=baseCost+moduleCost` intact) ; (b) étendre un type de sortie largement consommé est sûr tant que les consommateurs **lisent** (seul le constructeur `recommend` change) ; (c) UI = défaut neutre jusqu'à ce que `ResultsView` injecte `getMediaPricesEur()` en S-022 — séparation moteur/UI propre.
- 2026-05-27 — **S-032 ✅** Homepage de vente (portage du brouillon validé, refonte Strate UI). `app/page.tsx` réécrit : `homepage-draft.html` (décision #11) porté en composants React + **tokens du design system** (primary teal, Space Grotesk, rayons/ombres DESIGN.md — zéro couleur hors palette). Sections : nav sticky (ancres #pourquoi/#comment/#tarifs + lien /fiduciaire), hero (« …ne devrait jamais quitter vos murs »), **4 promesses** (preuve DURE 🟢 sur −risques = Exit Escrow + fiduciaire ; les 3 autres = preuve molle « à mesurer chez vous », **DÉFCON 1 : zéro stat inventée**), wedge sombre « on vous dit la vérité » + tags + lien charte, 3 étapes 90 s, **tarifs 3 paliers** (prix `[__ /mois]` + badge « Prix — sondage en cours » + note Van Westendorp/conjoint — **aucun montant au doigt mouillé**), section **diagnostic = QuickProfileForm réel** (chemin 90 s fonctionnel), CTA final, footer. `layout.tsx` : `scroll-smooth` pour les ancres. Nom « Mnémo » conservé (renommage Strate = tâche dédiée). e2e « chemin 90 s → verdict » **préservé** (bouton « Voir mon verdict » toujours sur `/`). Validation : typecheck 0, lint 0/0, **163 + 8 skip**, build OK (/ statique 1,95 kB), **rendu vérifié navigateur desktop 1280 + mobile 390** (responsive : grids 2/3 cols → 1 col, nav condensée). Learnings : (a) liens stylés en pilule = classes partagées (`PILL_PRIMARY`/`PILL_GHOST`) sur `<Link>`/`<a>` plutôt que le `<button>` Button ; (b) `.playwright-mcp/` + PNG de capture ajoutés au .gitignore (artefacts de vérif visuelle). **Dette signalée hors scope : `favicon.ico` 404 (aucun favicon dans le projet) — touche tout le site, à traiter séparément.**
- 2026-05-27 — **S-025 ✅** Price feed LIVE Firecrawl + garde-fou DÉFCON 1 (prérequis backup). **Faisabilité prouvée AVANT de coder** : extraction structurée Firecrawl (`format json` + schéma/prompt LLM) fiable sur page **dédiée** (H100 → 2,73 €/h) mais **aberrante sur page d'agrégat** (`/pricing/gpu/` → H100 à **0 €**). → garde-fou obligatoire. Créés : `lib/pricing/firecrawl.scrapeStructuredJson` (extraction, fetchImpl injectable, jamais throw), `lib/pricing/reconcile.ts` (`reconcilePrice` **pur** : live promu si dans bande ±60 % vs baseline → `live` ; aberrant/0/×10 → `flagged` baseline conservée + confiance basse ; null → `fallback`), `lib/pricing/backup-seed.ts` (egress 0,01 / archive 0,00254 / retrait 0,009 / hors-site 0,01 €/Go, sourcés `docs/pricing/backup-cost-sources.md`), `lib/pricing/live-feed.ts` (`getLivePrices` : scrape GPU+H100+stockage Scaleway 1× chacun en //, €/h GPU → mensuel ×heures {200/730/730}, réconcilie **médias + backup**, `getLivePricesCached` TTL 6 h), route `app/api/pricing/live` (clé serveur jamais exposée), composant `LivePriceStatus` (provenance : statut/source/fraîcheur). `ResultsView` : seed en valeur initiale (repli immédiat) → fetch `/api/pricing/live` → `setPrices(feed.media)` → **`recommend` recalcule avec le live**. Lecture JSON inconnu sans `as`/`any` (type guard `isRecord`). Périmètre live = GPU souverain + stockage + backup (€ Scaleway) ; API $ (OpenAI/Google/Runway) restent seed+FX (inextractibles fiablement → ne pas injecter du faux). Tests **+15** (reconcile 7, backup-seed 3, live-feed 5 : promotion, repli page H100 absente, flag ×13, repli total, clé absente) → **178 + 8 skip**. typecheck 0, lint 0/0, build OK (`/api/pricing/live` ƒ, `/resultats` statique 9,29 kB). **Validation LIVE réelle** : `/api/pricing/live` (vraie clé) → **8/8 postes `live`**, H100 dédié 1992,9 €/mois (et non 0), FX live. ADR-019. Learnings : (a) **tester la faisabilité d'extraction AVANT d'architecturer** — le 0 € H100 aurait été injecté sans garde-fou ; (b) le seed n'est pas qu'un repli réseau, c'est le **filtre de plausibilité** ; (c) cibler la **page dédiée** (pas l'agrégat) par prix ; (d) `Reflect.get` renvoie `any` → préférer un type guard `v is Record<string,unknown>` pour lire un JSON inconnu sans `as`/`any`.
- 2026-05-27 — **S-026 ✅** Moteur backup pur (spec n°1 §3/§4/§6/§7). Types `Backup`/`BackupPlan`/`VectorDecision`/`ThreeTwoOne`/`BackupCriticality`/`ErasurePolicy`/`VectorBackupStrategy` + `Profile.backup?` dans `types.ts` ; `BackupPriceTable` défini côté **engine** (contrat injecté ; `backup-seed.ts` l'importe maintenant). `lib/engine/backup.ts` : `deriveBackupPlan(profile)` (criticité→params table §4 ; surcharge conformité régulé/secret → crypto-shred+immutable+BYOK+`legalRetentionYears`) + `costBackup` (prix injectés) + `buildBackupPlan`. **Durées de rétention légale SOURCÉES** (sous-agent + vérif, DÉFCON 1) → `docs/pricing/backup-cost-sources.md` §conformité : HIPAA 6 (45 CFR §164.316), AI Act 10 (Règl. 2024/1689 art.18), secret-pro 5 (CNB, medium), RGPD/CNDP 0 (limitation, pas plancher). **Correctif modélisation** : le tier `cold` n'est PAS un remplacement du chaud (sinon `high` < `standard` vu l'archive ~6× moins chère) mais une archive longue durée EN PLUS → **monotonie criticité préservée**. Arbitrage vecteurs §5 : souverain+auto (non-critical) → ré-embed (reconstructible, coût 0, RTO long) ; critical → backup ; pas de double-comptage. Tests **+19** (dérivation, conformité, monotonie criticité/copies/rétention, offsite>onsite, sources, arbitrage) → **197 + 8 skip**. typecheck 0, lint 0/0, build OK. ⚠ Pas encore intégré dans `recommend` (= S-028). Learnings : (a) sourcer les durées juridiques (jamais inventer) + disclaimer « ingénierie, pas avis juridique » ; (b) le ratio hot/cold casse la monotonie naïve → archive additive ; (c) `BackupPriceTable` côté moteur (contrat), pricing implémente.
- 2026-05-27 — **S-027 ✅** Scoring 9ᵉ dimension `resilience` (refonte 8→9, spec §8). `SCORE_KEYS` gagne `resilience` (9 entrées). `scores.ts` : `computeScores(preset, p, totalCost, backupCore = deriveBackupPlan(p))` — **core injecté avec défaut auto-dérivé** (pattern S-018) → `recommend` reste cohérent sans modif, S-028 passera le core déjà calculé. Rubrique `resilienceScore(core)` pure : base par criticité (none1/standard5/high7/critical8) + 3-2-1-1-0 (copies≥3/offsite/airgap/immutable +0,5 chacun) + restauration testée (+1) + crypto-shred (+0,5) − **pénalité 2** si RPO/RTO incohérents avec la criticité (override expert laxiste) ; clamp [0,10]. `none` → score plancher 1 + libellé d'alerte. `RadarChart` **déjà dynamique** (`data.length` axes + aria-label auto) → rend 9 axes sans modif. MAJ : `SHORT_LABELS`+`resilience`, titres « 8 → 9 dimensions » (ResultsView, page meta, export `model.ts`, PRD, README). Tests : +4 resilience (monotonie criticité, plancher+alerte, pénalité incohérence, surcharge conformité) + 5 assertions `toHaveLength(8)→9` (engine ×2, recommend-sizing, profile-mappers) + heading export → **208 + 8 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) `deriveBackupPlan` (pur, sans prix) = bonne dépendance pour le scoring `resilience` (pas de cycle scores→backup, backup n'importe pas scores) ; (b) `SHORT_LABELS: Record<ScoreKey,…>` = filet de sécurité (le compilateur exige l'entrée `resilience`) ; (c) le double-arrondi de `recommend` (9,5→10) aplatit high/critical à l'affichage, mais la monotonie stricte se teste sur `computeScores` direct (non arrondi).
- 2026-05-27 — **S-033 (fondation) ✅** Compute souverain dimensionné + sourcé — répond au constat Amine « pour le self-hosted, le coût des serveurs d'hébergement est invisible » (forfait C6 hardcodé 30/100/400, ni dimensionné ni sourcé — viole aussi la doctrine prix-live S-025). Faisabilité prouvée : extraction structurée live des prix d'instances Scaleway (DEV1-M 0,0198 €/h, PRO2-XS 0,11, PRO2-S 0,219). Créés : `docs/pricing/compute-cost-sources.md` (sources + hypothèses ±30 %), `lib/pricing/compute-seed.ts` (`COMPUTE_PRICE_SEED` : glue=DEV1-M, node=PRO2-S, sourcés), `lib/engine/compute.ts` (`computeSovereignCompute(profile, preset, prices)` pur : LIGHT 1 glue / MEDIUM 1 node / HARD 2 nodes + scaling volume/users/débit ; `NEUTRAL_COMPUTE_PRICES`). Tests **+7** (dimensionnement, scaling, monotonie HARD≥MEDIUM≥LIGHT, sources, prix injectés) → **204 + 8 skip**. typecheck 0, lint 0/0, build OK. ⚠ **RESTE (intégration) = S-028 élargi** : remplacer le forfait C6 baké par ce compute dans `recommend` (+ le LLM tokens reste estimation documentée, dette notée) + feed live instances + visibilité CostMap. Learning : le forfait C6 mélangeait serveurs + LLM ; on sort les serveurs (dimensionnés+sourcés), le LLM à l'usage reste une estimation à chiffrer (story future, prix tokens live).
- 2026-05-27 — **Redéploiement prod + résolution bugs P0 (hors story).** Tout le travail du jour (S-027→S-042) **déployé en prod** via API Coolify (`POST /api/v1/deploy`, token coffre) ; nouvelle version confirmée par marqueur SSR `radar 9 dimensions` sur `/resultats`. **Bugs P0 du test utilisateur = obsolescence** (build prod antérieur), PAS de régression : `playwright.config.ts` gagne `E2E_BASE_URL` (cible externe, webServer désactivé si `https://`) → suite e2e **navigateur propre contre la prod** = **7/7 verts** (charte + export MD/PDF/ZIP + backup + round-trip). Le test manuel claude-in-chrome était pollué par les **extensions** d'Amine (Loom/Quillbot → requêtes pending → `document_idle` jamais atteint, captures bloquées) + beacon Cloudflare Insights 503 (tiers). Findings P0 marqués résolus dans `docs/test-feedback-2026-05-27.md`. Learning : pour tester un déploiement, viser la prod avec un **navigateur propre** (Playwright `E2E_BASE_URL`) plutôt que claude-in-chrome (extensions du poste).
- 2026-05-27 — **S-042 ✅** UX quick wins (langage, findings test 2026-05-27). **U9** `StatusDot` gagne un `title` (tooltip survol « Confiance : … » → répond à « pourquoi orange ? »). **U2** `BudgetMeter` chiffre le dépassement (« ≈ X € au-dessus du plafond de Y € », rappel « coût d'infra, pas prix de vente »). **U1/U3/U4** hints `options.ts` : sensibilité (public/interne/confidentiel/secret distingués), volume (repères « ≈ docs »), croissance. **U12** `EnsembleView` : label « Scénario » par carte + intro « scénarios « et si », pas des choix imposés » (le testeur croyait qu'on lui imposait le Maroc). Tests : +1 (dépassement budget) → **238 + 8 skip**. typecheck 0, lint 0/0, build OK. **Reste backlog** (`docs/test-feedback-2026-05-27.md`) : U5 (usages avancés à renommer), U7 (médias exemples), U8 (« polyglotte » faux-ami → touche l'identité), U10 (justifier le choix = reco-vivante), U11/U13 → stories dédiées. Learnings : quick wins sûrs = hints/title/textes (zéro impact identité/e2e) ; les renommages de couches (U8) touchent les tests de caractérisation → story à part.
- 2026-05-27 — **S-041 ✅** Compute souverain intégré dans `recommend` (S-033 RESTE — répond au constat « je ne vois pas les serveurs »). `Recommendation.compute: ComputeSizing` ; `recommend(…, computePrices = NEUTRAL_COMPUTE_PRICES)` appelle `computeSovereignCompute`. `applyCompute` (cost.ts) : **compute injecté > 0 → REMPLACE le forfait C6** (30/100/400) par le compute dimensionné+sourcé (note « serveurs souverains dimensionnés : X €/mois (1×PRO2-S…) · LLM à l'usage non chiffré (devis) ») ; **compute neutre → forfait conservé** (invariant tests préservé). Ordre couches : `applyCompute` → `applyMultimodalSizing` (GPU média s'ajoute) → `applyBackup`. `ensemble` + `ResultsView` + `Wizard` injectent `getComputePrices()` → **serveurs VISIBLES** (CostMap via la note C6). `costSources` inclut les sources Scaleway compute. Tests : +5 (`recommend-compute` : neutre→forfait 100, injecté remplace, monotonie HARD≥LIGHT, sources, invariant). typecheck 0, lint 0/0, **237 + 8 skip**, build OK. **Dette transparente** : le LLM tokens à l'usage (jadis noyé dans le forfait) reste NON chiffré → story future (prix tokens live). Learnings : (a) cycle **type-only** `types.ts`↔`compute.ts` (`import type`) accepté par tsc (effacé au runtime) ; (b) garder le forfait comme repli quand compute neutre = zéro régression sur les tests + serveurs réels dès injection ; (c) compute (serveurs) ≠ GPU média (génération) ≠ backup → 3 postes distincts cumulés dans C6, pas de double-comptage.
- 2026-05-27 — **S-031 ✅ — EPIC BACKUP COMPLET (S-026→S-031).** Résultats backup + e2e + nettoyage. `CostMap` : prop `backup?: BackupPlan` → section « Sauvegarde & résilience (déjà comprise dans C6) » (plan criticité/copies/rétention/vecteurs + coût récurrent + **sources cliquables** `backup.costSources` + érasure) ; note « mise en route » enrichie (« + premier full de sauvegarde »). `VerdictView` : note setup MAJ. `ResultsView` injecte `getBackupPrices()` dans `recommend` + `buildEnsemble` → coût backup VISIBLE. `ensemble.ts` threade `backupPrices` (baseline + variants, spread cohérent). Radar **9 axes** déjà OK (S-027). e2e `strate.spec` +1 (profil secret → criticité critique → /resultats : `Plan « critical »` + `crypto-shred` + `Radar des 9 dimensions`) **desktop + mobile** → **8 passés**. Nettoyage : **zéro `toHaveLength(8)` résiduel** (grep). typecheck 0, lint 0/0, **232 + 8 skip**, build OK. ⚠ Dette notée (backlog) : webServer Playwright utilise `next start` alors que `output: standalone` (warning ; tests verts quand même). Learnings : (a) injecter les prix backup dans ResultsView ET ensemble = chiffres cohérents partout (le moteur restait neutre par défaut) ; (b) section CostMap jumelle de « Apport multimédia » = transparence homogène.
- 2026-05-27 — **S-030 ✅** UI Bloc ② Sauvegarde & résilience (epic backup). `components/wizard/BackupBlock.tsx` : RadioCards criticité (Aucune/Standard/Élevée/Critique) + `InfoBubble` neutre ; `<details>` affinage expert (tier, vectorStrategy, érasure via RadioCards ; RPO/RTO/rétention/copies/tests via inputs number ; offsite/airgap/immutable/byok via YesNo) — **les champs affichent la valeur EFFECTIVE** (`buildBackupPlan` dérivé, intègre surcharge conformité), l'override est explicite ; aperçu live « ≈ X €/mois (+ Y € mise en route) » + détail du plan. Intégré dans le **Bloc ② infra** du Wizard ; `setField("backup", …)` propage. `recommend(profile, prices, undefined, getBackupPrices())` → le **budget-mètre reflète le backup**. `evaluateBudget(…, backupMonthlyCost = 0)` + `BudgetMeter` prop `backupMonthlyCost` : si la sauvegarde pèse ≥ médias → cause/levier dédiés (défaut 0 → 7 tests budget existants inchangés). Tests : +4 (`backup-block` : none→absence+pas d'expert, change criticité propage, critique→expert+aperçu, budget backup dominant). typecheck 0, lint 0/0, build OK. Learnings : (a) lier les champs experts au plan dérivé (valeur effective) > stocker des overrides bruts → l'UI montre toujours un plan cohérent (incl. surcharge conformité) ; (b) apostrophes JSX typographiques ’ (lint) dans `<legend>`/`<p>` ; (c) param optionnel `backupMonthlyCost=0` = extension non cassante du budget-mètre.
- 2026-05-27 — **S-029 ✅** Exit Escrow piloté par le `BackupPlan` (epic backup). `lib/exit/bundle.ts` : `BundleManifest.backup: BackupPlan` (cohérence manifest↔plan) ; `runbook.md` section « Sauvegarde & résilience » **dérivée du plan réel** (RPO/RTO via `fmtMinutes`, rétention + plancher légal, 3-2-1-1-0, tier, WAL/PITR, stratégie vecteurs, érasure crypto-shred/expiration, BYOK) + disclaimer « ingénierie, pas juridique » ; `scripts/backup.sh` dérivé (`restic forget --keep-within ${retentionDays}d`, `--tag ${criticality}`, notes conditionnelles object-lock/WAL/air-gap/crypto-shred) ; `criticality none` → runbook + script signalent l'absence de sauvegarde. Tests : +4 (`bundle.test` : manifest↔plan, runbook critique régulé, backup.sh dérivé, cas none) → **228 + 8 skip**. typecheck 0, lint 0/0, build OK. Learnings : (a) le générique « 3-2-1 » figé devient un texte **piloté par les données** du plan (même doctrine que les prix/catalogue) ; (b) les tests existants vérifiaient le runbook par égalité self (zip) → pas de casse en changeant le contenu.
- 2026-05-27 — **S-028 ✅** Intégration backup dans `recommend` (epic backup). `Recommendation.backup: BackupPlan` (requis, rempli par `recommend`). `recommend(profile, prices, catalog?, backupPrices = NEUTRAL_BACKUP_PRICES)` : `buildBackupPlan(profile, sizing, prices.storagePerGbMonth, backupPrices)` → `applyBackup` (cost.ts) impute le **récurrent dans C6** (dont le nom inclut « backup » ; jamais en ligne hors-total), `setupCost += backup.setupCost` (one-time), `computeScores(…, backup)` → `resilience` cohérente (le plan passé explicitement remplace le défaut S-027), `costSources` fusionnées+dédupliquées. **Invariant `none` préservé** : criticité none → `costBackup` court-circuite à 0 → couches inchangées → `totalCost = baseCost + moduleCost`. Prix backup INJECTÉS (défaut `NEUTRAL_BACKUP_PRICES`, pur) ; l'UI injectera `getBackupPrices()` (S-031). Tests : +6 (`recommend-backup` : backup rempli, invariant none même avec prix réels, coût dans C6, setup, resilience critical>none, sources) → **224 + 8 skip**. typecheck 0, lint 0/0, build OK. ⚠ **Compute (S-033 RESTE) NON inclus** (remplacer forfait C6 30/100/400 par `computeSovereignCompute` = change les coûts par défaut + MAJ tests → étape séparée après le bloc backup). Learnings : (a) `BackupPlan` est un sur-ensemble structurel de `BackupCore` → assignable à `computeScores` sans cast ; (b) imputer le backup à C6 (pas une ligne séparée) respecte la doctrine « coûts DANS les couches » (S-018) ; (c) l'invariant none tient sans effort car `costBackup` court-circuite (le défaut neutre n'est même pas sollicité).
- 2026-05-27 — **S-036 ✅** Veille temps réel du catalogue (reco vivante, spec n°3) — couche d'exposition finie. Cœur (déjà fait) : `searchWeb` (Firecrawl `/v2/search`) + `assembleLiveCatalog`/`proposeForSlotLive` (orchestration **pure**, garde-fou `reconcileCatalog`, repli seed par slot, anti-hallucination d'URL DÉFCON 1). **Ajouté ici** : `lib/catalog/cache.ts` (`getLiveCatalog`/`getLiveCatalogCached` : branche l'I/O réelle — `searchWeb` + `callLLM` wrappés en `proposeForSlot` ; **cache TTL court 1 h par profil**, `now`/I/O injectables, clés serveur only) ; route `app/api/catalog/live` (POST profil → `Catalog`, Node runtime `ƒ`, clés Firecrawl+LLM jamais exposées, profil reconstruit sur `DEFAULT_PROFILE` comme la reprise localStorage) ; migration `20260527120000_catalog_observations.sql` (audit trail : 1 ligne/couche, **RLS activée**, pivot `circle_id` null-friendly, CHECK slot/sovereignty/provenance/confidence, `source_url not null` = DÉFCON 1, helper `is_circle_member` réutilisé) ; builder **pur** `lib/catalog/observation.ts` (`buildCatalogObservations` → `CatalogObservationInsert[]`, pattern `conversion/log`) + types Supabase `CatalogObservation{Row,Insert}`. Tests : cache 3 (cache hit = pas de relance, expiration TTL relance, repli seed), observation 5 (7 lignes, source non vide, anonyme nuls, rattachement cercle, report fidèle + **invariant zéro pondération vendor** = aucune clé commission/affiliation), live-catalog +2 (filtre dur secret⇒api-third-party rejeté flagged ; invariant tout recommandé porte une URL) → **261 + 10 skip** (+10 unit, +2 RLS gated). typecheck 0, lint 0/0, build OK (`/api/catalog/live` ƒ, `/resultats` reste **statique**). **Migration validée LIVE** : `db reset` applique sans erreur ; test RLS gaté **2/2 vert** contre Supabase local (isolation cercle A↛B, anon ne lit rien en masse). **Périmètre tenu** : le branchement `ResultsView` (seed→live→recalcul) + provenance UI + snapshot = **S-037** (prd autorité), PAS S-036 → la route est livrée et prouvée, S-037 la consomme. Learnings : (a) garder la couche serveur (`cache.ts`/`live-catalog.ts`) **hors du barrel** `index.ts` (le barrel n'exporte que le pur : seed/reconcile/observation) → pas de fuite de code serveur (callLLM) dans le bundle client, `/resultats` reste statique ; (b) profil POST reconstruit via `{...DEFAULT_PROFILE, ...raw}` après garde `typeof===object` (raw: `object` → spread contribue `{}` → assignable `Profile` sans `as`, miroir de `loadProfile`) ; (c) `db reset` + `stop && start` pour restaurer `auth.uid()` avant le test RLS (learning S-023 confirmé). **Débloque S-037 (snapshot/provenance) et S-040 (assistant).**
- 2026-05-27 — **S-034 ✅** Fondation LLM plateforme (epic reco-vivante, spec n°3). **Proxy vérifié AVANT code** : `proxy.ai-mpower.com` répond 200, modèle `deepseek-v4-flash`, format OpenAI confirmé (PONG). `lib/llm/` : `types.ts` (`LlmMessage`/`LlmResult` ok|reason/`LlmCallOptions`), `client.ts` (`callLLM(messages, opts)` → `POST {baseUrl}/v1/chat/completions`, `fetch` injectable, `AbortController` timeout 30 s, **JAMAIS throw** → `{ok:false,reason}` ; clé/baseUrl lus `process.env.LITELLM_*` **serveur only**, override en opts pour les tests ; `extractContent` type-guard sans `as`/`any`), `index.ts` barrel. Route `app/api/llm/chat` (Node runtime, dynamic, `ƒ`) — le navigateur ne parle qu'à nos routes, clé jamais exposée. `.env.example` += `LITELLM_BASE_URL`/`LITELLM_API_KEY`/`LITELLM_MODEL`. **Secrets posés** : coffre DPAPI local (mes tests) + **Coolify** (`LITELLM_*` runtime=true/buildtime=false/literal=true, prendront effet au prochain déploiement). Tests : +5 (succès, 5xx, réseau KO, réponse vide, config absente → pas d'appel). typecheck 0, lint 0/0, build OK. Learnings : (a) tester `callLLM` avec `new Response(...)` + `fetch` injectable (pas de `as`) ; (b) clé via `process.env` (serveur) + override opts pour la testabilité ; (c) `is_buildtime` (sans underscore) côté API Coolify (422 sinon). **Débloque S-036/S-038/S-039/S-040.**
- 2026-05-27 — **S-035 ✅** Catalogue de composants injecté (reco vivante, spec n°3, 1ʳᵉ story de l'epic LLM). `lib/catalog/{types,catalog-seed,reconcile,index}.ts` créés ; `buildLayers(preset, profile, catalog = seedCatalog(...))` + `recommend(profile, prices, catalog?)` + `buildEnsemble(…, catalog?)` threadent le catalogue. **Identité prouvée** (tests de caractérisation : choix/notes/alternatives/coûts exacts) → 208→**218 + 8 skip** (+10 catalogue). typecheck 0, lint 0/0, build OK. `layers.ts` reste la source des coûts (le catalogue ne porte QUE les choix — DÉFCON 1). **Bloqué pour la suite (S-034/036/038/039/040) : clé LiteLLM absente du coffre** (à ajouter via le protocole coffre). Cf. mémoire [[strate-reco-vivante-llm-plateforme]]. Learnings : (a) transposer `layers.ts` en seed avec alternatives en **tableaux** (pas de split runtime) = round-trip exact garanti ; (b) `reconcileCatalog` réplique le pattern `pricing/reconcile` (live→seed `flagged`+confiance basse) ; (c) le seed n'est PAS une source web → confiance `medium`, calibration interne datée, honnête (DÉFCON 1).
- 2026-05-30 — **FIX lien court partage + QR** (hors Ralph, remontée Amine). Cause racine (debug systématique, reproduit contre la PROD via psql) : un `INSERT … RETURNING` est soumis à la **politique SELECT** ; `shared_reco` n'en a aucune pour `anon` (lecture réservée à la RPC SECURITY DEFINER `get_shared_reco`, anti-énumération) → `.insert().select("id").single()` échouait TOUJOURS pour un anonyme (« new row violates RLS ») → 503. **Fix racine** : id généré SERVEUR (`randomUUID`), insert SANS `.select()` ; `SharedRecoInsert` gagne `id?`. + **QR code** du lien court (`qrcode.react`, rendu LOCAL ; l'URL encodée est trop longue pour un QR fiable → QR réservé au court). Tests route+intégration RLS corrigés (ils « prouvaient » l'ancien comportement faux, gated/skippés en CI). Déployé prod (`26ee408`), vérifié end-to-end (POST→200, GET→encoded). Learnings : (a) sous Postgres, `INSERT…RETURNING` exige une politique SELECT sur la ligne retournée → générer l'id côté app évite de relire ; (b) un test gated/skippé en CI peut masquer un bug réel — la garde RLS doit tourner pour de vrai.
- 2026-05-30 — **i18n MOTEUR : 5 tranches vertes (reliquat S-058 partiel)**. Prose `lib/engine` → descripteurs `Message` (commit+push par tranche, FR identique au runtime, EN natif). **T1** `compliance` (`Engine.diagnostics.compliance.*`) ; **T2a** `risks`+`kmChecks`+`verdict.risk` ; **T2b** `presetReason` ; **T3** `WorkloadLine.estimate` ; **T4** `backup.VectorDecision.reason` ; **T5** résidence `conflict.reason`/`levers`+`interSiteSecurity.note`. Commits `670b942`→`2de5e94`. **Patterns clés (à RÉUTILISER pour modules + verdict restants)** : (a) **type frontière `DisplayVerdict`** (`lib/llm/narrate.ts`) = forme RÉSOLUE (tout chaînes) du `Verdict` moteur ; `ResultsView` résout le verdict (descripteurs) → `DisplayVerdict` AVANT fusion narration → `VerdictView`/`mergeVerdictNarration` n'opèrent QUE sur des chaînes (pipeline LLM string intact, S-059 non requis pour ça) ; (b) **champs COMPOSÉS** (presetReason ; à venir : modules `task`, `scores.moduleBonus`) → émettre les parties en `Message` + **helper pur** prenant le résolveur (`formatPresetReason(reason, resolve)` exporté de l'engine), JAMAIS de prose concaténée dans le moteur ; (c) **frontière LLM** : `presetReason`/`verdict.risk` alimentent `NarrationTexts` (strings) → résoudre via `resolveEngine` dans `ResultsView` avant d'appeler `/api/llm/narrate` (le pipeline narrate/route/prompts reste string, intact) ; (d) **ICU** : pas de `select` sur des clés à chiffre initial (`1to10`…) → clés distinctes (`volume_1to10`) ; `users`/plural OK ; (e) tests de prose → asserter `.id`/`.values` (ou pointer les fixtures vers de vraies clés `Message` pour les tests de rendu, ex. `residency-panel.test`). **3 EXCLUSIONS argumentées (data sourcée, JAMAIS i18n, DÉFCON 1)** : `CostSource.label` (vendor + feed live runtime, structurellement `PriceSource`), `Layer.choice/note/alternatives` (catalogue), `TransferFlag.legalBasis/note` (veille légale seed + LIVE LLM runtime via `lib/legal/live-transfers`) — impossible de mapper du runtime à des clés `Message` prédéfinies, et noms vendor neutres ; même nature que `Layer.choice` (déjà acté string). **RESTE** : (a) MODULES (le + couplé : `MODULES`/`EngineModule`/`ActiveModule` name/why/levels/task + `scores.moduleBonus` ; pièges = `task` composé → helper, `moduleBonus` interpole le nom → `select` ICU sur `modId` + test parité ; ~10 consommateurs dont export/bundle/recap/fiduciary) ; (b) VERDICT complet (`pain`/`gain`/`nextStep`/`firmPriceTier` → `Message`, résolus vers `DisplayVerdict`) + S-059 ; (c) garde ESLint « zéro littéral » + e2e fr↔en. **PROD = MAIN (`d74e3b1`)** : déploiement groupé fait & vérifié (health 200, fr défaut + bascule en OK). ⚠ 1ᵉʳ essai échoué à « Collecting build traces » (OOM transitoire du build container, pas un bug code) → retry OK. Décision Amine : périmètre MAXIMAL ; faire en session fraîche (contexte propre), 1 tranche = 1 commit vert.

- 2026-05-31 — **S-073 tranche 1 ✅** Annuaire fournisseurs souverains PAR CONTINENT (vision « présence mondiale » d'Amine). Contrat pur `lib/engine/providers.ts` (`Continent` europe/north-america/latam/africa/middle-east/apac, `SovereignProvider`, `providersByContinent`/`coveredContinents`) + seed sourcé `lib/pricing/sovereign-providers-seed.ts` (16 fournisseurs, 6 continents : OVHcloud/Scaleway/Outscale/IONOS/Infomaniak/Hetzner UE, UniCloud Africa + iXAfrica×Baobab Afrique, Core42/STC/e& Moyen-Orient, Sakura/NTT/NAVER APAC, Serpro Brésil, OVHcloud Canada). Doc audit `docs/pricing/sovereign-providers-sources.md` + tests `providers.test.ts` (6). **DÉFCON 1** : existence + pays + classe de souveraineté + source datée (URL https + checkedAt) ; AUCUN prix egress fabriqué (devis/veille, tranches suivantes) ; statut légal de transfert inchangé (`lib/legal` conservateur). typecheck 0 / lint 0-0 / 519 tests verts / build OK. RESTE : T2 (rattacher continent/pays aux vecteurs egress S-043), T3 (veille découverte par pays bornée S-072), T4 (UI choix N fournisseurs redondants + topologie), T5 (coûts/transferts via S-044). Learning : un annuaire d'EXISTENCE (sans prix) est le bon découpage DÉFCON 1 quand les prix egress par fournisseur ne sont pas sourçables sans devis — on ne shoehorne pas dans `EgressVector` (qui exige un prix).
- 2026-05-31 — **S-073 tranche 2 ✅** Rattachement continent/pays aux vecteurs egress chiffrés (additif). `EgressVector` étendu (`continent?: Continent` + `country?: string`, optionnels → zéro impact coût) ; 7 vecteurs du seed `residency-seed.ts` annotés (UE×5 : Hetzner/OVHcloud/Scaleway/IONOS/Outscale ; Amérique du Nord×2 : GCP/AWS) ; helper `vectorsForContinent` (moteur + ré-export seed). Tests `residency-continent.test.ts` (4) + 18 residency existants verts. typecheck 0 / lint 0-0 / 523 tests / build OK. Les continents sans vecteur egress chiffré (Afrique/Moyen-Orient/APAC/LATAM) → devis/veille, AUCUN prix fabriqué (DÉFCON 1). RESTE T3 (veille découverte par pays bornée S-072), T4 (UI N fournisseurs redondants + topologie), T5 (coûts/transferts S-044).
- 2026-05-31 — **S-073 tranche 3 ✅** Veille de DÉCOUVERTE de fournisseurs PAR PAYS CIBLE (vision « trouver des fournisseurs dans le(s) pays cible(s) »). `lib/residency/discovery.ts` (pur, I/O injectée, miroir du pattern catalogue) + `discovery-cache.ts` (câblage réel SearXNG/Crawl4AI + LiteLLM, repli annuaire seed tranche 1 filtré, cache TTL 1 h) + route `POST /api/residency/providers` (continent+country+profile → fournisseurs sourcés). **Contrainte S-072 ABSOLUE** appliquée 3× : oriente la requête web, règle DURE du prompt, ET re-filtre la sortie (`providerViolatesConstraints` : souveraineté exigée ⇒ rejet hyperscaler — on ne rejette QUE sur choix explicite). Garde-fous DÉFCON 1 : anti-hallucination d'URL (source ∈ résultats), AUCUN prix egress fabriqué, statut légal de transfert hors veille (lib/legal conservateur). Repli seed garanti → toujours un ensemble conforme + sourcé. Tests `discovery.test.ts` (12). typecheck 0 / lint 0-0 / 535 tests / build OK (route compilée). RESTE T4 (UI choix N fournisseurs redondants + topologie multi-juridiction), T5 (coûts/transferts via S-044). Learning : réutiliser `deriveCatalogConstraints` (source unique S-072) mais une contrainte fournisseur dédiée (sovereignty hyperscaler) car l'union ProviderSovereignty diffère de ComponentSovereignty (api-third-party).
- 2026-05-31 — **S-073 tranche 4 ✅** UI Redondance multi-continent (`components/results/RedundancyPanel.tsx`). Sélecteur continent (6) + pays cible → `POST /api/residency/providers` (profil porté → la contrainte S-072 voyage) → liste sourcée (nom, pays, badge souveraineté, badge provenance live/seed, lien source daté) → cases à cocher pour composer une topologie redondante → résumé (N fournisseurs / M localisations distinctes + message redondance multi-fournisseur). Entièrement i18n (`Results.redundancyPanel` fr/en + ar brouillon MSA à relire par Amine) ; rendu dans `ResultsView` après `ResidencyPanel` ; parse défensif sans `as`. Câblé sur `activeProfile`. Tests `redundancy-panel.test.tsx` (3 : rendu+contrainte, recherche→sélection→redondance via fetch mock, erreur réseau). typecheck 0 / lint 0-0 (garde i18n verte) / 538 tests / build OK (/resultats 22,4 kB). RESTE T5 (caveat coûts=devis + transferts inter-juridiction conservateurs S-044) → ensuite S-073 COMPLET + déploiement.
- 2026-05-31 — **S-073 tranche 5 ✅ → S-073 COMPLET** Caveats sur la topologie redondante composée (RedundancyPanel). (1) `legalCaveat` affiché dès ≥ 2 juridictions distinctes sélectionnées : « transferts inter-juridiction à valider avant décision, défaut conservateur, voir Résidence & transferts ; orientation d'ingénierie, pas un avis juridique » — réutilise la doctrine `lib/legal` S-044 (jamais de base légale par pays fabriquée). (2) `costCaveat` toujours affiché : « coûts sur devis par fournisseur, jamais figés ; egress chiffré pour UE + Amérique du Nord, ailleurs à confirmer ». i18n fr/en/ar. Test redundancy-panel enrichi (assertions caveats). typecheck 0 / lint 0-0 / 538 tests / build OK. **S-073 passes=true (T1→T5)** : couverture multi-continent + découverte par pays cible bornée S-072 + redondance multi-fournisseur, DÉFCON 1 bout-en-bout (aucun prix egress fabriqué, statut légal conservateur). Reste backlog : S-075 (CI/CD Coolify, UI 1-clic + watch_paths — non automatisable par API).
- 2026-05-31 — **S-075 (CI/CD) — câblage auto-deploy par API/DB.** Constat : `is_auto_deploy_enabled` était DÉJÀ `true` pour mnemo (app 13), mais la source pointait sur « Public GitHub » (`source_id=0`) au lieu de la GitHub App `coolify-afristrat` (id 1, org Afristrat, installée — 9 autres apps Afristrat l'utilisent déjà) → aucun webhook ne livrait les push. Correctif (réversible) : `update applications set source_id=1 where id=13` (aligné sur les autres apps). Coolify public joignable (`coolify.ai-mpower.com/api/health` 200) → GitHub livre le webhook de l'App. CE commit doc sert de test live : un déploiement doit apparaître sans appel API.
- 2026-05-31 — **S-075 — webhook manuel câblé (auto-deploy).** GitHub App coolify-afristrat installée sur dépôts SÉLECTIONNÉS sans mnemo (ajout = scope admin:org indispo non-interactif) → bascule sur **webhook manuel repo→Coolify** (entièrement pilotable par API repo-admin). source_id remis à 0 (Public GitHub, combinaison cohérente) ; webhook `Afristrat/mnemo` (id 634221541) → `coolify.ai-mpower.com/webhooks/source/github/events/manual`, event push, secret = `manual_webhook_secret_github` existant (injecté au runtime, jamais affiché). Ping GitHub → **200 OK** (signature validée). is_auto_deploy_enabled déjà true. CE push teste le déclenchement réel.
- 2026-05-31 — **S-075 (CI/CD auto-deploy) ✅ — fait 100% par API/DB/CLI, zéro geste UI** (réponse à « tu as l'API Coolify, fais-le seul »). Diagnostic : `is_auto_deploy_enabled` déjà true mais source = Public GitHub (`source_id=0`) → aucun webhook. GitHub App org sur dépôts sélectionnés sans mnemo (ajout = `admin:org` indispo) → **webhook MANUEL repo→Coolify** : webhook `Afristrat/mnemo` (id 634221541) → `/webhooks/source/github/events/manual`, push, json. **Piège résolu** : `manual_webhook_secret_github` est CHIFFRÉ en base (cast Laravel `encrypted`, ciphertext 256c) → secret EN CLAIR (40c) via `php artisan tinker` (le modèle déchiffre) ; le ciphertext → « Invalid signature ». `watch_paths` (inclusion code) posé par API PATCH. **Vérifié bout-en-bout** : push code (lib/) → 'Deployment queued' + deploy `gwh1rxup…` finished + health 200 ; push doc-only (.ralph/) → 'Changed files do not match watch paths. Ignoring deployment'. Secret jamais affiché (capté en variable). **BACKLOG VIDE : 0 story passes:false.** Anti-leak respecté (preuve = code HTTP / message, jamais la valeur).
- 2026-05-31 — **S-076 ✅ Géographie cohérente CONTINENT → PAYS** (retour Amine : « pas de continents hors Europe, États-Unis c'est un pays comme Maroc »). Refonte de l'axe géographique fondateur. **Modèle** : `lib/engine/geography.ts` (catalogue `JURISDICTIONS` : 6 continents, 22 pays ; `deriveZone(pays)` → bucket juridique ue/maroc/us/other ; `geographyFromZone` pour réconcilier les profils legacy). `Profile` gagne `continent`+`country` requis ; **`zone` DÉRIVÉE du pays** → le moteur + `lib/legal` restent INTACTS (zéro régression). `Continent`/`CONTINENTS` déplacés dans `types.ts` (canonique, supprime le cycle providers↔types). **Wizard** : sélecteur Continent (6) + Pays (dépendant du continent), `setGeography` pose continent+pays+zone de façon cohérente ; `useOptions.countriesFor(continent)`. **Récap** : ligne « continent · pays » (précise + précision libre si pays « Autre ») au lieu du bucket zone. **RedundancyPanel** s'ouvre sur le continent choisi. **i18n** fr/en/ar (Options.continent 6, Options.country 22, Wizard.fields.continent/country + info, recap.label.geography ; parité). **DÉFCON 1** : AUCUN statut juridique par pays fabriqué — légal précis UE/Maroc/US, sinon bucket `other` conservateur « à valider par un conseil ». Cascade Profile : ~40 fabriques de test + presets + quick-mapper + share encode/decode (continent/country font le round-trip). Tests : `geography.test.ts` (6) + wizard géo (2) + recap adapté. typecheck 0 / lint 0-0 / 546 tests / build OK. Learning : rendre `zone` dérivée (pas saisie) = refonte UX SANS toucher au moteur/légal.
- 2026-05-31 — **S-076 suite : découverte fournisseurs reliée au pays choisi + transferts live confirmés.** (1) `RedundancyPanel` pré-remplit le pays cible depuis `profile.country` (pays concret → libellé localisé via `jurisdictionFor`+Options ; bloc UE / « Autre » → vide) et s'ouvre sur `profile.continent` → le choix continent/pays du configurateur surface directement les fournisseurs souverains du pays (lien S-076 ↔ S-073). Tests : pré-remplissage Maroc + non-pré-remplissage UE. (2) **Transferts live = DÉJÀ câblés** (`ResidencyPanel` fetch `/api/legal/transfers`, merge bases réconciliées, pastilles liveFresh/liveFlagged) — vérifié prod (endpoint 200, bases DPF UE-US en direct). Rien à refaire. (3) DR/résidence : le picker de région reste volontairement coarse (4 buckets juridiques UE/Maroc/US/autre) — DÉFCON 1, on ne fabrique pas de précision légale/coût par pays ; la richesse par pays vit dans la découverte de fournisseurs. typecheck 0 / lint 0-0 / 548 tests / build OK.
- 2026-05-31 — **Homepage pricing : CTA déplacé + vrai comparatif repliable** (retour Amine : le CTA « Commencer le diagnostic » ne doit pas être réservé à la carte Pro). (1) Retrait de `hasCta` de la carte Pro → **CTA unique centré EN BAS** de la section tarifs (`Home.pricing.cta`). (2) **Vrai comparatif** : matrice `COMPARE_ROWS` (11 capacités × 4 paliers Diagnostic/Essentiel/Pro/Souverain+, ✓/—) rendue en `<table>` (header = nom+prix par palier). (3) **Hide/unhide natif** via `<details>`/`<summary>` (zéro JS, SSR, compatible garde i18n) + chevron rotatif. a11y : aria-label ✓/— i18n (compareYes/compareNo). i18n fr/en/ar (pricing.cta, compareSummary, compare.* 11, compareYes/No ; parité). typecheck 0 / lint 0-0 / 548 tests / build OK.- 2026-06-01 — **Session 10 : 3 bugs confirmés (revue Amine, session 9) corrigés, déployés au fil.** **① Verdict trop pauvre** (`recommend.ts buildVerdict`, commit `aa27b39`) : `risk`/`gain`/`firmPriceTier`/`nextStep` étaient identiques entre deux profils opposés. Désormais : `risk` = 1re incohérence détectée sinon risque STRUCTUREL (`salientRisk` : secret/secret-pro → `verdict.risk.secret` ; régimes → `verdict.risk.regulated` plural ; `preferSovereign` → sovereignty) ; `gain` + `firmPriceTier` = `select` ICU sur le preset (firmPriceTier abandonne le « 290 € » inventé → niveau d'offre seul, DÉFCON 1) ; `nextStep` = 4 cas (setup média × régime de conformité) ; **nouveau `Verdict.constraints`** (descripteurs purs preset+sensibilité+régimes+souveraineté+géo) rendu en chips dans `VerdictView`. Cascade : `types.ts`, `narrate.ts` DisplayVerdict (préservé par `mergeVerdictNarration`), `ResultsView`, `VerdictView` + `constraintsLabel`, messages `Engine.verdict.*` fr/en/ar. Tests : différenciation du verdict selon le profil (recommend-sizing +3, results +1). **② Prix LLM non per-1M** (commit `80b5f68`) : le panneau de fraîcheur (`PriceFreshness`) affichait l'empreinte BRUTE scrappée (« $0.01 · $0.02 … ») pour les couches LLM (Anthropic couche 1, Mistral couche 4). Correctif : `llm-token-seed.ts` ajout **Anthropic Claude Sonnet 4.6** (3 $/1M in, 15 $/1M out — relevé page officielle 2026-06-01, sourcé+daté, DÉFCON 1) + export `LLM_FRESHNESS_TOKEN_USD` ; `feed.ts` `PriceObservation.tokenPricing` + `withLlmTokenPricing` route les couches 1/4 vers le per-1M et efface l'empreinte brute (vendeurs infra Qdrant/Supabase/Scaleway inchangés) ; `PriceFreshness` rend « entrée $X/1M · sortie $Y/1M » (i18n `Results.freshness.tokenInOut`). Tests feed (+3). **③ /resultats ne s'idle jamais** (commit `86a4822`) : les 3 fetchs live (narration LLM, veille catalogue, prix live) n'avaient AUCUN timeout → page bloquée. Correctif : `lib/utils/fetchWithTimeout.ts` (AbortController, défaut 10 s, compose avec signal externe) ; câblé dans `ResultsView` (×3) + `LivePriceStatus` + `PriceFreshness`. Test util (3, faux timers). Chaque bug : typecheck 0 / lint 0-0 / build OK / ICU validé ×3 locales ; commit + push (auto-deploy) ; **déploiements `aa27b39` + `80b5f68` = finished vérifiés (base Coolify), health 200**. Tests cumulés : 569 + 18 skip. **Pattern réutilisable** : `fetchWithTimeout` pour tout appel live navigateur (repli garanti) ; un nouveau champ de `Verdict`/`DisplayVerdict` cascade dans les 2 littéraux de test (narrate.test, results.test) + ResultsView resolve + VerdictView ; prix vendor jamais hardcodé → vérifier la page officielle (URL+date+confiance) avant d'ajouter au seed.
- 2026-06-01 — **S-077 (régimes dynamiques) : spec VALIDÉE (3 décisions Amine) + socle pur livré (tranches 1-2, commit `864d4be`).** Décisions Amine verrouillées dans la spec : ① entrées = pays cible **+ résidence des clients** (`profile.clientResidence?`) ; ② moteur = **les deux** (modéliser quelques régimes phares hors-UE avec actions sourcées **+** afficher les non modélisés en sourcé-seul) ; ③ **persister un audit RLS** (`regime_observations`, pivot circle, comme S-036/S-062 ; migration prod manuelle). **Tranche 1** `lib/legal/regime-seed.ts` : type `RegimeSuggestion` (`code` mappable énum moteur | null, scope, pays, source, confiance, provenance) + seed des régimes notoires de **16 juridictions** (sources OFFICIELLES vérifiées web 2026-06-01 : RGPD+AI Act UE, nLPD CH, UK GDPR, HIPAA+CCPA US, PIPEDA CA, 09-08 MA, NDPA NG, DPA-2019 KE, POPIA ZA, LGPD BR, LFPDPPP MX, APPI JP, PIPA KR, DPDP IN, PDPL AE/SA) ; `regimesSeedFor`/`regimesSeedForCountries` (union cible+résidences, dédup). DÉFCON 1 : existence sourcée+datée, jamais d'avis, repli garanti, `autre-*`→[]. **Tranche 2** `lib/legal/regime-discovery.ts` (pur, I/O injectée, calque `residency/discovery.ts`) : recherche web→synthèse LLM→régimes sourcés ; **anti-hallucination d'URL**, `mapRegimeCode` (rgpd/gdpr/uk-gdpr→rgpd, ai act→aiact, hipaa→hipaa, cndp/09-08→cndp), dédup, **confiance live=low** (conservateur sur le droit, doctrine S-062), `[]` si I/O KO ; jamais d'avis juridique. Tests : seed (6) + discovery (10). typecheck 0 / lint 0-0 / build OK. **RESTE (sequence interne spec)** : **T3** `regime-discovery-cache.ts` (câblage SearXNG/Crawl4AI+LiteLLM + fusion live∪seed + cache TTL) + route `POST /api/legal/regimes` + **persistance `regime_observations` (migration RLS + application prod manuelle SSH/psql)** ; **T4** `profile.clientResidence?` (additif) + wizard (bouton « Détecter pour {pays} » + multi-input résidence clients + pré-cochage des régimes mappables, cases manuelles maîtres = repli) ; **T5** i18n fr/en/ar + **modélisation moteur de quelques régimes phares** (LGPD/APPI → actions `computeCompliance` sourcées) + e2e léger. Pattern : calquer S-036 (catalog) / S-062 (legal) pour cache+persist ; migration prod = `cat mig.sql | ssh serveurai 'docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1'`.
- 2026-06-01 — **S-077 tranche 3 ✅ (cache+fusion+route+audit RLS), VÉRIFIÉE END-TO-END PROD (commit `5cfcf3a`).** `regime-feed.ts` : assemblage serveur (SearXNG/Crawl4AI+LiteLLM) + **fusion PURE `mergeRegimes`** (seed = RÉFÉRENCE, la veille n'AJOUTE que les régimes absents du seed par clé pays+nom, confiance live=low — doctrine conservatrice du droit S-062) + cache TTL 6 h ; `source` seed|live|mixed. Route **`POST /api/legal/regimes`** : `{ country, residences[] }` → régimes sourcés (pays cible + résidence clients) ; clés scraper/LLM côté SERVEUR ; repli seed garanti ; jamais d'avis. **Persistance audit (décision Amine #3)** : migration RLS `regime_observations` (pivot circle, `is_circle_member`, jumeau `transfer_status_observations`) **APPLIQUÉE PROD** (RLS=on vérifié `relrowsecurity=t`, 2 policies) ; builder pur `buildRegimeObservations` + `persistRegimeObservations` (live seulement, jamais bloquant) ; types Supabase `RegimeObservation{Row,Insert}`. Prompt **`regime-veille`** ajouté au registry (gabarit canonique = source unique, `regime-discovery` l'importe → zéro dérive) + PROMPT_META. **VÉRIF PROD** : `POST /api/legal/regimes {maroc, residences:[bresil]}` → `source=mixed`, **7 régimes** = 2 seed (CNDP, LGPD) + **5 découverts live sourcés** (09-08 + cybersécurité 05-20 Maroc ; LGPD + Marco Civil + Code conso Brésil), confiance low, mapping `cndp` OK ; **audit `regime_observations` = 5 lignes toutes `live`** (seed correctement non persisté = gating OK). Tests : feed/fusion+cache (5), observation (2), persist gatée (4), route validation (3), RLS intégration gatée (skip). typecheck 0 / lint 0-0 / **599 tests** / build OK. ⚠ Cosmétique : un nom de régime DÉCOUVERT EN LIVE peut arriver mal encodé (mojibake « ProteÃ§Ã£o » via scraper/LLM) — n'affecte PAS le seed (référence propre) ; à nettoyer en T4/T5 si gênant à l'affichage. **RESTE S-077** : **T4** profil `clientResidence?` (additif) + wizard (bouton « Détecter pour {pays} » + multi-input résidence clients + pré-cochage régimes mappables, cases manuelles maîtres) ; **T5** i18n fr/en/ar + modélisation moteur LGPD/APPI (`computeCompliance` sourcé) + e2e léger.
- 2026-06-01 — **S-077 tranche 4 ✅ (UI configurateur), EN LIGNE & vérifiée prod (commit `293e6a2`).** La veille T3 devient actionnable dans le wizard. **`Profile.clientResidence?`** (codes pays) : ADDITIF — un régime suit souvent la résidence des clients (RGPD=sujets UE, LGPD=BR), pas l'hébergement ; **aucune cascade moteur** (ni coût ni zone) ; round-trip share encode/decode (décodeur étendu, défensif) + persistance localStorage. **`components/wizard/RegimeDetector.tsx`** : sous le champ régimes, un `<details>` avec multi-input « pays de résidence des clients » (`useOptions.allCountries`) + bouton « Détecter pour {pays} » → `POST /api/legal/regimes` (borné `fetchWithTimeout`) → **PRÉ-COCHE** les régimes mappables (cndp/rgpd/aiact/hipaa) via `addRegulations` + **LISTE** les régimes sourcés (lien + StatusDot confiance + « à valider par un conseil ») ; cases manuelles MAÎTRES (repli) ; jamais d'avis. `useWizardProfile` : `addRegulations` (pré-cochage sans doublon, retire « Aucun », non destructif) + `toggleClientResidence`. i18n `Wizard.regimeDetector` fr/en/ar. Tests : hook (3), RegimeDetector (3 : rendu, pré-cochage+liste via fetch mock, erreur réseau), share round-trip clientResidence. typecheck 0 / lint 0-0 / **605 tests** / build OK. **VÉRIF PROD** : `/configurateur` 200 + texte du détecteur présent 4× dans le HTML servi (SSR) = T4 en ligne. ⚠ NOTE INFRA : pendant cette tranche, **SSH au serveur (192.168.100.8:22) a timeout** (blip réseau ou IP DHCP changée après reboot) — l'app reste saine via le tunnel public (health 200) ; **re-vérifier l'IP LAN du serveur au prochain besoin d'opération SSH/DB** (cf. mémoire `mnemo-infra-credentials`, IP dynamique). **RESTE S-077 (T5, dernière)** : modélisation moteur de 2-3 régimes phares hors-UE (LGPD/APPI) dans `computeCompliance` (actions de conformité SOURCÉES — DÉFCON 1 : sourcer les obligations réelles par régime, sinon affichage-seul) + i18n des actions + e2e léger. Ensuite S-077 COMPLET → Lot 2 B.
- 2026-06-04 — **S-094 ✅ Restore Drill certifié (moat « chaîne de preuve », vague 2 #2).** La sortie Exit Escrow devient TESTABLE, pas seulement générée. **DÉFCON 1 fondateur** : Strate ne fabrique JAMAIS un RTO — elle fournit l'instrument et VÉRIFIE le certificat produit. **Deux modes offerts à l'utilisateur** (décision Amine « 1 et 2 au choix avec explications claires ») : **A** = drill local sur données réelles (RTO réel de bout en bout, fidélité max) ; **B** = répétition à blanc automatisée en CI jetable sur données SYNTHÉTIQUES (RTO non représentatif, limite dite clairement). Choix de conception : mode B = runner CI jetable, PAS de sandbox côté Strate (surface sécurité/coût ingérable + RTO synthétique côté Strate sans valeur ; sandbox côté Strate = piste vague 3 documentée, non retenue). **Modules PURS** lib/restore-drill/ : certificate.ts (parse anti-injection sans cast + verifyCertificate re-hash SHA-256 isomorphe via lib/decision/integrity + checklist agrégée, ne lève jamais) ; kit.ts (restore-drill.sh chronométrant le RTO + .github/workflows/restore-drill.yml + DRILL.md expliquant A/B + drill/seed/atom-001.md synthétique + restore-certificate.schema.json) ; render.ts (MD du certificat vérifié). Kit fusionné dans buildExitBundle (Object.assign sur files + EXPECTED_FILES mis à jour : les tests assertent l'ensemble EXACT des fichiers, l.45/237). UI components/results/RestoreDrillPanel.tsx (2 modes expliqués + collage/upload du certificat → vérif in-app + verdict), monté après ExitEscrow dans ResultsView. i18n Results.restoreDrill fr/en/ar. +13 tests (certificate 5, kit 5, render 1, bundle 1, panneau 1). typecheck 0 / lint 0-0 / **718 tests** / build OK. **Pattern** : artefact opposable = builder pur + hash SHA-256 isomorphe (computeIntegrityHash) + render pur + panneau de vérif — réutilise exactement le pattern Decision Record S-092. **Piège i18n consigné** : un placeholder contenant une accolade « { » casse le parseur ICU (MALFORMED_ARGUMENT) → bannir les accolades littérales dans les valeurs de messages.

- 2026-06-04 — **S-095 ✅ Preuve de résidence continue (moat « chaîne de preuve », vague 2 #3) → VAGUE 2 COMPLÈTE.** Prouve que les promesses de résidence tiennent sur les composants à EMPREINTE GÉOGRAPHIQUE CONNUE, dans le temps. **DÉFCON 1 fondateur** : on ne fabrique JAMAIS la juridiction d'un composant — les couches auto-hébergées sont en région primaire par construction (in-zone) ; les réplicas DR ont une région connue + base légale datée (réutilise les transfers déjà calculés par le moteur via lib/legal) ; le backup hors-site et la supervision n'ont pas de région pinnée → flag « unverified » (à confirmer), jamais inventé. **Modules PURS** lib/residency/ : continuity.ts (auditResidencyContinuity → report par composant {region|null, flag in-zone/restricted/out-of-zone/unverified, legalBasis?}, outOfZoneCount + unverifiedCount) ; evidence.ts (buildResidencyEvidence → MD + JSON + empreinte SHA-256 isomorphe via lib/decision/integrity = maillon opposable) ; continuity-observation.ts (builder PUR de la ligne DB) ; continuity-persist.ts (server-only, non bloquant, jumeau regime-persist). Migration residency_continuity_observations (RLS pivot circle, jumelle transfer_status_observations) — ⚠ À APPLIQUER MANUELLEMENT en prod (hors-LAN → terminal web Coolify base64). Route POST /api/legal/residency-continuity (re-hash SERVEUR jamais confiance au hash client + persist, rate-limitée S-093). UI components/results/ResidencyContinuityPanel.tsx (carte par composant + drapeaux + bouton evidence pack horodaté+haché, persiste en best-effort), montée après ResidencyPanel dans ResultsView. i18n Results.residencyContinuity (panneau) + Engine.residencyContinuity (descripteurs moteur) fr/en/ar. +9 tests (continuity 4, evidence 2, observation 2, panneau 1). typecheck 0 / lint 0-0 / **727 tests** / build OK. **La feature marche SANS la migration** (audit live + evidence pack fonctionnent) ; la persistance n'ajoute que la traçabilité de la dérive. **Pattern (3e application)** : artefact opposable = builder pur + hash SHA-256 isomorphe + render pur + panneau ; persistance d'audit = builder pur + persist server-only non bloquant + route re-hash + migration RLS jumelle. Hors périmètre (vague 3) : sandbox côté Strate, signature HMAC, alertes de dérive. **Vague 2 (chaîne de preuve) COMPLÈTE : #1 Decision Record (S-092) + #2 Restore Drill (S-094) + #3 Résidence continue (S-095).** « Strate ne recommande pas, il PROUVE. »

- 2026-06-05 — **S-096 ✅ Lock-in / Exit-Cost Meter (moat « chaîne de preuve », vague 2 #4).** Chiffre le COÛT DE SORTIE à côté du coût mensuel — l'asymétrie de sortie que les plateformes masquent (moat défensif : un concurrent commissionné ne peut pas l'afficher). **DÉFCON 1** : l'egress de rapatriement est chiffré UNIQUEMENT depuis un vecteur d'egress SOURCÉ + daté (exactement le même que le moteur résidence : selectEgressVector × hostingClassForProfile) = reco.sizing.storageGb × egressPerGb ; les frais d'API de sortie et la durée d'engagement mini = « unknown » → « à confirmer au contrat vendor », JAMAIS fabriqués. lib/exit-cost/meter.ts (PUR : computeExitCost(reco, profile, residencyPrices) → ExitCost{storageGb, egressPerGb, egressSourced, egressSource daté, exitEgressCost one-shot, monthlyCost, monthsEquivalent « ≈ N mois de service », apiFees/minCommitment unknown, disclaimer}). components/results/ExitCostPanel.tsx (egress chiffré + source datée cliquable OU « non sourcé → devis » ; équivalent en mois ; bloc « à confirmer » ; disclaimer i18n — le panneau utilise t() localisé, pas la chaîne FR du module pur). Monté après RestoreDrillPanel dans ResultsView. i18n Results.exitCost fr/en/ar. +5 tests (meter 4, panneau 1). typecheck 0 / lint 0-0 / build OK. Learning : un egress GRATUIT (OVH no-egress-fees, source présente, montant 0) est un BON signal anti-lock-in, affiché tel quel ; un panneau qui montre un disclaimer doit tirer t() localisé (la chaîne du module pur sert un futur export figé, pas l'UI multilingue).

- 2026-06-05 — **S-097 ✅ MBOM — Memory-Base Bill of Materials (moat « chaîne de preuve », vague 2 #6).** Manifeste signé type SBOM des ingrédients de la base mémorielle : composants sourcés + checksum SHA-256 par fichier du bundle + empreinte globale. **DÉFCON 1** : checksums CALCULÉS (jamais simulés) ; licences/versions non confirmées = null → « à confirmer » (jamais fabriquées). lib/mbom/manifest.ts (buildMbom(bundle) ASYNC : composants dérivés du manifeste Exit Escrow — couches + catalogue retenu (provenance/sourceUrl par slot c0..c6→couches) ; checksum sha256Hex par fichier ; empreinte globale canonicalJson+sha256 ; réutilise lib/decision/integrity). render.ts (PUR, MD : tableaux composants + checksums + empreinte + disclaimer). **Choix d'archi clé** : HORS buildExitBundle (qui est SYNCHRONE, les checksums sont async Web Crypto) → MBOM = artefact CLIENT async (comme le Decision Record / le certificat de drill), construit à partir du bundle déjà généré → **ZÉRO régression** sur l'Exit Escrow et EXPECTED_FILES inchangé. components/results/MbomPanel.tsx (aperçu async : n composants / n fichiers + empreinte sha256 ; téléchargement MD + JSON), monté après ExitEscrow. i18n Results.mbom fr/en/ar (clés panneau + clés du rendu MD). +7 tests (manifest 4, render 2, panneau 1). typecheck 0 / lint 0-0 / 739 tests / build OK. Hors périmètre : signature HMAC/Ed25519 + versions exactes par composant (non sourçables) — l'empreinte SHA-256 auto-vérifiable suffit. **Pattern (4e/5e application de la chaîne)** : builder pur/async + hash SHA-256 isomorphe + render pur + panneau. Avec S-096 (Lock-in Meter), la vague 2 « chaîne de preuve » compte désormais 5 artefacts : Decision Record (S-092), Restore Drill (S-094), Résidence continue (S-095), Exit-Cost Meter (S-096), MBOM (S-097).

- 2026-06-05 — **DETTE CONNUE & ASSUMÉE (décision Amine) — flakiness e2e pré-existante.** Constat lors de la vérif vague 2 : la suite `npm run test:e2e` est **flaky** — un test DIFFÉRENT échoue selon les runs (`i18n.spec` arabe lang=ar ; `strate.spec:100` intake libre mobile), jamais de façon déterministe ; chacun **passe en isolation** (i18n 3/3, etc.). **Cause racine** : le `webServer` de `playwright.config.ts` lance `npm run build && npm run start`, or `next start` est **incompatible avec `output: standalone`** de `next.config` (avertissement explicite au démarrage : « "next start" does not work with "output: standalone" configuration. Use "node .next/standalone/server.js" instead »). → le serveur de test sert un build dégradé → courses de timing au reload/hydratation sous 9 workers parallèles. **Touche aussi la CI** (`.github/workflows/ci.yml` → `npm run test:e2e`). **NON introduite par la vague 2** (les régressions de sélecteurs que la vague 2 avait introduites — `/RGPD chap. V/` ×2 et bouton `/Markdown/` ×2 — ont été corrigées et vérifiées déterministes, commit `878dbe2`). **Fix recommandé (option A, à faire dans un cycle dédié)** : servir le build standalone correctement dans le `webServer` (`node .next/standalone/server.js` + copie des assets `public/` et `.next/static/` dans `.next/standalone/`, cross-plateforme Windows local / Linux CI) ; mitigation rapide possible (option B) : `retries: process.env.CI ? 2 : 0` dans `playwright.config.ts` (dé-flake la CI sans masquer un vrai échec, qui rate les 3 essais). **Décision Amine (2026-06-05)** : tracer comme dette connue pour l'instant, ne pas faire la chirurgie d'infra e2e maintenant. À reprendre en cycle dédié (candidat prioritaire avant toute ouverture publique élargie, car impacte la fiabilité CI).

- 2026-06-05 — **DETTE FLAKINESS E2E : RÉSOLUE À LA RACINE (corrige l'hypothèse précédente, qui était FAUSSE).** Débogage systématique (Iron Law : cause racine avant fix). **Expérience décisive** : la suite en SÉRIE (`--workers=1`) passe 41/41 avec EXACTEMENT le même serveur `next start`/standalone → l'hypothèse « `next start` incompatible `output: standalone` » (notée plus haut) est **réfutée** : `next start` sert depuis `.next/` (l'avertissement est advisory ; le build standalone existe en parallèle). **Vraie cause racine** (lue dans l'`error-context.md` Playwright, PAS devinée) : **courses d'hydratation dans 2 tests**, amplifiées par ~9 workers parallèles sur une machine 22 cœurs (1 seul serveur Next). (1) `strate.spec:100 intake libre` : le bouton « Analyser » est gaté par l'état React `text` (`disabled={... || text.trim()===""}`) ; un `fill()` arrivé AVANT que React n'attache `onChange` est perdu → bouton `disabled` à vie → `click` timeout 30 s. (2) `i18n.spec:43 arabe` : `LocaleSwitcher` fait `router.refresh()` (RSC async) après cookie ; sélection pré-hydratation perdue OU refresh > 5 s sous charge → `toHaveAttribute(lang=ar)` échoue. **L'appli est CORRECTE** (composants contrôlés + refresh RSC) ; le défaut était dans les TESTS (interaction avant hydratation). **Fix** (condition-based-waiting, `expect.toPass`) : on (re)remplit / (re)sélectionne jusqu'à ce que l'effet React soit appliqué (bouton activé / `lang=ar`), au lieu de courir l'hydratation. Aucune modification appli, aucun worker-cap, aucun timeout masquant. **Vérification sous stress** : série ✓ 41/41 · `--repeat-each=3` @ 9 workers ✓ **123/123** (contention PLUS dure que le run qui flakait) · run normal ✓ 41/41 · lint 0-0 · typecheck 0. Commit des fixes e2e ci-dessous. **La flakiness e2e n'est plus une dette** : courses éliminées à la source, suite stable même sous parallélisme maximal. Leçon : ne jamais figer une cause racine « plausible » sans l'expérience qui l'isole (ici, `--workers=1` + lecture de l'`error-context`) — l'avertissement le plus visible (`next start`) n'était pas la cause.
